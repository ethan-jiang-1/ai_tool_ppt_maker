# 05-delivery

This target method module is the only owner of common final-manifest validation,
final projection, slide-canvas-image PPTX assembly, source-owned notes injection,
and delivery review. It accepts equivalent Framed and Pure manifest provenance
without branching on workflow semantics.

Notes-only refresh rebuilds delivery lineage without changing pixels.
