# Contract Closure Matrix

`Closed in plan` means the target decision and a future test obligation are explicit. It does not mean
code, a delta spec, or an OpenSpec change exists.

| Area | Current risk | Target decision | Evidence | Status |
|---|---|---|---|---|
| Protocol resolver | Missing/mismatched current state could be treated as legacy | Four disjoint current/legacy/repair classifications | active + legacy contracts | Closed in plan |
| Source ownership | Free prompt prose could ask Framed Image2 to render text | Provider-safe recipe/composition/motif registry only | active protocol | Closed in plan |
| Visual-language registry | Registry bytes could become an unowned or stale prompt authority | Dedicated schema, compatibility algorithm, semantic digest, and no-override rule | visual-language registry contract | Closed in plan |
| Text constraints | Pure text could conflict with no-text constraints | Authority-aware constraint validation; no silent drop | active protocol | Closed in plan |
| Agent identity | Labeled five-pose sheet could reach provider or HTML catalog could shadow it | Doctrine sheet plus clean role derivative registry | Agent contract | Closed in plan |
| Final pixels | Canvas header lock or a public compositor could become a second finalization route | `finalizePage(...)` is the only external Interface; compositor is a private adapter | parent + active + legacy contracts | Closed in plan |
| Raw review | Finalization could consume partial or stale evidence | Exact triple coverage plus projection/profile freshness | review/versioning contract | Closed in plan |
| Structural reuse | Post-apply raw copying could bypass preview/hash | Materialization map is an atomic part of apply | review/versioning contract | Closed in plan |
| Legacy adoption | Unknown/corrupt source could be adopted | Exact recognized legacy pairs only; all else repair/export | legacy contract | Closed in plan |
| Readiness | Doctor could require credentials for local Framed refresh | Closed unbound/run-bound/operation profile contract | active protocol | Closed in plan |
| Main specs | Capability-level cleanup can miss old requirements | Exact requirement ledger, owner, and test obligation | retirement ledger | In audit |

The final row is the remaining planning gate: its title coverage and dispositions must survive an
independent re-review before a future proposal is justified.
