# BUG-062: 短引用只覆盖 task-projection，一般 CLI stdout 仍甩完整 64 位哈希

> 严重级别: P3（UX/可读性）| 发现: 2026-08-06 | 状态: needs-info

## 症状

用户抱怨「生产出来的名字像 UUID 一样巨长」：`style-master`、`image2`、`ppt_flow`
一般命令的 stdout/回执里，`plan_sha256`、`batch_sha256`、`review_decision_sha256`
等全是完整 64 位 hex，Agent 原样转述给用户，不可读。

## 根因 / 现状

`6e8d0fb feat: add short page production references` 引入了短引用显示
（`p-<8hex>` / `b-<8hex>` / `e-<8hex>` / `r-<8hex>` / `m-<8hex>` / `d-<8hex>`，
见 `scripts/shared/workflow/page_production_task_projection.mjs` 的
`DISPLAY_REFERENCE_PREFIXES` 与 `displayReferenceKey`），但**只接入了
page-production 任务投影**，没有覆盖 style-master / image2 / 通用 CLI stdout。

目录名（`plans/<64hex>/`、`materializations/<64hex>/`、`batches/<64hex>/`）
保持完整是**协议要求**（内容寻址，record digest 与目录名必须一致，
见 `_lessons/hash-id-research.md`），不能重命名。问题只在**显示层**。

## 建议

1. 给通用 CLI stdout 提供「短引用」投影（或 Agent 约定）：对话里一律用
   `p-<8hex>` 短引用；完整哈希只用于 CLI 参数/协议传输。
2. 或提供 `--short-refs` 输出选项，让回执同时带完整哈希与短引用。
3. Agent 侧约定：转述给用户时用短引用，需要 CLI 传参时才用完整哈希。

## 关联

- `6e8d0fb`（短引用特性）；`_lessons/hash-id-research.md`（UX 契约）。
- BUG-061（工作区重复 bundle）同属可读性/卫生类。

## Triage Update — 2026-08-08

> *This was generated by AI during triage.*

**Category:** enhancement
**State:** needs-info
**Verdict:** 部分缓解，通用用户可见 CLI 输出的问题仍在；这不是内容寻址记录错误，而是尚未定义统一的
machine-output 与 human-display-output 边界。

### Verification

- Page Production Task Projection 已有 typed display-reference index：在该张 Markdown 卡内，plan、batch、
  evidence、review、manifest 和 delivery 的 64 位 digest 会显示为带类型前缀的短引用，并对碰撞加后缀。
  相应 focused tests 证明它只影响显示文本，不改变 owner payload。
- `ppt_flow` 中仍至少有六个直接 `JSON.stringify` stdout 路径，包括 status、slides JSON、current
  `image2`、Style Master 和 controller reports。它们直接透传含 `plan_hash`、`batch_hash` 等完整 digest 的
  owner result；没有通用 output projection 或 `--short-refs` 选项。
- 公开 current `image2` 命令的参数仍要求完整 `<sha256>`，这对于精确授权、记录查找和协议绑定是正确的，
  不应以截短值替换。

### What Changed

- 已有的短引用不是全局失效：它为当前任务卡和 Agent 转述提供了安全的显示模型。
- 缺口是该模型没有进入普通 CLI success JSON，也没有记录一条覆盖所有用户沟通的展示约定。因此用户仍会
  直接面对完整 hash，尤其在 Agent 原样转述 stdout 时。

## Triage Notes

**What we still need from the maintainer:**

- 选择目标表面：只要求 Agent/Controller 转述使用短引用，还是需要 CLI 本身提供人类可读 projection。
- 若改 CLI，选择 additive display fields、单独 `--short-refs` human view，或另一份稳定的 display report；
  默认 JSON 不能无意破坏现有 machine consumers。
- 确认短引用的解析/碰撞策略是否仅在单张 card 内有效，还是应成为跨命令的稳定交互标识。

在这些决定明确前，不应全局替换实际 hash、CLI 参数或不可变记录中的完整 digest。
