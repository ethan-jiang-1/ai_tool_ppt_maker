# BUG-056: Agent 让用户查看产物（中间图/PPT/评审图）时不提供完整路径

> 严重级别: P2 | 发现: 2026-08-05 | 状态: ready-for-agent

## 症状

在 deck_dark_factory 生产过程中，Agent 多次要求用户查看产物（Style Master 候选图、Pilot/Expansion raw 图、
最终 PPTX），但没有给出**完整文件路径**，用户不知道在哪个目录看，只能猜测或要求再确认。

例：
- 「3 张 Pilot raw 图已打开」—— 没说是哪 3 个文件、在哪个目录
- 「deck.pptx 已构建」—— 没给出 PPTX 的完整路径
- 用户反馈：『你总得给个全路径吧，用户哪知道在哪儿看，给相对路径好吧』

## 根因

Agent 的产物查看习惯只做「open 打开」动作 + 口语化描述，没有**显式附带产物文件的完整路径（或 repo 相对
路径）**。用户不在 `open` 弹出的 viewer 场景时（或想自己找文件时）无从定位。

## 修复方向

在**每次要求用户查看产物**时（中间生成图、评审图、contact sheet、manifest、PPTX、delivery receipt 等），
消息中必须显式给出：
1. 产物的**完整绝对路径**（或至少 repo 相对路径，如 `deck_dark_factory/.../final/deck.pptx`）
2. 简短说明这是什么、怎么看

这是 Agent 沟通契约的一部分，不是代码逻辑改动。可以沉淀为 AGENTS.md / deck-guide.md 的操作规则，或在
每次展示产物时强制执行。

## 关联

- 触发于 deck_dark_factory 生产全程（Style Master / Pilot / Expansion / final）。

## Triage Update — 2026-08-08

> *This was generated by AI during triage.*

**Category:** enhancement
**State:** ready-for-agent
**Verdict:** 部分缓解，尚未解决为统一的用户沟通契约。

### Verification

- 当前 Style Master review projection 已提供稳定 `candidate_id` 和 deck-relative
  `candidate_path`，候选查看不再必须靠 SHA-256 猜测路径。
- Page Image provider-input inspection 也提供 run-relative locator，且人类浏览用图片采用
  `NN_slideID.png` 命名。
- 但根 AGENTS 指引、生成的 deck guide 和公共 CLI/projection 契约都没有规定：当 Agent 要求人
  查看任一 artifact 时，回复必须逐项给出可直接定位的绝对或 repo-relative 路径。因此 Pilot、
  Complete Page Review、PPTX、notes receipt 和 delivery receipt 仍可再次出现“已打开但找不到”的问题。

## Agent Brief

**Category:** enhancement
**Summary:** 让所有要求人类查看产物的 Agent 回复携带可直接定位的 artifact locator。

**Current behavior:** 个别 owner projection 有相对路径，但没有跨 workflow 的交互规则；Agent 可以仅
描述“已打开”或“已生成”，而不列明实际文件。

**Desired behavior:** 每次要求人类检查一项或多项产物时，回复为每个 artifact 给出绝对路径或从 repo
根开始的路径，并说明 artifact 类型与检查目的。已有 owner-issued locator 应优先使用；不得从
`_generated/` 反推编辑权限，也不得为方便显示创造新的路径协议。

**Acceptance criteria:**
- [ ] 根级 Agent 指引和新 run bundle 的 operating guide 均定义同一条 artifact 展示规则。
- [ ] Style Master candidates、raw/review projections、final PNG/PPTX、notes receipt 和 delivery receipt
      的用户提示都有可复制定位符。
- [ ] 多 artifact 评审按稳定 slide ID 或 candidate ID 列出路径，不以 SHA-256 作为人工选择主键。
- [ ] 规则明确 locator 仅用于查看，不授予 `_generated/` 手工编辑权限。

**Out of scope:** 改变 artifact 所有权、将内容寻址目录改为短名，或让 CLI 把所有内部绝对路径当作
machine schema 的必填字段。
