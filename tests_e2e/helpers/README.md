# E2E helpers

Journey input builders and fake external adapters only; no production business rules. Local Canvas/PPTX fixtures belong under `tests/helpers/` because they are render-tier integration evidence, not mocked E2E.
