# Amber Agent Image2 Reference

`model-sheet.png` is the verified multi-pose visual doctrine. It is never a provider reference.

`guide.png` and `collaborating.png` are the two reviewed, label-free, single-subject role derivatives
that may be selected through `image2-reference-material.yaml`. They are intentionally separate from the
HTML `asset-manifest.yaml` catalog.
