# Page Image generation credentials. Copy this file to .env and fill in both
# values. The harness loads .env for supported production commands.

IMAGE2_API_KEY=
IMAGE2_BASE_URL=           # e.g. https://<relay>/v1

# Optional live diagnostics; disclose and obtain approval before either command.
# node ppt_maker_harness/scripts/ppt_flow.mjs doctor --probe-vendors
# node ppt_maker_harness/scripts/ppt_flow.mjs doctor --smoke
