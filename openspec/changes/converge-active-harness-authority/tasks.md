## 1. Plant Authority-Map Negative Controls

- [ ] 1.1 [harness-charter] Add focused pure-evaluator tests with one valid
  synthetic capability registry and safe planted missing, extra, duplicate,
  malformed, mismatched-spec, forbidden-path, and nonexistent-path variants;
  assert bounded root codes, no input/repository mutation, and a passing rerun
  after restoring the exact valid input.
- [ ] 1.2 [harness-charter] Add focused cases proving capability identity/shape
  failures short-circuit dependent owner-path symptoms, script `.mjs` owners
  must be registered interfaces/executables, and valid documentation/schema
  owners may be omitted or resolve only inside active Harness source roots.

## 2. Implement One Coherence Checkpoint

- [ ] 2.1 [harness-charter] Add a pure authority-map evaluator to
  `harness_coherence.mjs` with closed record shape, unique lower-kebab IDs,
  exact derived spec paths, exact set reconciliation, bounded diagnostics, and
  the single repair-and-rerun action; add no state, CLI, fallback, waiver,
  provider call, or production-data scan.
- [ ] 2.2 [harness-charter] Add the thin repository adapter that extracts exactly
  one marker-bounded YAML registry from `openspec/config.yaml`, parses it with
  the declared `yaml` dependency, discovers immediate main-spec directories,
  reads the existing source/test ownership manifest, and invokes the pure
  evaluator from `scanHarnessCoherence()`.
- [ ] 2.3 [harness-charter] Reuse the existing source/test ownership manifest for
  public script admission, register the already consumed
  `contracts/harness_coherence.mjs` interface if required by that manifest, and
  remove the unused `activeSurfaceFiles` collection without weakening the
  semantic/link/command checks the coherence checkpoint actually performs.

## 3. Converge Active Authority Projections

- [ ] 3.1 [harness-charter] Replace the free-form capability tables in
  `openspec/config.yaml` with one marker-bounded closed YAML registry that
  enumerates every immediate current main spec exactly once, binds its exact
  spec path, keeps scope summaries non-normative, and cites only necessary
  current public owner files.
- [ ] 3.2 [harness-charter] Remove numeric lifecycle routing, render-mode
  compatibility aliases, Chain aliases, the stale Stage 1-5 production story,
  and current-Phase entry language from config while retaining the current
  Page Image pipeline, `framed|pure`, method-module/Controller, gate, and
  OpenSpec maintenance distinctions through their existing owners.
- [ ] 3.3 [harness-charter] Delete the `Frozen Identifier` historical-reader
  category from `CONTEXT.md` without inventing a replacement compatibility
  noun; preserve exact current literals only through their existing schema and
  capability authorities.
- [ ] 3.4 [harness-charter] Fill the current
  `slide-identity-and-ordering` main-spec Purpose directly, as required for
  archive-generated placeholder repair, without changing its requirements.

## 4. Remove Targeted Ownership Residue

- [ ] 4.1 [harness-charter] Correct the stale `ppt_flow.mjs` state and CLI-error
  comment paths so they point to the current public owner modules, with no code
  or observable CLI behavior change.
- [ ] 4.2 [harness-charter] Replace maintenance-only "Phase" ownership labels in
  `ppt_maker_harness/scripts/README.md`, the foundation test/E2E README files,
  and matching architecture checker/test identifiers and diagnostics with
  accurate method-module/owner terminology; do not touch presentation-content
  examples where phase has ordinary domain meaning.

## 5. Focused And Baseline Verification

- [ ] 5.1 [harness-charter] Run the focused documentation-coherence and Harness
  architecture tests; prove the checked-in registry is bijective with current
  main specs, all literal owner paths exist and are admitted, every planted
  violation fails, and the exact restored input passes the same checkpoint.
- [ ] 5.2 [harness-charter] Run scoped active-surface residue searches for all
  paths and concepts targeted by this change, explicitly excluding
  `openspec/changes/archive/`, this change's historical rationale, and ordinary
  presentation-domain examples; inspect every remaining hit and resolve or
  document it against an owning current contract.
- [ ] 5.3 [harness-charter] Run `npm test`, `npm run test:sweep`,
  `openspec validate converge-active-harness-authority --strict
  --no-interactive`, `openspec validate --all --strict --no-interactive`, and
  `git diff --check`; confirm no real-provider command ran and no `deck_*`,
  `dpt_*`, or `_generated/` path changed.
- [ ] 5.4 [harness-charter] Review the final diff against the proposal, delta,
  design, and progress-plan Change 1 closure checklist; mark every completed
  task and record focused/baseline evidence before requesting OpenSpec archive.
