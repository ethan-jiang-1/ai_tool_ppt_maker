## 1. Authorize and inventory the retirement boundary

- [ ] 1.1 [project-versioning] Obtain the separately authorized H.10 release/version decision, record the v1 support deadline and whether an operator migration artifact is required; do not implement the breaking removal before this decision exists.
- [ ] 1.2 [framework-charter, framework-directory-layout, framework-script-layout] Build a checked-in absorb-or-delete matrix for every active v1/compatibility source, import, receipt, state field, controller node, doc link, test, and main-spec clause; assign each one to `v2 owner`, `shared invariant`, `one-off migration`, or `delete` with a direct proof owner.
- [ ] 1.3 [run-bundle-management, node-specification] Define the exact retired-input classification boundary from source/state direct facts, its protected identity/bytes/lineage invariants, and its single migration-preview or export next action; prove normal observation has no write authority.

## 2. Absorb retained behavior into the one v2/shared graph

- [ ] 2.1 [content-parsing, node-specification] Move every reusable v1 source/receipt/state/evidence invariant into the selected v2 or shared owner, remove per-slide authority/default parsing from current source construction, and ensure non-v2 input fails before receipt or provider work.
- [ ] 2.2 [image-generation, image-production, pipeline-orchestration] Preserve necessary raw-plan, evidence, final-manifest, and shared-delivery semantics through typed v2 workflow contracts; remove retired adapter/finalization/foreign-evidence fallback from current execution.
- [ ] 2.3 [slide-identity-and-ordering] Preserve stable-ID and exact structural-plan invariants solely through v2 workflow-bound preview/apply; remove v1 structural materialization and adoption-matrix execution from active structural paths.
- [ ] 2.4 [workflow-inspection, playbook-execution] Make marker-first observation and controller routing resolve only selected v2 workflow facts; a retired pair returns one non-mutating migration/export hard-stop, while a hybrid/corrupt pair remains its identity repair hard-stop.
- [ ] 2.5 [workflow-inspection, node-specification] Add focused negative coverage proving status, ordinary state, inspection, controller resume, and target execution cannot initialize a v1 receipt/state, dispatch a v1 writer, infer a workflow, or mutate a retired bundle; prove each repair reruns the same direct checkpoint.

## 3. Deliver and close any necessary migration boundary

- [ ] 3.1 [run-bundle-management, playbook-execution] If and only if the approved absorption matrix identifies an in-scope migratable input, implement an isolated operator migration preview that reads a named source, produces an exact v2 target plan/hash, makes zero provider calls, and is not discoverable through normal `ppt_flow` routing.
- [ ] 3.2 [run-bundle-management, node-specification] Require explicit human confirmation with a bounded reason before migration materialization; create a distinct v2 target, preserve original v1 source/state/bytes, and hard-stop ambiguous, corrupt, unauthorized, or non-migratable input without force/waiver.
- [ ] 3.3 [playbook-execution, workflow-inspection] Add focused and E2E coverage for preview, confirmed materialization, rejection, and export; verify the migration artifact has a recorded sunset owner and cannot become a controller, fallback, or provider path.

## 4. Delete retired active surfaces and Agent-visible noise

- [ ] 4.1 [framework-script-layout, image-production] Delete `PPTMAKER_FRAMEWORK/scripts/compatibility/` and every v1 writer, adapter, receipt initializer, state branch, process dispatch, and inventory entry after its matrix row is absorbed or migrated; do not leave redirect/re-export stubs.
- [ ] 4.2 [framework-directory-layout, framework-charter, commands-reference] Delete `PPTMAKER_FRAMEWORK/workflow/compatibility/`, v1 classifiers, compatibility links, and historical current-route prose; make BOOTSTRAP, Charter, COMMANDS, workflow roots, and playbooks expose only `03 XOR 04 -> 05 -> 06` plus a bounded retired-input stop.
- [ ] 4.3 [framework-directory-layout, framework-script-layout] Delete `tests/compatibility/` active proof and all v1/legacy adoption fixtures, manifests, ownership records, README placeholders, and E2E routes after converting retained behavior into v2/shared invariant or one-off migration proof.
- [ ] 4.4 [framework-directory-layout, framework-script-layout] Update architecture, directory, source-to-test, executable/import, governance, retirement, controller-manifest, and documentation audits to fail closed if active roots register a compatibility/v1 owner, import, writer, classifier, receipt, or route.

## 5. Synchronize the current specification surface

- [ ] 5.1 [commands-reference, framework-charter, framework-directory-layout, framework-script-layout] Apply and verify the delta removals/rewrites so human guidance, active ownership, and architecture specs contain no current compatibility model.
- [ ] 5.2 [content-parsing, image-generation, image-production, pipeline-orchestration] Apply and verify the delta removals/rewrites so parsing, raw evidence, finalization, and orchestration specs define only v2/shared current behavior.
- [ ] 5.3 [node-specification, playbook-execution, run-bundle-management, slide-identity-and-ordering, workflow-inspection] Apply and verify the delta removals/rewrites so state, controller, bundle, structural, and observation specs use one v2 graph and one retired-input hard-stop rather than an active legacy route.
- [ ] 5.4 [all affected capabilities] Add a main-spec and active-root audit that excludes OpenSpec archives but fails on remaining v1/compatibility current-contract language, paths, imports, or executable ownership; confirm historical evidence is retained only in archive or the time-bounded migration delivery.

## 6. Prove retirement and close out

- [ ] 6.1 Run focused parser/state/inspection/structural/raw/final/delivery tests plus architecture, directory, ownership, controller, documentation, and main-spec negative audits; record the absorb-or-delete matrix evidence and no-write snapshots.
- [ ] 6.2 Run process and selected E2E journeys for valid v2 Framed/Pure work, retired/hybrid hard-stops, and migration preview/materialization only if task 3 applies; confirm no production `deck_*`, `dpt_*`, or `_generated/` data was implicitly read or changed.
- [ ] 6.3 Run `npm test`, `git diff --check`, and `openspec validate retire-current-v1-compatibility --strict`; resolve failures without restoring a compatibility fallback or hand-editing generated artifacts.
- [ ] 6.4 After the user separately authorizes closeout, sync accepted delta specs, update LR.0–LR.9/LR.CP in the progressive plan with migration/deletion evidence, archive, commit, and record the removal/sunset result; do not make a new project-version decision beyond the already authorized H.10 result.
