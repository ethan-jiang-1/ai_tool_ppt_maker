## 1. Production-Mode Policy And State Authority

- [ ] 1.1 Add the pure shared `production_mode` policy module with the closed three-mode vocabulary, mode-to-pipeline/page-authority/refinement/style-master matrix, exact run-version inspection inputs, and transition classification; add focused unit coverage for every mapping and invalid value. (Capabilities: `node-specification`, `pipeline-orchestration`; done when no caller needs a private mode table.)
- [ ] 1.2 Upgrade the state schema and discoverability header to include canonical `production_mode.by_version["3_versions/vN"].mode`, preserving `state.pipeline` only as an actual-pipeline compatibility projection and rejecting noncanonical version keys/record shapes through bounded validation. (Capability: `node-specification`; done when round-trip and read-only validation cover valid, missing, extra, and malformed records.)
- [ ] 1.3 Implement idempotent pre-mode state migration for every visible version using only the canonical source marker (`html-first-v1 -> html-only`, markerless/`legacy-image2-first -> image2-only`), including mixed-version decks and non-writing historical markerless observation; prove refinement, metadata, history, and generated bytes cannot influence migration. (Capability: `node-specification`; done when pre-v4 fixtures migrate once and post-v4 record loss fails closed.)
- [ ] 1.4 Expose state-owned exact-run inspection that combines canonical state with the source marker, reports typed metadata-mirror drift and `transition_required|state_drift`, and short-circuits before generated/provider checks on missing, invalid, or inconsistent authority. (Capabilities: `node-specification`, `cli-surface`; done when focused negative tests show no fallback or mutation.)
- [ ] 1.5 Implement the expected-state/CAS transition and audit path for `html-only <-> html-then-image2`, revalidate retained refinement work when enabling it, preserve all plan/attempt/candidate/source/provenance/review bytes when disabling it, and reject every HTML/Image2 cross-pipeline in-place request without mutation. (Capability: `node-specification`; done when concurrent/stale writes fail and directory/state diffs prove no data loss.)
- [ ] 1.6 Extend the authoritative completion/status evaluator for all three modes, including required current refinement plus renewed final review for `html-then-image2`, no refinement debt for `html-only`, and no HTML/refinement debt for `image2-only`; keep guide/confirm/hard-stop and evidence-waiver semantics distinct. (Capabilities: `node-specification`, `visual-slot-refinement`; done when each missing prerequisite yields one nearest owner action.)
- [ ] 1.7 Add the state-owned metadata mirror writer/repair action for the last presented mode/version, use it after init/transition, and surface interrupted mirror publication as repairable drift without changing routing or introducing another journal/success store. (Capabilities: `node-specification`, `run-bundle-management`; done when state-first interruption tests preserve authority.)

## 2. Mode-Aware Run-Bundle Initialization

- [ ] 2.1 Refactor `bundle_layout.mjs` around one common scaffold plus explicit HTML and whole-page Image2 seed adapters, retaining thin compatibility helpers without implementing Image2 init as create-HTML-then-rewrite. (Capability: `run-bundle-management`; done when all deck types pass structure checks for all three modes.)
- [ ] 2.2 Make omitted mode default to `image2-only`; seed mnemonic-v1 source, matching pipeline marker, authoritative v1 mode, pending mode-owned gates, and non-authoritative metadata mirror for each explicit mode while creating no style master, generated output, refinement state, or provider attempt. (Capability: `run-bundle-management`; done when exact directory-diff tests cover default and three explicit modes.)
- [ ] 2.3 Add and return-audit `ppt_flow init --mode html-only|html-then-image2|image2-only`, validate the enum before filesystem creation, and report normalized mode, derived pipeline, and next action in help/human/JSON surfaces. (Capability: `cli-surface`; done when invalid mode is a one-envelope `USAGE` failure with zero writes.)
- [ ] 2.4 Preserve existing explicit legacy-to-HTML migration by assigning a successfully published HTML target `html-only` through the bounded new-version/state initialization path, without adding a general cross-pipeline switch or changing its preview/hash/no-replace contract. (Capabilities: `run-bundle-management`, `node-specification`; done when migration regression tests leave the source mode unchanged and target mode/source consistent.)

## 3. Public Routing And Existing Production Adapters

- [ ] 3.1 Introduce one root-CLI adapter router that inspects production mode before branch-specific flags/readiness and dispatches `validate`, `pilot`, approvals, `build`, `refresh`, `state`, and `status` to existing HTML or whole-page owners; update registered direct entries to share or enforce the same closed boundary. (Capabilities: `cli-surface`, `pipeline-orchestration`; done when wrong-adapter spies and directory diffs remain empty.)
- [ ] 3.2 Promote the current whole-page Stage 1-5 implementation to the normal `image2-only` pilot/build/refresh path while retaining provider authorization, generation manifests, reviewed-byte/header evidence, cache/provenance, progress, PPTX, and notes contracts. (Capabilities: `image-generation`, `pipeline-orchestration`; done when the public path no longer requires legacy-maintenance entry and no renderer code is duplicated.)
- [ ] 3.3 Keep both HTML modes on the local HTML adapter, reject whole-page/provider options before writes, and make `html-then-image2` report required refinement freshness after HTML build/refresh without invoking a provider automatically. (Capabilities: `pipeline-orchestration`, `visual-slot-refinement`; done when HTML provider-call spies remain zero.)
- [ ] 3.4 Make modern `ppt_flow image2 *` eligibility mode-aware: optional for `html-only`, required-by-completion for `html-then-image2`, and typed not-applicable guidance for `image2-only`; retain offline planning, exact authorization, unknown-submit, candidate review, promotion, and final-review invalidation contracts. (Capabilities: `visual-slot-refinement`, `cli-surface`; done when not-applicable creates no refinement/provider state and stale authorization still hard-stops.)
- [ ] 3.5 Route run-aware doctor from production policy while preserving direct env-check's explicit base/`--image2`/live modes: base only for `html-only`, base plus later Image2 guidance for `html-then-image2`, and offline Image2 presence for `image2-only`; never trigger a live probe implicitly. (Capabilities: `environment-check`, `cli-surface`; done when readiness scope and no-network tests cover all modes.)
- [ ] 3.6 Route `style-master` through production policy: reuse the current in-framework Image2 generator for `image2-only` and return zero-write typed `capability_not_available` guidance for the reserved HTML adapter seam, while leaving unknown identity and provider authorization as hard stops. (Capabilities: `style-master-generation`, `cli-surface`; done when HTML tests create no legacy artifact or provider request.)
- [ ] 3.7 Extend CLI diagnostic and success-result allowlists plus the executable return audit for mode/init/routing/mirror/transition outcomes, preserving one secret-safe final JSON envelope and producer/consumer ownership. (Capability: `cli-surface`; done when direct and delegated failure-path audits pass.)

## 4. Mode-Aware Controllers And Guidance

- [ ] 4.1 Update `create-deck.md` to declare both pipelines and an early canonical mode branch, with complete Image2-primary authoring/style-master/pilot/review/build/PPTX/notes/final-review nodes, retained `html-only` flow, and `html-then-image2` handoff/return through the existing refinement controller. (Capability: `playbook-execution`; done when no new deck is routed through compatibility-only maintenance.)
- [ ] 4.2 Extend node conditions/index/state branch handling so inapplicable create nodes are deterministically skipped with a mode-bound reason, same-pipeline mode changes reopen/skip only affected refinement completion nodes, reserved evidence remains outside the working set, and mode/source drift blocks controller entry. (Capabilities: `node-specification`, `playbook-execution`; done when graph simulation covers fresh, resume, round-trip, and drift cases.)
- [ ] 4.3 Update `image2-refine.md`, lifecycle metadata, resume cards, and completion handoff so refinement remains HTML-only in implementation, optional in `html-only`, required in `html-then-image2`, and never becomes the `image2-only` whole-page route. (Capabilities: `playbook-execution`, `visual-slot-refinement`; done when controller validation and both optional/required simulations pass.)
- [ ] 4.4 Update `COMMANDS.md` and `change-classifier.md` to classify by authoritative mode then verified pipeline, document the default/explicit creation paths and mode-owned iteration/refresh behavior, disclose provider cost/authorization at the actual boundary, and keep Structural Versioning Path outside the three refresh paths. (Capability: `commands-reference`; done when docs consistency tests contain no claim that all new decks default to HTML or all whole-page work is legacy-only.)
- [ ] 4.5 Update `BOOTSTRAP.md` environment remediation and first-time credential guidance for the selected/default mode, keeping exact base/Image2 check names, copy-pasteable repairs, offline/live/production authorization separation, and `html-only` as a no-credential route. (Capability: `bootstrap-env-guidance`; done when check-name/gate-scope consistency tests cover all three modes.)
- [ ] 4.6 Update `charter/NODE-SPEC.md` and affected workflow/reference material with the version-scoped SSOT, migration/transition ownership, mode-aware completion, first-class Image2 terminology, HTML style-master reserved seam, and explicit Change-2 cross-pipeline boundary without duplicating CLI schemas. (Capabilities: `node-specification`, `playbook-execution`; done when repository docs and controller validation agree with delta specs.)

## 5. Focused And End-To-End Verification

- [ ] 5.1 Add/extend secret-free unit fixtures for mode policy, schema migration, exact-version lookup, invalid/corrupt state, metadata drift/repair, CAS races, transition work preservation, completion projection, init seeds, readiness, and style-master eligibility. (Capabilities: all changed JS owners; done when targeted suites pass without reading production decks.)
- [ ] 5.2 Add CLI/integration coverage for every routed command across all modes, including prerequisite-first diagnostics, no wrong-owner writes, no implicit live/provider calls, repair then rerun of the same checkpoint, and successful human/JSON return shapes. (Capabilities: `cli-surface`, `pipeline-orchestration`; done when registered executable/return audits pass.)
- [ ] 5.3 Add playbook simulation coverage for default Image2-primary creation, explicit `html-only`, required `html-then-image2` handoff/completion, same-pipeline round-trip, historical resume/migration, and cross-pipeline refusal with the original execution/state intact. (Capability: `playbook-execution`; done when the normative manifest/index has no impossible gates or ownership conflicts.)
- [ ] 5.4 Add mocked-provider E2E coverage from default init through whole-page authoring, style master, pilot/content-visual-header review, build, PPTX, notes, and final review; assert no HTML production or modern-refinement artifact becomes authority. (Capabilities: `run-bundle-management`, `image-generation`, `playbook-execution`; done when the new release path completes with no live network.)
- [ ] 5.5 Add E2E coverage proving `html-only` completes provider-free and `html-then-image2` remains incomplete until exact authorized refinement and a renewed final review, including preservation of accepted/partial work after a mode round-trip. (Capabilities: `playbook-execution`, `visual-slot-refinement`; done when status and actual artifacts agree at each boundary.)

## 6. Final Validation

- [ ] 6.1 Run the focused state/init/CLI/pipeline/playbook/refinement suites, `npm test`, and `npm run test:e2e`; resolve regressions without weakening mode identity, provider authorization, provenance, CAS, or recovery invariants.
- [ ] 6.2 Run CLI return audits, docs/controller consistency checks, `git diff --check`, and `openspec validate add-production-mode-and-image2-primary --strict`; confirm no framework production dependency, generated artifact edit, or Change-2 cross-pipeline implementation entered the change.
