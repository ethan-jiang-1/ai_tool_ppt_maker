## 1. Production-Mode Policy And State Authority

- [ ] 1.1 Add the pure shared `production_mode` policy module with the closed three-mode vocabulary, exact mappings (`html-only` refinement disabled, `html-then-image2` required, `image2-only` not applicable), markerless whole-page pipeline normalization, exact run-version inspection inputs, and transition classification; add focused unit coverage for every mapping and invalid value. (Capabilities: `node-specification`, `pipeline-orchestration`; done when no caller needs a private mode table or treats `legacy-image2-first` as source frontmatter.)
- [ ] 1.2 Upgrade state schema v3 to v4 and the discoverability header to include canonical `production_mode.by_version["3_versions/vN"].mode`, preserving `state.pipeline` only as an actual-pipeline compatibility projection plus every existing reserved-record/journal/reset/recovery invariant, and reject noncanonical version keys/record shapes through bounded validation. (Capability: `node-specification`; done when round-trip/read-only validation covers valid, missing, extra, malformed, and gate-journal-fenced mode writes without weakening the existing recovery matrix.)
- [ ] 1.3 Implement idempotent pre-mode state migration for every visible version using only the canonical marker probe (`html-first-v1 -> html-only`, markerless `legacy` branch -> `image2-only`) without adding a whole-page frontmatter marker, including mixed-version decks and non-writing historical markerless observation; prove refinement, metadata, history, and generated bytes cannot influence migration. (Capability: `node-specification`; done when pre-v4 fixtures migrate once and post-v4 record loss fails closed.)
- [ ] 1.4 Expose state-owned exact-run inspection that combines canonical state with the source marker, reports typed metadata-mirror drift and `transition_required|state_drift`, and short-circuits before generated/provider checks on missing, invalid, or inconsistent authority. (Capabilities: `node-specification`, `cli-surface`; done when focused negative tests show no fallback or mutation.)
- [ ] 1.5 Implement the expected-state/CAS transition and audit path for `html-only <-> html-then-image2`, revalidate retained refinement work when enabling it, preserve all plan/attempt/candidate/source/provenance/review bytes when disabling it, and reject every HTML/Image2 cross-pipeline in-place request without mutation. (Capability: `node-specification`; done when concurrent/stale writes fail and directory/state diffs prove no data loss.)
- [ ] 1.6 Extend the authoritative completion/status evaluator and shared state/status resume-card projection for all three modes: required current refinement plus renewed final review for `html-then-image2`; no debt from absent/retained historical refinement under `html-only` while accepted promotions or unresolved chargeable work remain visible; evidence-bound whole-page final review with no HTML/refinement debt for first-class `image2-only`; and null-mode non-writing historical compatibility. (Capabilities: `node-specification`, `cli-surface`, `visual-slot-refinement`; done when human/JSON output exposes exact mode/pipeline consistency and each missing prerequisite yields one nearest owner action without losing existing HTML review/journal enums.)
- [ ] 1.7 Add the state-owned metadata mirror writer/repair action for the last presented mode/version, use it after init/transition, and surface interrupted mirror publication as repairable drift without changing routing or introducing another journal/success store. (Capabilities: `node-specification`, `run-bundle-management`; done when state-first interruption tests preserve authority.)
- [ ] 1.8 Extend controller/node frontmatter and the canonical index with validated `supported_production_modes`/`production_modes`, compute mode-filtered active node IDs, retain inactive records without marking them skipped or deleting them, and move an inapplicable current pointer only through a declared CAS-bound handoff. (Capabilities: `node-specification`, `playbook-execution`; done when human `skipped` semantics remain unchanged and round-trip mode tests reactivate retained work.)
- [ ] 1.9 Add state-owned first-class Image2 provider-authorization and final-review node evidence: authorization binds current operation/run/IDs-or-role/profile/count/execution, and `--record-image2-delivery-review` derives current header/contact-sheet/PPTX/notes lineage with no force path. (Capabilities: `node-specification`, `playbook-execution`; done when stale scope/evidence fails before submit/completion and historical maintenance semantics remain compatible.)

## 2. Mode-Aware Run-Bundle Initialization

- [ ] 2.1 Refactor `bundle_layout.mjs` around one common scaffold plus explicit HTML and whole-page Image2 seed adapters, retaining the common empty asset catalog but making its README ownership mode-honest, and retaining thin compatibility helpers without implementing Image2 init as create-HTML-then-rewrite. (Capability: `run-bundle-management`; done when root/version/deck-guide seeds and all deck types pass structure/first-look checks for the default plus all three explicit modes.)
- [ ] 2.2 Make omitted mode default to `image2-only`; seed mnemonic-v1 source, explicit HTML marker or canonical markerless whole-page source as appropriate, authoritative v1 mode, pending mode-owned gates, and non-authoritative metadata mirror while creating no style master, generated output, refinement state, provider attempt, or `production.pipeline: legacy-image2-first`. (Capability: `run-bundle-management`; done when exact directory-diff tests cover default and three explicit modes.)
- [ ] 2.3 Add and return-audit `ppt_flow init --mode html-only|html-then-image2|image2-only`, validate the enum before filesystem creation, and report normalized mode, derived pipeline, and next action in help/human/JSON surfaces. (Capability: `cli-surface`; done when invalid mode is a one-envelope `USAGE` failure with zero writes.)
- [ ] 2.4 Preserve existing explicit legacy-to-HTML migration by assigning a successfully published HTML target `html-only` through the bounded new-version/state initialization path, without adding a general cross-pipeline switch or changing its preview/hash/no-replace contract. (Capabilities: `run-bundle-management`, `node-specification`; done when migration regression tests leave the source mode unchanged and target mode/source consistent.)
- [ ] 2.5 Make every same-pipeline `createVersion`/Structural Versioning publication call idempotent state-owned target mode registration after visibility; on interruption preserve the target and expose `mode_registration_required`, and verify source/target/mode/pipeline/CAS before repair. (Capabilities: `run-bundle-management`, `node-specification`; done when success, already-current, crash window, conflict, and wrong-deck/cross-pipeline tests pass without copying approvals or generated work.)

## 3. Public Routing And Existing Production Adapters

- [ ] 3.1 Implement and return-audit the closed CLI grammar: `init --mode`, `doctor --mode|--run-dir`, `state --set-production-mode`, mirror repair, same-pipeline version registration, and `--record-image2-delivery-review`, including all mutual exclusions before reads/writes; introduce one run-scoped adapter router for existing commands and direct-entry enforcement. (Capabilities: `cli-surface`, `pipeline-orchestration`; done when wrong-adapter spies, directory diffs, and one-envelope usage failures pass.)
- [ ] 3.2 Promote the current whole-page Stage 1-5 implementation to normal `image2-only` style-master/pilot/content-visual-header approval/build/title/visual refresh while retaining contact-sheet placement, generation manifests, reviewed-byte/header evidence, reuse/cache/provenance, progress, PPTX, and notes contracts; before an actual submit, rederive and require the current scoped Controller authorization, but bypass that gate for proven zero-submit reuse/local work. (Capabilities: `cli-surface`, `image-generation`, `pipeline-orchestration`; done when every whole-page public command works through first-class ownership without legacy-maintenance entry or duplicated renderer/provider protocol.)
- [ ] 3.3 Keep both HTML modes on the local HTML adapter, reject whole-page/provider options before writes, and make `html-then-image2` report required refinement freshness after HTML build/refresh without invoking a provider automatically. (Capabilities: `pipeline-orchestration`, `visual-slot-refinement`; done when HTML provider-call spies remain zero.)
- [ ] 3.4 Make modern `ppt_flow image2 *` eligibility mode-aware: typed disabled guidance plus no writes for `html-only`, required-by-completion for `html-then-image2`, and typed not-applicable guidance for `image2-only`; retain prior work, one-slide eligibility, offline planning, exact authorization, unknown-submit, candidate review, promotion, and final-review invalidation. (Capabilities: `visual-slot-refinement`, `cli-surface`; done when disabled/not-applicable create no state/provider attempt and stale authorization still hard-stops.)
- [ ] 3.5 Refactor env-check into common, HTML, and Image2 groups; retain no-selector common+HTML behavior, make `image2-only`/compatibility `--image2` common+Image2 with no HTML loads, make `html-then-image2` Image2 failures deferred until its explicit boundary, and ensure `--smoke|--probe-vendors` remain explicit live selections. (Capabilities: `environment-check`, `cli-surface`; done when profile JSON/text/exit semantics and no-network/no-HTML-load tests cover every mode.)
- [ ] 3.6 Route `style-master` through production policy: reuse the current authorized in-framework Image2 generator for `image2-only` and return successful zero-write typed `available: false` guidance for the reserved HTML adapter seam, while leaving unknown identity and required provider authorization as hard stops. (Capabilities: `style-master-generation`, `cli-surface`; done when HTML tests create no legacy artifact/provider request and exit zero only for the safe guide.)
- [ ] 3.7 Extend CLI diagnostic and success-result allowlists plus the executable return audit for mode/init/routing/mirror/transition outcomes, preserving one secret-safe final JSON envelope and producer/consumer ownership. (Capability: `cli-surface`; done when direct and delegated failure-path audits pass.)

## 4. Mode-Aware Controllers And Guidance

- [ ] 4.1 Update `create-deck.md` to declare both pipelines and mode-filtered nodes, with complete Image2-primary authoring/scoped provider gates/style-master/pilot/review/build/PPTX/notes/evidence-bound final review, retained `html-only` flow, and `html-then-image2` handoff/return through the existing refinement controller. (Capability: `playbook-execution`; done when no new deck is routed through compatibility-only maintenance and zero-submit work creates no provider gate.)
- [ ] 4.2 Wire mode-filtered index/state behavior into controller entry, successors, pending/completion, existing-deck resume, gates, and Structural Versioning handoff; preserve inactive records, keep reserved evidence outside the working set, retain explicit-human-only `skipped`, and block mode/source drift or missing target registration before controller/materializer work. (Capabilities: `node-specification`, `playbook-execution`; done when graph simulation covers fresh, historical and first-class resume, round-trip, inactive-current handoff, vNext registration, and drift cases.)
- [ ] 4.3 Update `image2-refine.md`, lifecycle metadata, resume cards, and completion handoff so refinement remains disabled with switch guidance in `html-only`, required in `html-then-image2`, supports a one-slide deck, retains old work across switching, and never becomes the `image2-only` whole-page route. (Capabilities: `playbook-execution`, `visual-slot-refinement`; done when controller validation and disabled/required/round-trip simulations pass.)
- [ ] 4.4 Update `COMMANDS.md` and `change-classifier.md` to classify by authoritative mode then verified pipeline, document the default/explicit creation paths and mode-owned iteration/refresh behavior, disclose provider cost/authorization at the actual boundary, and keep Structural Versioning Path outside the three refresh paths. (Capability: `commands-reference`; done when docs consistency tests contain no claim that all new decks default to HTML or all whole-page work is legacy-only.)
- [ ] 4.5 Update `BOOTSTRAP.md` remediation and first-time credential guidance for common/HTML/Image2 profiles and `doctor --mode`, keeping exact check names, copy-pasteable repairs, deferred Image2 guidance for `html-then-image2`, offline/live/production authorization separation, and `html-only` as a no-credential route. (Capability: `bootstrap-env-guidance`; done when check-name/profile/gate-scope consistency tests cover all three modes.)
- [ ] 4.6 Update the framework charter, `charter/NODE-SPEC.md`, and affected workflow/reference material with the version-scoped SSOT, `image2-only` release default, migration/transition ownership, mode-aware completion, first-class Image2 terminology, HTML style-master reserved seam, and explicit Change-2 cross-pipeline boundary without duplicating CLI schemas. (Capabilities: `framework-charter`, `node-specification`, `playbook-execution`; done when no active constitutional guidance still claims every new deck defaults to HTML and repository docs/controller validation agree with delta specs.)

## 5. Focused And End-To-End Verification

- [ ] 5.1 Add/extend secret-free unit fixtures for mode policy, schema migration, exact-version lookup, invalid/corrupt state, metadata drift/repair, CAS races, version registration/recovery, mode-filtered working sets, transition work preservation, provider/final-review evidence, completion projection, init seeds, readiness profiles, and style-master eligibility. (Capabilities: all changed JS owners; done when targeted suites pass without reading production decks.)
- [ ] 5.2 Add CLI/integration coverage for every routed command across all modes, including prerequisite-first diagnostics, no wrong-owner writes, no implicit live/provider calls, repair then rerun of the same checkpoint, and successful human/JSON return shapes. (Capabilities: `cli-surface`, `pipeline-orchestration`; done when registered executable/return audits pass.)
- [ ] 5.3 Add playbook simulation coverage for default Image2-primary creation, explicit `html-only`, required `html-then-image2` handoff/completion, same-pipeline round-trip, historical resume/migration, and cross-pipeline refusal with the original execution/state intact. (Capability: `playbook-execution`; done when the normative manifest/index has no impossible gates or ownership conflicts.)
- [ ] 5.4 Add mocked-provider E2E coverage from default init through whole-page authoring, scoped authorizations, style master, pilot/content-visual-header review, build, PPTX, notes, and evidence-bound final review; assert no HTML production/modern-refinement artifact becomes authority and Image2 readiness ignores broken HTML-only runtime fixtures. (Capabilities: `run-bundle-management`, `image-generation`, `playbook-execution`; done when the new release path completes with no live network.)
- [ ] 5.5 Add E2E coverage proving `html-only` completes provider-free and `html-then-image2` remains incomplete until exact authorized refinement and a renewed final review, including preservation of accepted/partial work after a mode round-trip. (Capabilities: `playbook-execution`, `visual-slot-refinement`; done when status and actual artifacts agree at each boundary.)

## 6. Final Validation

- [ ] 6.1 Run the focused state/init/CLI/pipeline/playbook/refinement suites, `npm test`, and `npm run test:e2e`; resolve regressions without weakening mode identity, provider authorization, provenance, CAS, or recovery invariants.
- [ ] 6.2 Run CLI return audits, docs/controller consistency checks, `git diff --check`, and `openspec validate add-production-mode-and-image2-primary --strict`; confirm no framework production dependency, generated artifact edit, or Change-2 cross-pipeline implementation entered the change.
