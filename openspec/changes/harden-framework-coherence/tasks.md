## 1. Capture Every Known Failure Before Changing Behavior

- [ ] 1.1 Add playbook fixtures proving embedded nodes currently vacuously pass, `requires` is not enforced, duplicate IDs are ambiguous, `restructure-slides` lacks terminal verification, legacy `phase` is semantically ambiguous, decision values are undeclared, step syntax disagrees with NODE-SPEC, and self-completed entry gates are invalid. Encode the normative forty-node design manifest as an expected-contract fixture before editing controllers. [pipeline-script; `node-specification`, `playbook-execution`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_state_yaml.mjs`, new `tests/test_playbook_validation.mjs`]
- [ ] 1.2 Add state fixtures for invalid node/gate enums, stale `completed` timestamps after restart, missing schema/execution/time fields, repeated-playbook leakage, typed decision migration, duplicate-node alias collision, nested shared-node overwrite, five-field stack snapshot restoration, ambiguous legacy-stack safe fallback, stable workflow `started_at`, and idempotent second reads. [pipeline-script; `node-specification`, `playbook-execution`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_state_yaml.mjs`]
- [ ] 1.3 Add regressions proving `speaker_notes_injected` is circular today and that stale/missing/path-escaping receipts, empty note blocks, multiple target PPTX files, and failed reruns must block completion. [pipeline-script; `notes-injection`, `node-specification`; `--stage 5`; no `--force-images`, `--only`, `--new-version`, or style-master regeneration; tests: `tests/test_stage5_inject_notes.mjs`, `tests/test_state_yaml.mjs`]
- [ ] 1.4 Add direct and delegated subprocess fixtures for prose-only standalone failures, double envelopes, fragmented/no-newline child envelopes, spawn errors, and exit-zero children that emit `ok:false`. [pipeline-script; `cli-surface`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_cli_error.mjs`, `tests/test_ppt_flow.mjs`]
- [ ] 1.5 Add documentation fixtures for broken relative links, obsolete production paths, invalid documented CLI flags, render-unaware title chains, version-copy drift, hierarchy ambiguity, and stale `openspec/config.yaml` facts. [content-constraint; `framework-charter`, `framework-directory-layout`, `commands-reference`, `cli-surface`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_docs_consistency.mjs`]

## 2. Build the Canonical Playbook Model

- [ ] 2.1 Implement `scripts/lib/playbook_model.mjs` for controller frontmatter, ordered fenced YAML nodes, shared-node frontmatter, exact source lines, includes, requires, optional decision enums, lifecycle/module metadata, and canonical step blocks. [pipeline-script; `node-specification`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_playbook_validation.mjs`]
- [ ] 2.2 Implement validation for global node uniqueness, reserved system IDs (`header-review`), include/require resolution, dependency cycles/order, lifecycle/module enums, deterministic-entry syntax, typed evidence/decision placement, canonical classification scope, declared branch values, GATE/decision agreement, step numbering/types, impossible self-entry, and absent node bodies. [pipeline-script; `node-specification`, `playbook-execution`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_playbook_validation.mjs`]
- [ ] 2.3 Make `checkEntry` evaluate declared `requires` as `node_done:<id>` before explicit entry conditions and make `checkExit` use the same indexed declaration; absent, duplicate, invalid, or unknown nodes must fail closed. [pipeline-script; `node-specification`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_state_yaml.mjs`, `tests/test_playbook_validation.mjs`]
- [ ] 2.4 Add typed evidence and decision records/helpers plus `evidence:`, `user_evidence:`, `decision_recorded`, `user_decision_recorded`, `node_evidence:`, and `node_decision:` evaluation; make decision writes validate against the canonical node enum; heal booleans/scalars conservatively to agent provenance; and require cross-node sources to be declared, completed, and in the current execution. [pipeline-script; `node-specification`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_state_yaml.mjs`]
- [ ] 2.5 Make completed/pending/playbook-complete queries accept the active playbook node set and derive unique or branching eligible-next results from the same index. [pipeline-script; `node-specification`, `playbook-execution`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_state_yaml.mjs`, `tests/test_ppt_flow.mjs`, `tests/test_playbook_validation.mjs`]

## 3. Version and Harden State Semantics

- [ ] 3.1 Add `STATE_SCHEMA_VERSION = 2`, active `execution_id`/`execution_started_at`, stable workflow `started_at`, active controller-working-set invariants, ordered idempotent v1→v2 migrations, and v2 serialization while excluding reserved `header-review` evidence from controller execution tagging/clearing. [pipeline-script; `node-specification`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_state_yaml.mjs`, `tests/test_header_review.mjs`]
- [ ] 3.2 Enforce node and gate enums in writers/validators; heal invalid persisted values to blocking `pending` with diagnostics and clean incompatible timestamps/failure fields during transitions. [pipeline-script; `node-specification`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_state_yaml.mjs`]
- [ ] 3.3 Add playbook-scoped migration aliases for known renamed nodes; when old/new records collide, preserve the canonical record and merge only missing legacy fields. [pipeline-script; `playbook-execution`, `node-specification`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_state_yaml.mjs`]
- [ ] 3.4 Make `startPlaybook` create a clean controller working set and new execution ID/start time only when the stack is empty and incomplete replacement is explicit; require `switchPlaybook` for nested work; deep-snapshot controller nodes in a five-field stack entry; make `resumePlaybook` restore the parent snapshot while preserving current reserved records; tag all node/evidence/decision writes; preserve workflow `started_at`; and fail closed on mismatched records. [pipeline-script; `node-specification`, `playbook-execution`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_state_yaml.mjs`]
- [ ] 3.5 Move state temporary writes beside `_state/state.yaml`, atomically rename on the same filesystem, ignore/clean stale temp siblings, and add an EXDEV regression fixture. [pipeline-script; `node-specification`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_state_yaml.mjs`]

## 4. Normalize Every Registered Controller

- [ ] 4.1 Apply the normative forty-node manifest: replace legacy `phase` with `lifecycle_phase`/`method_module`, rename both duplicate `verify-output` IDs, add `verify-restructure-output`, declare routing decision enums, reject reserved collisions, and normalize every body to canonical MD/CLI/GATE steps. [content-constraint; `node-specification`, `playbook-execution`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_playbook_validation.mjs`, `tests/test_docs_consistency.mjs`]
- [ ] 4.2 Remove duplicated predecessor tokens from entry lists, rely on `requires`, persist canonical classification scope, convert completion claims to typed evidence/decisions, use exact `node_decision` values for mutually exclusive branches, reject skipped predecessors as evidence sources, and map re-checkable tokens to shared deterministic checks (`slide_specs_valid`, scoped `header_review_current`, receipt-backed `speaker_notes_injected`). [content-constraint; `node-specification`, `playbook-execution`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_playbook_validation.mjs`, `tests/test_state_yaml.mjs`, `tests/test_header_review.mjs`]
- [ ] 4.3 Rewrite the text-edit controller around `ppt_flow refresh --kind title`: body-lock completes Chain A (`1,3,4,5`), while full-page follows `TITLE_REVIEW_REQUIRED` → selected 2K pilot with `--only <id> --force-images` → `approve header` → reviewed-image reuse. [pipeline-operation; `playbook-execution`, `commands-reference`; Chain A/B; full-page uses `--only`/`--force-images`; no `--new-version` or style-master regeneration; tests: `tests/test_ppt_flow.mjs`, `tests/test_playbook_validation.mjs`]
- [ ] 4.4 Run the validator over all nine controllers plus the shared node and make exact forty-node manifest agreement plus zero parse, duplicate, dependency, decision, condition, metadata, and step errors a required test gate. [pipeline-operation; `playbook-execution`, `node-specification`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_playbook_validation.mjs`]
- [ ] 4.5 Wire `ppt_flow state` human/JSON output to the active controller index so Pending includes unwritten nodes, a unique eligible node is suggested, branches list candidates, and `waiting_for` retains priority. [pipeline-script; `playbook-execution`, `node-specification`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_ppt_flow.mjs`, `tests/test_state_yaml.mjs`]

## 5. Make Stage-5 Completion Deterministic

- [ ] 5.1 Declare `jszip` as a direct runtime dependency and update the lockfile, preserving Node 18+ compatibility. [pipeline-script; `notes-injection`; `--stage 5`; no image/version flags; tests: `tests/test_stage5_inject_notes.mjs`, dependency install/import check]
- [ ] 5.2 Add dependency-free `scripts/lib/notes_receipt.mjs` helpers that validate normalized relative paths plus lexical/realpath containment and atomically write/read receipt schema v1 with input/PPTX hashes, counts, and timestamp under `_generated/qa/notes_injection.json`. [pipeline-script; `notes-injection`; `--stage 5`; no image/version flags; tests: `tests/test_stage5_inject_notes.mjs`]
- [ ] 5.3 Make the run-dir Stage-5 path invalidate the prior receipt before attempting work, reject zero/multiple target PPTX files and empty note blocks, write the PPTX through a same-directory temporary file/atomic rename, and publish the receipt only after final hashes/counts succeed. [pipeline-script; `notes-injection`; `--stage 5`; no image/version flags; tests: `tests/test_stage5_inject_notes.mjs`, `tests/test_unified_pipeline.mjs`]
- [ ] 5.4 Replace the circular `speaker_notes_injected` condition with receipt validation and actionable stale/missing repair reasons. [pipeline-script; `notes-injection`, `node-specification`; `--stage 5`; no image/version flags; tests: `tests/test_state_yaml.mjs`, `tests/test_stage5_inject_notes.mjs`]

## 6. Enforce One CLI Failure Receipt per Invocation

- [ ] 6.1 Extend `cli_error.mjs` with a reusable standalone-entry wrapper for usage, known failure, and uncaught paths while keeping module imports side-effect free. [pipeline-script; `cli-surface`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_cli_error.mjs`]
- [ ] 6.2 Migrate the explicit eleven-entry executable registry to the shared wrapper with stable codes, exactly one final direct-invocation envelope, and side-effect-free `--help`; remove the misleading shebang from library-only `image_api_client.mjs`. [pipeline-script; `cli-surface`; Chain/`--stage`: N/A; no image/version flags; tests: corresponding `tests/test_<script>.mjs`, `tests/test_cli_error.mjs`, `tests/test_runtime_constitution.mjs`]
- [ ] 6.3 Change `ppt_flow` delegation to stream child stdout, frame stderr across chunk/newline boundaries while retaining at most one 64-KiB candidate final line, consume only a schema-valid final child envelope, relay/synthesize bounded fallback diagnostics otherwise, preserve valid child exit status, and emit one parent envelope. [pipeline-script; `cli-surface`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_ppt_flow.mjs`, `tests/test_cli_error.mjs`]
- [ ] 6.4 Add an executable-inventory assertion so a new direct CLI cannot be added without deterministic direct and, where applicable, delegated failure probes. [pipeline-script; `cli-surface`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_runtime_constitution.mjs`, `tests/test_cli_error.mjs`]

## 7. Make Documentation Coherence Executable

- [ ] 7.1 Add reusable library `scripts/lib/framework_coherence.mjs` for relative Markdown links, file-specific exclusions, stale paths/skills, canonical render terms, version semantics, and hierarchy vocabulary with source-line diagnostics. [pipeline-script/test infrastructure; `framework-charter`, `framework-directory-layout`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_docs_consistency.mjs`]
- [ ] 7.2 Add fenced/inline command-example extraction for the specified shell fence languages and first-token grammar; join backslash continuations, ignore prompt/comment/output lines, resolve direct scripts or `ppt_flow` subcommands, compare long flags with real `--help`, and enforce the adjacent single-example `<!-- coherence:pseudocode reason="..." -->` marker. [pipeline-script/test infrastructure; `cli-surface`, `framework-directory-layout`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_docs_consistency.mjs`]
- [ ] 7.3 Correct active Stage 2/style-master/contact-sheet paths and replace invalid Stage 3/4/5 direct invocation examples with current `ppt_flow`/`unified_pipeline` commands or real low-level flags. [content-constraint; `framework-charter`, `framework-directory-layout`, `cli-surface`; Chain/`--stage`: documentation only; no runtime image/version flags executed; tests: `tests/test_docs_consistency.mjs`]
- [ ] 7.4 Correct every active edit-chain summary, anti-pattern, playbook, template, glossary entry, and example so title edits branch by resolved mode and point to `ppt_flow refresh --kind title`. [content-constraint; `framework-charter`, `commands-reference`, `playbook-execution`; Chain A/B documentation only; no runtime image/version flags executed; tests: `tests/test_docs_consistency.mjs`]
- [ ] 7.5 Repair all active relative Markdown links and register any intentionally unresolved template link through an exact source-file/target/reason exception; do not reuse the command pseudocode marker as a link exemption. [content-constraint; `framework-directory-layout`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_docs_consistency.mjs`]
- [ ] 7.6 Normalize init/manual-copy guidance, deck-guide behavior, source-delta version wording, and Lifecycle Phase/Method Module/Pipeline Stage/Playbook Node terminology including Phase 2.7. [content-constraint; `framework-charter`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_docs_consistency.mjs`]
- [ ] 7.7 Update `charter/NODE-SPEC.md`, controller guidance, and `openspec/config.yaml` to use 11 iron laws, current paths, default `full-page`, resolved-mode refresh routing, state schema/execution/evidence rules, source-delta versioning, and the canonical hierarchy. [content-constraint; `framework-charter`, `node-specification`, `commands-reference`; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_docs_consistency.mjs`, `tests/test_playbook_validation.mjs`]

## 8. Prove the Whole Change Is Coherent

- [ ] 8.1 Run strict OpenSpec validation and verify proposal capability names exactly match the seven delta-spec directories. [pipeline-operation; all modified capabilities; Chain/`--stage`: N/A; no image/version flags]
- [ ] 8.2 Run `bundle_layout.mjs --self-check`, the complete Vitest suite, direct/delegated CLI failure inventory, full documentation/link/command scans, and dependency import checks. [pipeline-operation; all modified capabilities; Chain/`--stage`: N/A; no image/version flags]
- [ ] 8.3 Audit a legacy-state fixture and `deck_ai_sdlc_keynote` resume card through default heal, confirming only `_state` schema normalization is permitted and deck sources plus `_generated/` remain unchanged. [pipeline-operation; `node-specification`, `playbook-execution`; Chain/`--stage`: N/A; no `--force-images`, `--only`, `--new-version`, or style-master regeneration]
- [ ] 8.4 Re-run targeted contradiction searches for external skills, render-unaware title chains, unsupported standalone flags, “complete-copy” version language, ambiguous phase labels, duplicate node IDs, unknown conditions, and prose-only failures; all active matches must be zero or explicitly historical. [pipeline-operation; all modified capabilities; Chain/`--stage`: N/A; no image/version flags]
