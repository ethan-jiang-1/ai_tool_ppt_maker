## 1. Lock the Current Coherence Failures into Tests

- [ ] 1.1 Add failing playbook fixtures that prove fenced node declarations are currently skipped, duplicate IDs are ambiguous, impossible self-entry gates are accepted, and unknown conditions are not cataloged (`node-specification`, `playbook-execution`; pipeline-script change; Chain/`--stage`: N/A; no `--force-images`, `--only`, `--new-version`, or style-master regeneration; tests: `tests/test_state_yaml.mjs` and new `tests/test_playbook_validation.mjs`).
- [ ] 1.2 Add deterministic subprocess tests showing prose-only failures from each documented standalone CLI (`cli-surface`; pipeline-script change; Chain/`--stage`: N/A; no image/version flags; tests: extend `tests/test_cli_error.mjs` and the corresponding `tests/test_<script>.mjs` files).
- [ ] 1.3 Add whole-framework documentation fixtures for broken relative links, active `image2-ppt`/removed-path references, render-unaware title-chain statements, and inconsistent version/phase terminology (`framework-charter`, `framework-directory-layout`, `commands-reference`; content-constraint change; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_docs_consistency.mjs`).

## 2. Implement Canonical Playbook Parsing and Validation

- [ ] 2.1 Replace substring/frontmatter-only node discovery in `scripts/lib/state.mjs` with a canonical index that parses ordered fenced YAML nodes plus standalone shared-node frontmatter and records exact source locations (`node-specification`; pipeline-script change; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_state_yaml.mjs`, `tests/test_playbook_validation.mjs`).
- [ ] 2.2 Implement validator checks for globally unique node IDs, valid includes/requires references, canonical condition syntax, impossible self-entry gates, and structurally invalid declarations (`node-specification`, `playbook-execution`; pipeline-script change; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_playbook_validation.mjs`).
- [ ] 2.3 Update `checkEntry`, `checkExit`, and `getMissingConditions` to consume the same index, fail closed for absent/duplicate/unknown nodes, and evaluate the declaration humans actually see (`node-specification`; pipeline-script change; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_state_yaml.mjs`, `tests/test_playbook_validation.mjs`).
- [ ] 2.4 Add `manual:<evidence-key>` condition support and state helpers for persisted node evidence, including tolerant read/heal of missing evidence mappings (`node-specification`; pipeline-script change; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_state_yaml.mjs`).

## 3. Normalize the Registered Playbooks

- [ ] 3.1 Rename duplicate generic node IDs to intent-specific IDs, correct `instantiation`'s impossible entry condition, and update all includes/requires/current-node references (`playbook-execution`; content-constraint + pipeline-operation change; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_playbook_validation.mjs`).
- [ ] 3.2 Replace free-form entry/exit tokens across all registered playbooks with catalog conditions or explicit `manual:<evidence-key>` conditions and update `charter/NODE-SPEC.md` to match (`node-specification`, `playbook-execution`; content-constraint change; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_playbook_validation.mjs`, `tests/test_docs_consistency.mjs`).
- [ ] 3.3 Add read/heal aliases for known renamed in-progress nodes scoped by active playbook, preserving status extras, waiting fields, history, and playbook stack (`playbook-execution`, `node-specification`; pipeline-script change; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_state_yaml.mjs`).
- [ ] 3.4 Rewrite `edit-text`/`edit-visual` routing so Stage 1 resolved mode selects Chain A (`--stage 1,3,4,5`) for `body+header-lock` or Chain B (`--stage 1,2,3,4,5 --only <id> --force-images`) plus header review for `full-page` (`playbook-execution`, `commands-reference`; pipeline-operation change; Chain A/B; `--only` and `--force-images` required only for full-page; no `--new-version` or style-master regeneration; tests: `tests/test_ppt_flow.mjs`, `tests/test_playbook_validation.mjs`).

## 4. Enforce the CLI Failure Constitution Everywhere

- [ ] 4.1 Extend `scripts/lib/cli_error.mjs` with a reusable standalone-entry wrapper that emits one final JSON envelope while leaving imported module behavior unchanged (`cli-surface`; pipeline-script change; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_cli_error.mjs`).
- [ ] 4.2 Migrate `bundle_layout.mjs`, `env-check.mjs`, `unified_pipeline.mjs`, style-master/contact-sheet scripts, and Stage 1–5 standalone entry paths to the shared failure wrapper with stable `USAGE`/`FAILED`/`UNCAUGHT` mapping (`cli-surface`; pipeline-script change; Chain/`--stage`: N/A; no image/version flags; tests: each corresponding `tests/test_<script>.mjs` plus `tests/test_cli_error.mjs`).
- [ ] 4.3 Add an executable inventory assertion so every documented standalone `.mjs` has a deterministic failure-envelope probe and future scripts cannot bypass coverage (`cli-surface`; pipeline-script change; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_runtime_constitution.mjs`, `tests/test_cli_error.mjs`).

## 5. Converge Active Documentation and OpenSpec Context

- [ ] 5.1 Update all active edit-chain tables, examples, playbooks, templates, anti-patterns, glossary entries, and script guidance to branch title edits by resolved render mode (`framework-charter`, `commands-reference`, `playbook-execution`; content-constraint change; Chain A/B documentation; no runtime flags executed and no image/version regeneration; tests: `tests/test_docs_consistency.mjs`).
- [ ] 5.2 Remove active external `image2-ppt`/skill production instructions and replace them with the in-framework Node Stage 2, style-master, and contact-sheet paths while preserving narrowly marked historical records (`framework-charter`, `framework-directory-layout`; content-constraint change; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_docs_consistency.mjs`, `tests/test_runtime_constitution.mjs`).
- [ ] 5.3 Correct all broken local Markdown links across the 93 framework documents and add source-line-aware link validation with explicit file-specific exceptions only (`framework-directory-layout`; content-constraint change; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_docs_consistency.mjs`).
- [ ] 5.4 Normalize active version semantics, init/manual-copy guidance, deck-guide behavior, and the hierarchy vocabulary Lifecycle Phase / Method Module / Pipeline Stage / Playbook Node (`framework-charter`; content-constraint change; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_docs_consistency.mjs`).
- [ ] 5.5 Update `openspec/config.yaml` context and rules so future changes use 11 iron laws, current paths, policy-default `full-page`, resolved-mode edit routing, source-delta versioning, and the canonical hierarchy terms (`framework-charter`, `framework-directory-layout`, `commands-reference`; content-constraint change; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_docs_consistency.mjs`).

## 6. End-to-End Verification

- [ ] 6.1 Run playbook validation against the full registered controller set and verify zero duplicate IDs, unresolved references, unknown conditions, or vacuous entry/exit passes (`node-specification`, `playbook-execution`; pipeline-operation verification; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_playbook_validation.mjs`).
- [ ] 6.2 Run representative direct CLI failure probes and confirm every process exits non-zero with exactly one final parseable envelope (`cli-surface`; pipeline-operation verification; Chain/`--stage`: N/A; no image/version flags; tests: `tests/test_cli_error.mjs` and script-specific tests).
- [ ] 6.3 Run `bundle_layout.mjs --self-check`, `ppt_flow.mjs test`, full Markdown link/semantic scans, and a resume-card audit against `deck_ai_sdlc_keynote` without mutating its sources or `_generated/` artifacts (all modified capabilities; pipeline-operation verification; Chain/`--stage`: N/A; no `--force-images`, `--only`, `--new-version`, or style-master regeneration).
