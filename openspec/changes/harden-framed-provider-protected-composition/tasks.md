## 1. Current Contract Cutover

- [ ] 1.1 Update the current `page-source-receipt`, `page-layout`, and `image2-request` schema/stage declarations and serialization inventory for the closed source restriction, profile-derived normalized protected composition, body-safe provenance, and the removal of provider-visible exact header-literal context; retain no versioned or legacy reader path.
- [ ] 1.2 Make the current Framed header contract cut over atomically across the parser/receipt, Page Image Core, Framed overlay and raw-contract validators/compiler, architecture prompt-assembly inventory, shared fixtures, and `BOOTSTRAP.md`: retain only local header literals, remove every `context_not_to_render` reader/writer, converter, and fallback, and keep C5 serialization as an opaque adapter-request publisher.
- [ ] 1.3 Preserve the parsed closed `subject_restrictions` fact as a Core semantic binding; project it into only the Framed raw/request lineage while retaining Pure's existing receipt and Visual Config identity-resolution semantics without a C6 Framed request field.
- [ ] 1.4 Extend the opt-in production-schema conformance evaluator and synthetic tests for the all-workflow receipt restriction, Framed composition provenance and request header-literal absence, and Pure's absence only of Framed C6 bindings; keep the evaluator provider-free and non-runtime.
- [ ] 1.5 Add focused source/Core clean-cutover tests for the closed source-restriction grammar, local-only Framed headers, no legacy context reader, restriction retention, malformed/missing direct facts, and unchanged Pure header plus identity-resolution semantics.

## 2. Profile-Derived Composition

- [ ] 2.1 Extend the selected Framed profile resolver and configuration validation to derive one normalized reserved-header/body-safe composition from canonical local geometry, with selected-profile and inherited-value provenance.
- [ ] 2.2 Reject out-of-canvas, overlapping, malformed, stale, or cross-workflow composition facts through the existing source/configuration repair path without a default, generated-file read, or prior-plan fallback.
- [ ] 2.3 Add resolver/configuration tests for valid selected profiles, invalid safe geometry, unselected sibling isolation, and Pure's absence of Framed composition facts.

## 3. Framed Raw And Request Binding

- [ ] 3.1 Carry the selected composition and Core-bound source restrictions into only the immutable Framed raw contract, raw-plan bindings, and C5 inspection data; validate their exact digest/lineage before plan publication without removing Pure's existing parser/Visual Config restriction use.
- [ ] 3.2 Compile the exact Framed provider request from non-text composition guidance and source restrictions while retaining the full continuous provider canvas; omit `context_not_to_render`, every exact local header literal, and any native region/mask transport field.
- [ ] 3.3 Preserve shared transport opacity and Pure's C6 Framed-field isolation; add focused Framed/Core/adapter tests for exact bytes, header-literal absence, restriction propagation, malformed binding short-circuits, no partial C5 publication, and no removal of Pure's pre-existing restriction semantics.

## 4. Existing Lifecycle And Review Path

- [ ] 4.1 Extend the existing invalidation evaluator so composition or source-restriction drift requires raw rebuild and a new Complete Page Review, never a local-header refresh or raw-evidence reuse.
- [ ] 4.2 Preserve one Complete Page Review decision and add negative coverage proving composition checks or any optional observation cannot create approval, waiver, retry, state, or a second acceptance controller.
- [ ] 4.3 Verify diagnostics use the existing earliest source/configuration repair action, protect canonical raw lineage as a hard-stop invariant, and return the same planning checkpoint after repair.

## 5. Integration And Regression Coverage

- [ ] 5.1 Add temporary-bundle integration coverage from selected Framed profile through resolver, Core, raw contract, C5 publication, and mock transport; verify Pure remains free of Framed facts.
- [ ] 5.2 Extend the mock-provider public CLI journey only as needed to prove the existing authorization/review handoff retains the new exact Framed request binding without calling a real provider.
- [ ] 5.3 Run focused source/Core/Framed/review/schema suites, `npm test`, strict OpenSpec validation, the Run Bundle layout self-check, and `git diff --check`; record any non-C6 baseline failure separately.

## 6. Scope Closure

- [ ] 6.1 Before marking C6 complete, record implementation evidence that it made no provider-native region/mask claim or transport change, paid or real-provider probe, v3 repair, production-bundle read/migration, or manual `_generated/` edit; preserve the separate explicit human Work Request and follow-up-change boundary for any future probe or native primitive.
