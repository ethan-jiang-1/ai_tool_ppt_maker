## 1. Stable Identity And Slide Document Core

- [ ] 1.1 Extend the `slide-identity-and-ordering` library surface with 5–8-letter two-block BlockCase syntax, `spoken_key` normalization, reserved-word and near-confusion checks, deck-history reservation, legacy-ID classification, and the shared selector precedence; keep semantic `SUBJECT + MOVE` quality Agent-owned and prove `UX gap` variants, longer clear IDs, ambiguity, legacy fallback, and deleted-ID reservation in `tests/test_slide_ids.mjs`.
- [ ] 1.2 Add the shared `slide_document` ESM module that byte-ranges frontmatter, preamble, valid slide blocks, and epilogue, and serializes unchanged regions byte-for-byte; add golden tests for CRLF/whitespace preservation, complete-block movement, a post-slide `## Change Log` boundary, and blocking malformed slide-like headings.
- [ ] 1.3 Implement document validation for non-empty/current-unique IDs, spoken-key uniqueness, canonical continuous heading projections, `identity.scheme: mnemonic-v1`, strict new-ID syntax, markerless legacy compatibility, and per-file `01..N` numbering for standalone multi-input Stage 1; return structured issues with source locations and a normalize repair hint.
- [ ] 1.4 Implement per-token snapshot selector binding with original token, formal ID, position, and `matched_by`; keep duplicates for callers, then plan normalize/move/delete/insert/multi-operation transactions with caller-owned conflict rules, `base_spec_sha256`, anticipated publication target, recursively canonical mutation-only `plan_sha256`, before/after order, `render.header-lock` updates, and deterministic natural-language page-reference warnings.
- [ ] 1.5 Implement in-memory transaction application and edit-receipt generation so apply requires the confirmed canonical plan hash plus current base hash, never replans a bare `--apply`, preserves block bodies, and never creates a second persistent order source; cover multi-delete shift, duplicate bindings, move/insert conflicts, plan/base hash mismatch, structured references, and no-op cases.

## 2. Stage 1 Identity Projection And Source Validation

- [ ] 2.1 Refactor `stage1_build_inputs.mjs` to consume the shared slide-document parser, preserve/project the supported identity marker, emit formal `slide_id` plus derived 1-based `position` in plan and prompt records, retain array order as assembly order, and preserve standalone multi-input policy scoping.
- [ ] 2.2 Make duplicate/empty ID, spoken-key collision, mnemonic-native syntax violation, malformed slide-like heading, and canonical heading mismatch blocking Stage 1 errors with source-aware `cli_error.mjs` diagnostics; verify Stage 1 never silently normalizes source and directs heading-only drift to `ppt_flow slides normalize`.
- [ ] 2.3 Change new prompt records to use an ID-stable logical raw-image output while keeping any position-bearing prompt twin explicitly cheap/derived; remove position, heading number, order, and prompt-twin filename from semantic generation inputs and fingerprints.
- [ ] 2.4 Expand `tests/test_stage1_build_inputs.mjs` with stable-ID reorder invariance, explicit positions, canonical numbering failures, duplicate-ID failure, legacy `s07_problem`, and two-input local-numbering/global-position regression fixtures.

## 3. ID-Addressed Render Artifacts

- [ ] 3.1 Add a shared render-artifact resolver interface for `(slide_id, render_engine, artifact_kind, fingerprint?)` that reads Stage-owned manifests, verifies declared byte hashes, returns fingerprint/profile lineage and explicit `verified|legacy-located|missing|ambiguous` status, supports multiple kinds/engines, and contains legacy position-prefixed adaptation without glob-based identity guessing.
- [ ] 3.2 Update Stage 2 and `image_provenance.mjs` so new `raw-render` images and manifest entries are keyed/named by formal ID, generation fingerprints exclude position, and legacy `NN_<id>.png` records become reusable only with verified kind/engine/fingerprint/profile/bytes; otherwise expose them only as `legacy-located`.
- [ ] 3.3 Add Stage 2 helpers for cross-version raw-render evidence validation and target-owned manifest publication, requiring stable ID, engine, `raw-render` kind, generation fingerprint/profile, and verified source-byte SHA; failed entries return `needs_render` and never invoke a remote renderer.
- [ ] 3.4 Update Stage 3 to require verified raw input by ID/engine/kind, write ID-stable final paths, and atomically publish `header_locked/_manifest.json` entries keyed as `final-slide` for overlay and full-page passthrough with raw/final hashes, resolved mode, header/config fingerprint, and timestamp.
- [ ] 3.5 Keep Stage 3 target-local: after raw materialization, recompute cheap final/header-lock outputs and target-owned manifests rather than copying prior-version final bytes; block on `legacy-located` or missing raw prerequisites without invoking a remote renderer.
- [ ] 3.6 Extend image-generation, header-lock, provenance, and resolver tests for reorder raw-cache hits, semantic-change misses, `verified` versus `legacy-located`, raw/final kind isolation, two engine variants for one ID, full-page entries, selected-run preservation, and tampered bytes.

## 4. ID-Proven PPTX Assembly And Notes

- [ ] 4.1 Refactor Stage 4 to iterate current `slide_plan.json`, resolve exactly one verified selected-engine `final-slide` image per formal ID through the Stage 3 manifest, reject missing/`legacy-located`/ambiguous mappings, and stop using directory or filename order as identity.
- [ ] 4.2 Atomically publish `_generated/qa/pptx_assembly.json` only after successful PPTX publication, binding schema version, plan hash, ordered IDs, per-ID final-image paths/hashes, PPTX path/hash, and timestamp; invalidate or omit the receipt on failure.
- [ ] 4.3 Refactor Stage 5 note extraction through `slide_document` to produce `{slide_id, note}`, require an exact ID-set match with the current plan, order notes by plan ID, and reject equal-count-but-different-ID inputs.
- [ ] 4.4 Upgrade `notes_injection.json` to schema v2 with immutable root assembly lineage and predecessor PPTX/receipt linkage; implement separate strict completion and notes-only rerun-input-lineage validators so edited notes source invalidates completion without deleting valid pre-replacement ancestry, and test the post-PPTX/pre-receipt failure recovery boundary.
- [ ] 4.5 Update the `speaker_notes_injected` gate to use only the schema-v2 completion validator with contained paths, current source/plan/final-PPTX hashes, ordered IDs, counts, and assembly lineage; never use the rerun-lineage validator or node completion as a completion proxy.
- [ ] 4.6 Expand Stage 4/5 and gate tests for reordered assembly, missing/`legacy-located`/ambiguous artifacts, tampered image/PPTX bytes, deleted-middle-page note alignment, equal-count ID mismatch, schema-v2 completion versus rerun lineage, schema-v1 compatibility limits, and failed rerun receipt invalidation.

## 5. Preview-First Structural CLI And Version Commit

- [ ] 5.1 Deepen run-bundle management with an ownership-token reservation per confirmed vNext, a separate unique hidden staging sibling under `3_versions/`, staging-aware structure/source validation, and a no-clobber same-parent final rename; preserve every visible/concurrent version and scope cleanup to that attempt's reservation/staging only.
- [ ] 5.2 Register the 13th top-level `ppt_flow slides` command and implement read-only `list` and `resolve` output as `position + formal slide_id + title` in stable human and JSON forms using the shared resolver.
- [ ] 5.3 Implement `slides normalize` preview plus its atomic current-version exception, requiring the confirmed plan hash and proving it changes only heading projections, never block order, IDs, body bytes, deck version, state, or generated artifacts.
- [ ] 5.4 Implement `slides move`, `delete`, and `insert` as adapters to the shared transaction planner: default to no-write preview, require an Agent-supplied valid/historically available insert ID, and accept apply only with both explicit `--apply` and matching `--plan-sha256`.
- [ ] 5.5 Implement `slides apply-plan` for schema-valid, self-hash-valid, base-hash-bound plans under the current version's `_scratch/`, returning the same preview/edit-receipt contract and refusing stale source, changed plan hash, paths outside the allowed boundary, or plans that would become a second order SSOT.
- [ ] 5.6 Route every structural failure through the existing `cli_error.mjs` envelope/diagnostic transaction with bounded selector candidates, source/operation lineage, argument-safe recovery, and `requires_human` for genuine ambiguity; keep JSON success output free of human progress text.
- [ ] 5.7 Update the fixed command inventory, framework coherence checks, help/docs flag audit, and return registry from 12 to 13 commands, including per-`slides`-subcommand success/usage/source-validation/conflict/stale-base/commit coverage.
- [ ] 5.8 Add CLI integration tests proving preview writes nothing, bindings preserve token order/`matched_by`, multi-position selectors use one snapshot, bare or mismatched-hash apply fails, confirmed apply equals preview, move/delete/insert publish vNext only after hidden staging validation, normalize stays in place, source races fail closed, and all failures emit one secret-safe final envelope.

## 6. Structural Impact And Verified Cross-Version Reuse

- [ ] 6.1 Add a stable-ID source/target diff that classifies retained, inserted, deleted, reordered, and semantic/profile-changed slides and reports current position, ID, title, artifact proof status, required local stages, `needs_render`, and review warnings.
- [ ] 6.2 Integrate verified raw-render-only materialization into `unified_pipeline.mjs`: run target Stage 1 first, copy only owner-validated raw bytes, atomically publish target-owned raw manifests with source-version lineage, rebuild Stage 3 and later cheap outputs locally, and never use the source version as an implicit runtime fallback.
- [ ] 6.3 Re-establish only verified per-slide header-review approvals when stable ID, generation profile, and reviewed raw-image SHA match; publish target-version-scoped evidence with source lineage, never borrow source records, and never carry waivers or `legacy-located` evidence.
- [ ] 6.4 Make structural apply/impact/materialization renderer-free. For reorder/delete-only with verified retained raw renders, rebuild Stage 3, contact sheet/QA, PPTX, and notes with zero remote calls; for missing/stale IDs, return `needs_render` and require a separately invoked Generated Image Rebuild scoped to explicit authorization.
- [ ] 6.5 Update pipeline status, pilot/contact-sheet labels, selector diagnostics, and impact receipts to show current `position + formal slide_id + title` while keeping the manifest association and generation fingerprint ID-based.
- [ ] 6.6 Add orchestration integration tests with fake renderer counters proving zero calls during every structural/materialization path, zero-call complete reorder/delete, single-insert `needs_render` without a call, one call only in the later explicit refresh, isolated stale retained ID, target-owned raw/final manifests, approval carry-forward but waiver rejection, and unchanged per-ID raw fingerprints after reorder.

## 7. MD Controller, Templates, And Framework Guidance

- [ ] 7.1 Rewrite `playbook/restructure-slides.md` so Agent-owned intent/ID naming leads to preview, user confirmation, exact `plan_sha256` apply, renderer-free publication, receipt-driven local rebuild/explicit `needs_render` authorization, semantic page-reference review, escape-ladder routing, and identity-aware final verification.
- [ ] 7.2 Update `charter/NODE-SPEC.md` and runtime Agent consumer guidance to display position/ID/title, retain plan hashes without exposing them as user chores, consume previews/receipts by reference, stop on ambiguity or `requires_human`, refresh rather than rebase stale plans, separate structure from render authorization, and avoid copying producer wire schemas.
- [ ] 7.3 Update `COMMANDS.md` and `scripts/change-classifier.md` with Chinese position/spoken-mnemonic examples, snapshot and plan-hash semantics, insert-ID authorship, renderer-free structural apply, explicit `needs_render` refresh costs, and the vNext/new-deck escape ladder.
- [ ] 7.4 Update `AGENT_CONTRACT.md`, `charter/WORKFLOW.md`, workflow/reference docs, scripts README, glossary, and active constitutional mirrors so stable identity, derived position, kind-aware render identity, raw-only verified materialization, local cheap rebuild, legacy status, and escape-ladder rules agree without weakening clean-vNext or no-hand-edit `_generated/` rules.
- [ ] 7.5 Update slide-specification templates, deck initialization fixtures, and authoring prompts to declare `identity.scheme: mnemonic-v1`, request a durable `SUBJECT + MOVE` BlockCase ID, prefer 5–6 ASCII letters, allow 7–8 only when clearer, and reject one-word categories, numbered/random suffixes, or forced compression as Agent authoring choices.
- [ ] 7.6 Extend MD-controller, initialization, documentation consistency, framework coherence, and template tests to enforce hash-bound preview/confirmation, position versus identity vocabulary, mnemonic marker/examples, 13-command guidance, renderer authorization boundaries, legacy wording, escape-ladder behavior, and absence of a second dual-render identity/order model.

## 8. End-To-End Compatibility And Validation

- [ ] 8.1 Add a `tests_e2e` temporary run-bundle scenario for reorder and multi-delete that confirms a plan hash, publishes vNext from hidden staging, proves the source version is unchanged, makes zero fake remote calls, locally rebuilds cheap outputs, assembles exact ordered IDs, and injects each retained ID's note into its new position.
- [ ] 8.2 Add a temporary run-bundle insertion scenario that requires an Agent-authored mnemonic, publishes structure with zero remote calls and exactly one `needs_render`, then explicitly authorizes/renders only that ID, materializes verified retained raw renders/approvals, rebuilds Stage 3 locally, and publishes matching plan, assembly, notes-v2, and edit receipts.
- [ ] 8.3 Add legacy regression coverage using repository test fixtures, not production `deck_*`: accept markerless `s07_problem`, distinguish proven manifest entries from merely located `NN_<id>.png`, reorder without implicit ID migration, and rebuild PPTX/notes correctly after current provenance is established.
- [ ] 8.4 Run targeted identity/document, Stage 1-5, CLI, pipeline, playbook, docs, and E2E suites; then run `npm test`, `node PPTMAKER_FRAMEWORK/scripts/ppt_flow.mjs doctor`, and relevant temporary-bundle layout checks with no production deck mutation.
- [ ] 8.5 Run `openspec validate add-stable-slide-identity-and-order-editing --strict`, `git diff --check`, direct-entry/return/coherence audits, and confirm no framework production dependency, generated artifact, or unrequested `deck_*`/`dpt_*` fixture was introduced.
