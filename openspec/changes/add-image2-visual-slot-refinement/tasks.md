## 1. Phase-4 Boundary and Contracts

- [x] 1.1 Create the `04-image2-refinement/` public `index.mjs`, private implementation tree, mirrored unit/E2E owners, and architecture inventory changes; route its operations only through `ppt_flow image2` and prove no Phase-3/shared/legacy-private crossover.
- [x] 1.2 Define the versioned pure plan, attempt, candidate, review, provenance, and journal contracts with canonical hashing; add deterministic fixtures and schema validation without provider dependencies.
- [x] 1.3 Add the injectable private modern transport adapter and fake test adapter; preserve secret-safe receipts and ensure no ordinary HTML/legacy import initializes it.
- [x] 1.4 Extend the source-to-test ownership manifest, architecture/load-closure checks, executable inventory, CLI return audit, and recursive discovery for Phase 4.

## 2. Source, Assets, and Lazy Bundle Partitions

- [x] 2.1 Extend the v2 catalog's controlled roots for exact Phase-4 refined asset paths; add bounded public Phase-2 asset-registration and Phase-3 selection-binding source transactions; reuse the existing single `primary_visual.selection` and current/stale/broken resolver unchanged, while defining adjacent Phase-4 provenance without extending the closed v2 asset-manifest schema.
- [x] 2.2 Add canonical lazy refinement path constants and bundle validation for accepted assets, `overrides/visual-style/image2-refinement.yaml`, derived evidence, and scratch journals; retain HTML-only absence as conformant and reject alternate provenance paths.
- [ ] 2.3 Implement canonical version-source provenance and atomic style-reference/accepted-candidate promotion journals binding old/new source/state SHAs; honor gate/reset fences and expected-state CAS, then add idempotent exact-transaction recovery and vNext source-only copy/re-evaluation semantics.
  - In progress: visual-slot and style-reference promotions now have prepared source/asset/provenance/state SHA bindings, fence checks, and expected-state CAS. A complete visual-slot temporary-run transaction verifies vNext source-only copy/re-evaluation and non-inheritance of generated/scratch/state. Recovery still needs intermediate-commit completion semantics.
- [x] 2.4 Implement explicit hash-bound cleanup with derived-only retention of at most one recent rejected candidate per slide and fail-closed ambiguous review ordering.
- [ ] 2.5 Add unit/integration coverage for existing selection/asset-resolver compatibility, refinement provenance, bundle partitions, promotion/recovery, cleanup, and vNext non-inheritance using temporary fixtures only.

## 3. Cost-Safe Refinement Lifecycle

- [ ] 3.1 Implement recommendation and deterministic exact plan construction only from current `html-delivery-review: proceed`, bounded 2-4 slide scope, one slot/candidate per slide, separately counted setup attempts, and canonical plan hashes.
- [ ] 3.2 Implement single-use authorization persistence that allocates random authorization/attempt IDs outside the deterministic plan hash, plus the attempt state machine (`planned` through `unknown-submit`); reject stale plans, scope expansion, retries, and duplicate execution before any submit.
- [ ] 3.3 Implement provider submission/reconciliation with persisted attempt IDs, partial-failure receipts, and human-only `unknown-submit` handling; never blindly resubmit.
- [ ] 3.4 Add a bounded Phase-3 public review-only candidate-composition operation accepting verified in-memory candidate values (never Phase-4 paths); publish candidate SHA/provenance and actual resolved-slot comparisons under the refinement derived root without changing current HTML delivery.
- [ ] 3.5 Implement per-page accept/use-html review decisions, source-promotion transaction, local recomposition, and the required renewed HTML final-review evidence; ensure normal build remains provider-free.
- [ ] 3.6 Add focused lifecycle tests covering declined refinement, stale authorization, setup dependency failure, crash/recovery before and after submit/promotion, partial failure, accept/use-html, and zero-provider local recomposition.

## 4. State, CLI, and Controller Integration

- [ ] 4.1 Extend the exact reserved-ID registry with version-scoped `image2-refinement` state and consumer projections for authorization, attempts, reviews, and safe human-action diagnostics without changing HTML delivery/legacy gate semantics.
- [ ] 4.2 Add the closed `ppt_flow image2` command group and Phase-4 adapters with canonical identifiers, validation, help, return audits, and envelope-safe diagnostics; reject markerless decks before transport loading.
- [ ] 4.3 Add the optional `image2-refine` playbook controller, normative node metadata, resume/completion behavior, and MD-to-JS protocol; declining or completing it must leave no Phase-4 debt.
- [ ] 4.4 Update COMMANDS, classifier, BOOTSTRAP, charter, workflow, scripts README, and active examples to explain optional authorized visual-slot refinement and legacy isolation without duplicating schema.
- [ ] 4.5 Add integration tests for state/CLI/controller routing, current-HTML eligibility, human decision gates, and bidirectional modern/legacy rejection.

## 5. End-to-End Verification and Cutover

- [ ] 5.1 Add Phase-4 E2E journeys with fake provider coverage for plan/authorize/generate/review/promote/keep-html/cleanup and interrupted recovery; verify provider call counts and source/derived ownership.
- [ ] 5.2 Add regressions proving fresh HTML-only delivery, ordinary local edits, structural vNext, and markerless maintenance neither create refinement state nor load/call modern transport.
- [ ] 5.3 Run focused Phase-2/3/4/5 interfaces, architecture/load-closure, CLI envelope/return audit, bundle self-check, documentation coherence, source-to-test ownership, and relevant benchmark checks; resolve every regression.
- [ ] 5.4 Run complete `npm test`, relevant `tests_e2e`, `git diff --check`, and strict change/all OpenSpec validation; verify normal HTML paths remain zero-provider and accepted assets rebuild after derived-artifact deletion.
