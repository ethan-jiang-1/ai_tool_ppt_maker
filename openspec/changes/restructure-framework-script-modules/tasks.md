## 1. Baseline And Architecture Contracts

- [x] 1.1 Record the current registered executable inventory, direct-entry indicators, import graph, active documentation paths, recursive test baseline, and Change-3 regression commands without reading `deck_*` or `dpt_*` data.
- [ ] 1.2 Create the exact `scripts/00-setup` through `scripts/05-iteration`, categorized `shared`, `contracts`, `fonts`, and `fixtures` skeleton, with Phase-4 README-only/unavailable content and no executable placeholder.
- [ ] 1.3 Create the mirrored `tests/` and `tests_e2e/` ownership directories, `tests/helpers`, `tests_e2e/helpers`, and the checked-in source-to-test ownership manifest location under `tests/contracts/`; add short owner READMEs wherever no current suite exists so Git preserves the exact tree.
- [ ] 1.4 Create `scripts/contracts/executable_inventory.mjs` as the fourteen-entry path-qualified pure-data registry and make `shared/cli/cli_error.mjs` consume it without changing the producer envelope contract; prove the registry plus shared CLI helpers use Node built-ins only and remain safe in the pre-install env-check closure.
- [ ] 1.5 Implement `scripts/contracts/framework_architecture.mjs` against synthetic target-tree fixtures for root whitelist, Phase interface inventory, Phase-4 absence, exact acyclic Phase adjacency, old-path rejection, `scripts/lib/` rejection, forbidden generic dumping grounds, literal-dynamic repo-local import edges, external-leaf handling, and canonical path-token ownership; do not add a transition bypass.
- [ ] 1.6 Extend the architecture checker with legacy/modern Image2 isolation, direct-entry/return inventory comparison using only the exact shared CLI helper exceptions, recursive test-tree checks, and source-to-test manifest validation; add focused network-free static tests plus subprocess load-closure probes for base doctor, HTML-local, and markerless-provider selection under `tests/contracts/`.

## 2. Shared Modules And Phase Interfaces

- [ ] 2.1 Move and regroup CLI bootstrap, error, progress, and delegated-process helpers under `scripts/shared/cli/` while preserving the existing producer contract and direct-entry behavior.
- [ ] 2.2 Move bundle layout/coherence/marker helpers and lessons ownership under `scripts/shared/run-bundle/`, preserving `bundle_layout.mjs` as the run-bundle SSOT and keeping shared modules free of Phase imports.
- [ ] 2.3 Move state/controller primitives into `scripts/shared/state/` and update state-header/README imports without changing schemas, fingerprints, or reset/gate semantics.
- [ ] 2.4 Move the canonical JSON implementation into `scripts/contracts/canonical_json.mjs` with a stable zero-logic `shared/identity/canonical_json.mjs` facade; move generic byte hashing, notes-receipt, and provider-neutral final-slide identity/read primitives into shared identity, remove `stableJson`, and prove byte/fingerprint equivalence with no contracts-to-shared edge.
- [ ] 2.5 Split HTML review evidence into the public `shared/state/html_review_evidence.mjs` facade and private `html_review_evidence_core.mjs`; make bundle-layout and facade callers construct the same trusted context without a facade cycle.
- [ ] 2.6 Extract the narrowly scoped versioned pure source-AST/review-projection contracts used by Phase 1/2/3 publishers and the review core; remove Phase source-contract/renderer imports from the core, recompute current content/system/recipe/page evidence from a confined closed byte map plus verified persisted records, and freeze notes-only, copy-only, visual-system, recipe, and page-dependency parity goldens while preserving all five synchronous interfaces without a second validator.
- [ ] 2.7 Build an import-safe provider-free Phase 0 interface; move base environment, HTML runtime profile/inspection, and font readiness behind it; keep the direct env-check adapter separate, update `scripts/fonts/`/`scripts/fixtures/` resolution, and preserve all modes, check names, and zero-npm startup closure.
- [ ] 2.8 Add the import-safe provider-diagnostic operation to the Phase 5 interface over legacy private logic, then keep Phase 0 provider-free and make `00-setup/env-check.mjs`/root doctor invoke it only after Phase 0 prerequisites and explicit selection; prove base mode statically loads no Phase 5/provider client or Phase 3 renderer.
- [ ] 2.9 Build the Phase 1 interface and regroup structured source, slide identity, render-policy, and content parsing implementation behind that seam; preserve legacy branch behavior and stable slide IDs.
- [ ] 2.10 Build the Phase 2 interface and regroup visual configuration, asset catalog, tokens, geometry consumers, and component implementation behind that seam.
- [ ] 2.11 Split pure geometry schema/formula/validation/semantic hashing from Phase-2 file resolution/loading; move the pure definition into `scripts/contracts/`, convert `generate_html_family_geometry.mjs` and `seed_html_visual_presets.mjs` to import-safe no-shebang/no-top-level-write functions exercised by contract tests, update the preset owner path from obsolete `workflow/01-visual` to `workflow/02-visual-system`, and keep contracts free of Phase back edges.
- [ ] 2.12 Build the Phase 3 interface and move HTML source-contract/Stage-1 orchestration plus direct Stage-1 adapter logic into its private tree without changing plan receipts.
- [ ] 2.13 Move Phase 3 browser capture, renderer, HTML publication/lock, preview, and composition implementation; consume Phase 0 runtime readiness and verify font/overflow behavior through public interfaces.
- [ ] 2.14 Split provider-neutral final-slide record schemas/fingerprints and caller-supplied byte/record verification into `shared/identity/`; keep Phase 3 owner-path/current-manifest/PNG-decode/write/lock/commit behavior and Phase 5 legacy manifest/discovery/profile/materialization/image-decode/adapter behavior private, remove `fast-png`/canvas and all discovery/writes from shared identity, and prove `shared -> Phase` remains absent.
- [ ] 2.15 Move Phase 3 HTML Stage-4/Stage-5 assembly and notes implementation behind its interface, consume the shared provider-neutral final-slide identity/read contract, and preserve PPTX/notes bytes and receipts; keep legacy final-slide adaptation in Phase 5.
- [ ] 2.16 Complete the Phase 5 interface begun by the provider-diagnostic seam, then move structural versioning, local iteration, and classifier implementation behind it; preserve exact plan-hash/identity transactions.
- [ ] 2.17 Refactor HTML migration to call Phase 3 public preview/materialization operations and shared state/identity interfaces only; remove renderer/object-store/unified private imports.
- [ ] 2.18 Move markerless Image2 transport plus the complete `image_provenance.mjs` business module (generation profile/fingerprint, raw-render manifests, inspection/repair/materialization) and style-master/contact-sheet/header-lock implementation only under `05-iteration/legacy-image2/`; make it consume shared canonical JSON/byte hashing rather than export generic aliases, preserving legacy prerequisites and provider boundaries.
- [ ] 2.19 Leave `scripts/04-image2-refinement/` README-only and add an explicit architecture test proving no modern Image2 command, adapter, provider transport, or executable is introduced by this change.
- [ ] 2.20 Reserve the future Phase-4 private transport-port seam in design/ownership metadata only; do not add a shared provider module, placeholder adapter, authorization record, or runtime import.
- [ ] 2.21 Before any canonical path switch, run focused parity through every target Phase/shared interface, verify the exact Phase adjacency and five-adapter import closure against the synthetic architecture fixtures, and record the passing checkpoint; target modules may coexist only in the uncommitted migration worktree and old-path removal remains mandatory at cutover.

## 3. Direct CLI And Import Migration

- [ ] 3.1 Update `ppt_flow.mjs` to use command/marker-selective string-literal dynamic imports of only Phase interfaces and declared public shared interfaces under `shared/cli`, `shared/run-bundle`, and `shared/state`; route every existing command through the same owner without changing marker probing, options, exits, logical diagnostic `where`, child-envelope behavior, or pre-install doctor startup.
- [ ] 3.2 Move and update registered direct executables to their canonical owner paths as thin CLI adapters over their owning Phase `index.mjs`; keep Phase interfaces free of executable imports/bootstrap, explicitly split files that currently mix Commander/direct-entry setup with exported application behavior, and preserve Phase 0, Phase 3 Stage, Phase 5 legacy, and shared run-bundle CLI behavior.
- [ ] 3.3 Refactor the exact non-root cross-owner adapter allowlist—`00-setup/env-check.mjs` plus Phase-3 `unified_pipeline.mjs`, `stage1_build_inputs.mjs`, and `stage4_build_pptx.mjs`—to coordinate/probe/classify only at the process seam and delegate to public Phase/shared interfaces; keep Stage 5 as an ordinary Phase-3 adapter over an import-safe Phase-3 notes/PPTX operation that Phase 5 may call only through `03-html-production/index.mjs`, and import no owner-private implementation.
- [ ] 3.4 Replace basename-only executable registration with normalized path-qualified inventory and update direct-entry/return-audit fixtures for every registered executable, including `lessons.mjs`.
- [ ] 3.5 Rewrite all static imports, literal dynamic import specifiers, `import.meta.url` resource resolution, child-process invocation paths, shebang/bootstrap query paths, and safe inspect/invocation path fields affected by the move; preserve stable logical diagnostic `where` values.
- [ ] 3.6 Run the import-direction checker and remove every non-allowlisted Phase-to-foreign-internal, shared-to-Phase, root-to-private, and HTML/legacy Image2 crossover before deleting old paths.
- [ ] 3.7 Perform one atomic canonical-path cutover for the five cross-owner process adapters, all ordinary CLIs, root delegation, imports, executable registry, diagnostics, and resource paths; after this task no production caller may resolve an old path and focused old-path rejection must pass.

## 4. Test And Verification Migration

- [ ] 4.1 Move shared, Phase-0, Phase-1, and Phase-2 unit/integration suites plus their static fixtures into mirrored owners; place state/controller/review evidence under `tests/shared/state/` and lessons/layout under `tests/shared/run-bundle/`, and keep helper modules importable and provider-free.
- [ ] 4.2 Move Phase-3 HTML delivery, renderer/runtime, benchmark, and runtime-evidence suites; replace private-wiring assertions with Phase-interface assertions.
- [ ] 4.3 Move only Phase-5 structural, migration, classifier, and markerless legacy suites under `tests/05-iteration/`; retain provider fakes only at the explicit legacy adapter seam and do not absorb shared state/lessons tests into Phase 5.
- [ ] 4.4 Move E2E suites into their final outcome owners: HTML delivery to Phase 3, structural/migration/legacy to Phase 5, and state/lessons to `tests_e2e/shared/{state,run-bundle}`; add README-only owners where no suite exists and create only the Phase-4 absence contract, with no paid refinement journey.
- [ ] 4.5 Replace private-file wiring tests with Phase-interface tests at the same observable behavior surface, retaining only stable pure contract/golden and true adapter tests at justified internal seams.
- [ ] 4.6 Change `vitest.config.mjs` to recursively discover nested `tests/**/test_*.mjs` and `tests/**/test-*.mjs` suites, preserve existing test exclusions/fixtures, and prove root flat business files are absent.
- [ ] 4.7 Complete `tests/contracts/source-test-ownership-v1.json` for every Phase interface, public shared interface, declared executable/architecture/canonical/source-AST/review-projection contract interface, direct executable, unit/integration suite, and owning E2E journey; make missing, duplicate, and mismatched entries fail closed.
- [ ] 4.8 Add focused tests for architecture layout, import direction, command-selective load closure, executable inventory, recursive discovery, source/test manifest, old-path rejection, Phase-4 absence, exact shared-helper exceptions, and no `deck_*`/`dpt_*` fixture access.

## 5. Documentation, Playbook, And Spec Path Cutover

- [ ] 5.1 Update `scripts/README.md` with the exact target tree, deep Phase interface seams, direct executable inventory, import rules, manifest location, and pointers to canonical `cli-surface`/`framework-script-layout` authorities.
- [ ] 5.2 Move the mixed `agent-prompts.md` appendix to `PPTMAKER_FRAMEWORK/reference/agent-prompts.md` and `change-classifier.md` to Phase 5, then update `COMMANDS.md`, `BOOTSTRAP.md`, `AGENTS.md`, charter, workflow references, and playbook references to canonical paths.
- [ ] 5.3 Update active OpenSpec delta/main-spec path references and coherence fixtures for `framework-directory-layout`, `cli-surface`, `commands-reference`, `bootstrap-env-guidance`, `framework-charter`, `playbook-execution`, `environment-check`, `lessons-management`, `node-specification`, `run-bundle-layout`, `run-bundle-management`, and `pipeline-orchestration` without copying another capability's schema.
- [ ] 5.4 During the final implementation path cutover, apply the design's Purpose/preamble reconciliation table to the twelve path-owning main specs, removing stale counts/runtime/Image2 summaries without changing unrelated requirements; then run main-spec coherence before archive.
- [ ] 5.5 Update controller/shared-node manifests and playbook metadata to point to Phase interfaces/direct executable owner paths, preserve lifecycle/module values, and keep Phase 4 unregistered.
- [ ] 5.6 Refactor `contracts/framework_coherence.mjs` to resolve documented CLIs and authority pointers through canonical path-qualified owner paths rather than basenames; run active Markdown/CLI example coherence checks and remove all stale flat script paths, `scripts/lib/` references, old classifier/prompt paths, and old direct executable examples, retaining only exact historical/archive exceptions.

## 6. Atomic Removal And Regression

- [ ] 6.1 Remove the old flat business scripts, `scripts/lib/`, old direct executable paths, and any compatibility shim collection only after all imports, docs, tests, inventories, and controllers resolve to canonical paths.
- [ ] 6.2 Atomically connect the architecture checker to default `npm test` only after the real source/test trees match the final contract; verify no transition mode or bypass remains.
- [ ] 6.3 Run targeted Phase 0/3/5 interface, CLI envelope, direct-entry, docs, architecture, and source-to-test tests and compare command envelopes, exits, stdout, diagnostics, and receipts with the recorded baseline.
- [ ] 6.4 Run complete `npm test`, all relevant `tests_e2e`, doctor/runtime evidence, HTML benchmarks, bundle self-check, CLI return audit, and documentation coherence suites; verify no provider call appears on HTML/local-only paths.
- [ ] 6.5 Run `git diff --check`, `openspec validate restructure-framework-script-modules --strict`, `openspec validate --all --strict`, old-path/flat-layout audits, and source-to-test manifest audit; resolve every failure before declaring the change ready to archive.
- [ ] 6.6 Confirm artifact bytes/fingerprints, state/gate/reset/migration semantics, markerless legacy behavior, and canonical `ppt_flow` command compatibility are unchanged except for explicitly breaking internal direct paths.
