## 1. Renderer Contract and Deterministic Composition

- [ ] 1.1 (`html-slide-rendering`) Pin exact `echarts@6.1.0` and lock/transitives; record Apache-2.0/license/audit evidence; probe SSR SVG output on the pinned Node majors/current Mac; define and golden-test the closed ECharts option adapter and renderer-output SVG subset before accepting generated SVG.
- [ ] 1.2 (`html-slide-rendering`, `html-render-runtime`) Run a pinned-Chromium experiment for exact `1000 x 562.5` CSS clipping at device scale factor 2, prove the `2000 x 1125` PNG profile/timeout/cleanup behavior, and update design/spec/profile before implementation if fractional capture is not deterministic.
- [ ] 1.3 (`html-slide-rendering`) Implement the dependency-light page preparation seam that consumes only a validated Change-2 plan, escapes all authored strings, embeds only page-used verified WOFF2 shards/assets, emits inert self-contained HTML, and rejects every external/executable resource form without parsing source or resolving alternate controls.
- [ ] 1.4 (`html-slide-rendering`) Implement one closed component registry for all ten families and every typed block/header/callout using only resolved named geometry/theme tokens; enumerate all 68 variants and reject missing/extra boxes, reordered collections, undeclared overlays, and arbitrary HTML/CSS/coordinates.
- [ ] 1.5 (`html-slide-rendering`) Implement deterministic chart, asset, icon-composition, abstract-pattern, fallback, selected, and stale adapters; keep fallback-for-review forcing explicit; validate embedded raster/passive-SVG bytes and ECharts SVG without widening Change-2 catalog/source semantics.
- [ ] 1.6 (`html-slide-rendering`) Implement pinned-runtime sequential browser composition with zero-network/service-worker guards, animation disabling, local font readiness, CDP custom-font evidence for rendered roles, DOM field/slot markers, root/text/chart/image/overlay pixel-bound checks, bounded diagnostics, per-page/deck timeouts, and `finally` cleanup.
- [ ] 1.7 (`html-slide-rendering`) Implement `composition_fingerprint_v1` and `html_delivery_digest_v1` with checked-in field classification/coherence tests proving position/source-locator/order/unreferenced-catalog exclusions and renderer/runtime/theme/geometry/fallback/font/version inclusions.
- [ ] 1.8 (`html-slide-rendering`, `run-bundle-layout`) Implement separate schema-closed HTML-page/final-slide/preview manifests, exact receipts and evidence, hidden transaction directories, pre-rename byte/realpath rechecks, atomic owned-set replacement, stale-file cleanup only after success, and crash/failed-first-run preservation.

## 2. HTML Production, Delivery, and Local Maintenance

- [ ] 2.1 (`cli-surface`, `html-slide-rendering`) Add registered `stage2_render_html.mjs` and `stage3_compose_slides.mjs` ESM CLIs with canonical arguments, write-free dry-run/help, provider-option rejection, existing failure envelopes, secret/path/prose-safe diagnostics, and direct executable/return audits.
- [ ] 2.2 (`pipeline-orchestration`) Replace the temporary HTML delivery-unavailable route with one complete HTML branch adapter for validate/preview/Stages 1-5/build while preserving Change-2 Stage-1 publication/validation restrictions and keeping marker probe before all branch readiness/writes.
- [ ] 2.3 (`pipeline-orchestration`, `html-slide-rendering`) Add production-equivalent representative/affected-page preview and contact-sheet orchestration that may run with gates pending, publishes no PPTX, never resolves provider prerequisites, and emits exact review-plan/evidence hashes.
- [ ] 2.4 (`pptx-assembly`) Deepen `render_artifacts.mjs` to a provider-neutral verified `final-slide` contract; update Stage 4 to consume the explicit branch manifest in plan order, bind HTML delivery digest or legacy lineage, verify PNG SHA/dimensions/fingerprints, and remove engine/glob/position inference without changing full-frame PPTX output.
- [ ] 2.5 (`pipeline-orchestration`, `pptx-assembly`) Complete HTML Stage 4/5/contact-sheet/final-review/readiness receipts and atomic publication; prove notes use the existing current assembly/source/order contract and that delete/reorder cannot shift stable-ID notes.
- [ ] 2.6 (`pipeline-orchestration`, `commands-reference`) Implement pipeline-first classification and public HTML Local Slide Rebuild, Local Deck Rebuild, Notes-Only Refresh, and scoped dry-run/build paths with smallest stale sets, exact review obligations, provider traps, and markerless legacy behavior unchanged.
- [ ] 2.7 (`slide-identity-and-ordering`, `pipeline-orchestration`) Extend structural source preview/apply with explicit target-local HTML materialization: preserve raw fences/IDs/notes/fingerprints, reuse position-independent page/final artifacts, rebuild order-dependent delivery, revalidate staged effective controls, and make zero remote/Image2 writes.
- [ ] 2.8 (`run-bundle-management`, `slide-identity-and-ordering`, `cli-surface`) Add `ppt_flow migrate-html preview/apply` with canonical version-local candidate controls, complete source/full-deck comparison, exact plan hash, hidden-vNext target validation/render, atomic publish, no prompt inference/generated-copy/authorization inheritance, and drift/decline/crash recovery.

## 3. Run-Bundle Default and Physical Ownership

- [ ] 3.1 (`run-bundle-layout`, `run-bundle-management`) Extend `bundle_layout.mjs` constants/tree/whitelists/resolvers/checks for `_generated/html_production/{html_pages,final_slides,preview}`, migration scratch, lazy Image2 scratch/generated/accepted partitions, pipeline-specific ownership, and conformant absence; reject candidate/authorization history under HTML/upstream paths.
- [ ] 3.2 (`run-bundle-management`) Atomically switch `bundle_layout --init` and `ppt_flow init` to a valid HTML-first/mnemonic structured source template, valid `html_first` visual controls, empty v2 catalog/README, state/lessons scaffolding, and no style master/legacy prompts/generated/Image2 directories.
- [ ] 3.3 (`run-bundle-management`, `pipeline-orchestration`) Preserve markerless old decks and v1/absent asset catalogs without mutation; make checks/readiness/status pipeline-aware and prove stray opposite-branch generated artifacts cannot satisfy gates or delivery.
- [ ] 3.4 (`run-bundle-layout`, `html-slide-rendering`) Add page/deck size, sequential memory, runtime, receipt-count, and file-count benchmarks for worst valid HTML embedding/rendering; document acceptance limits without weakening integrity/overflow checks.

## 4. Workflow, Playbooks, Gates, and State Migration

- [ ] 4.1 (`framework-directory-layout`) Atomically migrate `workflow/` to exact final directories `00-setup`, `01-content`, `02-visual-system`, `03-html-production`, README-only unavailable `04-image2-refinement`, and `05-iteration`; remove old active directory aliases and update every active file/link/frontmatter reference.
- [ ] 4.2 (`framework-directory-layout`, `framework-charter`) Rewrite/move the roadmap-mandated workflow files/templates/presets: structured content/families in Phase 1, renderer-neutral visual system in Phase 2, local Stages 1-5 delivery in Phase 3, no executable Phase 4, HTML/local iteration in Phase 5, and legacy whole-page guidance only in `reference/legacy-image2-first-maintenance.md`.
- [ ] 4.3 (`playbook-execution`) Rewrite `create-deck`, quick preview, content/visual/notes/structure/style iteration, migrate-import, and shared classifier for the complete HTML path; add isolated `legacy-image2-maintenance`; keep `probe-image-channels` off-path Phase 0; register exactly no modern Image2-refinement execution and validate a checked-in controller/node manifest.
- [ ] 4.4 (`node-specification`, `playbook-execution`) Migrate node metadata to lifecycle `0|1|2|3|4|5` and final method-module enums; reject removed names and Change-3 Phase-4 executable nodes; update NODE-SPEC, parser, condition catalog, fixtures, generated controller controls, and consumer semantics together.
- [ ] 4.5 (`node-specification`) Implement state schema v3 and an exhaustive checked-in old-playbook/node/module mapping keyed by detected pipeline; preserve executions/status/evidence/decisions/waits/gates/reserved records/stacks, map markerless work only to legacy ownership and HTML work only to new controllers, and return non-writing `replacement_required` for every ambiguous/missing/conflicting case; prove idempotency.
- [ ] 4.6 (`node-specification`, `pipeline-orchestration`, `cli-surface`) Add reserved `html-visual-review` and pipeline-specific content/visual gate evidence: deck visual-system fingerprint/coverage and page-local reviews with forced-fallback preview; make approval exact-plan-hash bound, keep copy edits from staling deck approval, stale only affected page reviews for valid asset/family changes, and isolate legacy header/visual evidence.
- [ ] 4.7 (`node-specification`, `playbook-execution`, `commands-reference`) Update `state`, `status`, resume cards, next-node calculation, final completion, human replacement/restart, and pipeline/controller ownership so complete HTML delivery has no pending refinement debt and legacy sessions resume without greenfield restart.
- [ ] 4.8 (`framework-charter`, `bootstrap-env-guidance`, `commands-reference`) Update root/charter/BOOTSTRAP/COMMANDS/AGENTS/glossary/classifier/scripts READMEs/reference/templates/OpenSpec context to one HTML-complete narrative, base local readiness, real-artifact visual gates, pipeline-specific maintenance terms, unavailable modern refinement, explicit migration, and legacy-only optional Image2 troubleshooting.

## 5. Verification and Apply-Ready Closure

- [ ] 5.1 (`html-slide-rendering`) Add unit/golden fixtures for all families/min/max/bilingual/callout/fallback-selected-stale/chart kinds/formatters, every 68 geometry variant, HTML escaping/self-containment, exact font-shard selection, ECharts SVG, component order, and fingerprint classifications.
- [ ] 5.2 (`html-slide-rendering`, `html-render-runtime`) Add current-Mac pinned-browser screenshot/measurement tests for exact PNG profile, non-empty pixels, custom-font use, text/chart/image overflow, allowed overlays, network/service-worker traps, timeout/cleanup, deterministic rerender, and explicit no cross-OS pixel claim.
- [ ] 5.3 (`html-slide-rendering`, `pipeline-orchestration`, `pptx-assembly`) Add integration tests for manifest schemas/receipts/atomic drift/crash/no-directory failure, per-slide reuse/reorder delivery digest, contact sheet/PPTX/notes lineage, missing/stale/ambiguous final-slide rejection, and complete exact write sets.
- [ ] 5.4 (`pipeline-orchestration`, `node-specification`, `cli-surface`) Add gate/freshness/classifier/state-v3/old-state-stack/resume/replacement/approval-hash/command-envelope tests, including HTML↔legacy evidence and artifact isolation plus zero provider adapter access on every HTML path.
- [ ] 5.5 (`slide-identity-and-ordering`, `run-bundle-management`) Add structural and migration integration/E2E fixtures for move/delete/insert, target-local zero-remote rebuild, raw-fence/notes/identity preservation, full comparison/decline/confirm, target drift, hidden staging recovery, and unchanged legacy version.
- [ ] 5.6 (`framework-directory-layout`, `playbook-execution`, `run-bundle-management`) Extend full active-link/path/frontmatter/node-enum/controller-manifest/template/init/bundle self-check tests; assert final six workflow directories, README-only Phase 4, zero old active paths, and no production `deck_*`/`dpt_*` fixture dependency.
- [ ] 5.7 (`pipeline-orchestration`, `run-bundle-management`, `framework-charter`) Add a fresh-user E2E from init through structured authoring fixture, content/real visual approval, local HTML preview/build, contact sheet/PPTX/notes/final completion with no Image2 env/style master/provider call/lazy refinement directory/pending node.
- [ ] 5.8 (`pipeline-orchestration`, `playbook-execution`) Run markerless legacy create/pilot/build/title/generated-image/structural/resume E2E under the migrated framework and prove current remote prerequisites/review/materialization behavior remains isolated and functional.
- [ ] 5.9 Run focused renderer/orchestration/state/migration suites, full `npm test`, relevant `npm run test:e2e`, CLI executable/return audits, dependency/license/lock/`npm audit --omit=dev`, benchmarks, bundle self-check, docs coherence, `git diff --check`, strict change/all OpenSpec validation, and a final scope audit confirming no modern Image2 command/adapter/authorization/candidate/promotion/cleanup or extra workflow root was introduced.

## 6. Requirement Traceability

- [ ] 6.1 Audit every proposal capability, delta requirement, design decision, roadmap Change-3 inclusion/completion bullet, and `06-framework-directory-impact.md` file/path rule against tasks/tests; record any intentional deferral by reopening artifacts rather than silently omitting it, then mark this traceability task complete only when the fresh zero-Image2 vertical slice and legacy compatibility completion lines are both fully covered.
