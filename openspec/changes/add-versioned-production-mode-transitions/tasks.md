## 1. State-Owned Transition Protocol

> Implementation progress (2026-07-22): Tasks 1.1 and 1.2 are complete. Schema-v5 execution binding, the closed source suspension/restore path, receipt-bound target handoff, and selected-run fail-closed behavior are covered by focused state tests. Task 1.3 is next.

- [x] 1.1 Advance state to schema v5. Bind the active context, every ordinary controller record, and every ordinary stack frame to normalized `run_version`; make `startPlaybook`, `switchPlaybook`, `resumePlaybook`, node/evidence/decision writers, active-record queries, entry/exit evaluation, state/status cards, and read-only validation fail closed on a selected-run mismatch. Migrate v4 only from a persisted exact version or an unambiguous single visible version; an exact active legacy `apply-html-migration` may bind only from its matching persisted `migration_source_version`/execution/hash/mode, while an exact historical confirmation may atomically bind only through its re-inspected closed confirmation. Preserve ambiguous bytes for the explicit repair/replacement route. (Capabilities: `node-specification`.)
- [x] 1.2 Add the exact-source, CAS-bound cross-pipeline transition transaction in the state owner: candidate receipts, anticipated vNext, target mode/pipeline, exact plan confirmation, target registration, bounded audit history, and no competing mode/transition store. Prepare/preview are state-nonwriting; confirmation replaces the resumable stack with one schema-closed non-resumable transition-suspension frame containing the complete source controller context and starts only closed `migrate-import/apply-production-mode-transition`; only atomic terminal registration/baseline handoff or no-target source restoration may satisfy its transition exit and replace context. Publication-only, registration-pending, journal/recovery hard-stop, and target-conflict states remain `in_progress` with typed recovery. (Capabilities: `node-specification`.)
- [ ] 1.3 Implement the closed uncertain-owner recovery-confirmation write: only the state owner may persist a CAS-bound `kind:user` no-active-apply record after it verifies the exact old-enough uncertain journal token; bind plan/source execution/source-target versions and journal-byte SHA, invalidate it on any drift, never store or emit the raw token, and require it before takeover. (Capabilities: `node-specification`, `cli-surface`.)
- [ ] 1.4 Preserve the existing in-place HTML-mode transition while making every `html-* <-> image2-only` in-place setter return transition guidance with zero mutation; implement source-preserving target handoff without rerunning init, idempotent visible-target recovery, and no-visible-target recovery that restores the exact source controller context. Create completed baseline records only after rechecking their original target exits: `instantiation`; target-only user `checkpoint-intake` decision/evidence from the plan-hashed target-intake object; and selected author/configuration nodes with new target source/control evidence. Each baseline record must carry the closed target receipt/source-control binding; gates, reviews, authorization, refinement, generated outputs, and final completion remain absent/pending. (Capabilities: `node-specification`, `playbook-execution`.)
- [ ] 1.5 Add focused state tests for v4-to-v5 migration/ambiguity preservation, mismatched-run observe/advance/write refusal, nested same-version round trip, stale CAS/plan/source drift, decline with no pointer write, single-frame transition-suspension validation/non-resume/history archival, legacy migration-field isolation, target conflict, exact target-baseline entry/exit satisfaction and provenance, source evidence preservation, interrupted registration, recovery-confirmation replay/drift rejection, and recovery without manual state/generated edits. Prove publication-only, journal conflict, uncertain-confirmation absence, and ambiguous-target diagnostics cannot complete/advance the transition apply node; prove only atomic target handoff or source restoration replaces that context. Include v4-to-v5 legacy confirmation/apply/journal/receipt-handoff continuations and prove that malformed or fresh durable legacy requests cannot obtain a run binding. (Capabilities: `node-specification`.)

## 2. Directional Candidates And Publication

- [ ] 2.1 Generalize the existing Image2-to-HTML candidate/preview/apply mechanics into the separate `_scratch/production-mode-transition/` adapter to select `html-only` or `html-then-image2`, retain no-replace/journal/receipt rules, isolate it from legacy `_scratch/html-migration/`, and limit transition proof to the existing runnable HTML contract rather than HTML quality or visual parity. Register the exact scratch immediate entries and confined artifact validation in `bundle_layout.mjs`, `run-bundle-layout` specs, and layout tests without relaxing other scratch/version roots. (Capabilities: `run-bundle-management`, `run-bundle-layout`, `pipeline-orchestration`.)
- [ ] 2.2 Add the HTML-to-Image2 explicit markerless candidate path with confined source/control inputs, offline validation, exact plan receipts, and no inference from HTML source prose/notes, pixels, generated artifacts, metadata, or history.  Restrict both directional preparers to an identity/order continuity ledger and require all renderer-owned target fields to be explicitly authored. (Capabilities: `run-bundle-management`, `pipeline-orchestration`.)
- [ ] 2.3 Publish both directional targets as clean vNext source/control with the exact transition preview/journal/success schemas, target-specific receipt and post-publication mode registration; preserve source bytes/evidence and report target `needs_local_materialization` or `needs_render` without implicit rendering. (Capabilities: `run-bundle-management`, `node-specification`.)
- [ ] 2.4 Extend pipeline tests to prove HTML target preview has no new quality score/parity gate, neither directional candidate copies/infer target fields from opposite-pipeline inputs, and HTML-to-Image2 preview has zero provider/style-master transport; retain normal post-publication Image2 authorization/review behavior. (Capabilities: `pipeline-orchestration`.)

## 3. CLI And Controller Handoff

- [ ] 3.1 Implement the closed `ppt_flow state` transition prepare/preview/confirm/apply/recovery-confirmation/recover grammar, including exact owner-token and age/confirmation recovery rules, plan hashing of mode/source execution/candidate/state/target-marker/target-intake inputs, single-envelope prerequisite-first diagnostics, mutual exclusions, and return-audit entries without making `state` a generic editor. Keep `migrate-html` bounded to compatibility and route mode-governed sources before mutation, except that an exact historical legacy confirmation/apply/journal/receipt-handoff checkpoint may complete through its current closed command; reject fresh durable `migrate-html prepare`/`preview`, a mismatched source/execution/hash/mode, and any attempt to use legacy fields as transition authority. (Capability: `cli-surface`.)
- [ ] 3.2 Update `migrate-import`, `create-deck`, and state resume/handoff behavior for both directions: add the isolated `apply-production-mode-transition` declaration with `transition_apply_current` entry and `transition_publish_or_recovery_recorded` exit, plus recovery-confirmation guidance; register/test both closed conditions in `CONDITIONS`, the index validator, and `charter/NODE-SPEC.md` catalog. Update `controller-manifest-v3.json` and its exact list test, and correct `charter/NODE-SPEC.md` to state and assert the canonical inventory: 11 ordered controllers, 1 shared node, and 44 globally unique nodes, alongside schema v5. Do not change `state-migration-map-v3.json`: it owns legacy node aliases and requires no alias for this new canonical node. Show exact target mode/hash, preserve incomplete source controller context on decline/failure through a non-resumable run-bound snapshot, and create only legally satisfied target baseline records before entering normal HTML preview or Image2 style-master authorization after verified registration. (Capabilities: `playbook-execution`, `node-specification`.)
- [ ] 3.3 Update `COMMANDS.md`, change classification, charter, and controller guidance so page-authority changes use versioned transition while HTML-quality-only requests remain outside this change; retain normal Image2 quality/authorization disclosure. (Capabilities: `commands-reference`, `framework-charter`.)

## 4. Verification

- [ ] 4.1 Add CLI/integration coverage for both directions, invalid/mixed flags, fresh durable-mode `migrate-html` rejection, v4-to-v5 exact legacy confirmation/apply/owner-journal/receipt-handoff continuation, stale confirmation/hash/input/target-intake drift, collision, 60s same-host and 300s uncertain recovery matrices including persistent confirmation/replay failure, scratch/schema isolation, selected-run execution mismatch refusal, no wrong-owner writes, and one final diagnostic envelope. (Capabilities: `cli-surface`, `run-bundle-management`, `run-bundle-layout`.)
- [ ] 4.2 Add playbook simulations for incomplete-source preservation, decline without pointer mutation, non-resumable full source-context suspension, target-intake confirmation and every target baseline original exit, normal next-node entry without init after registration, source/target mode consistency, and cross-pipeline refusal outside the transaction. Prove recoverable hard-stops, publication-before-registration, journal conflict, and missing recovery confirmation leave `apply-production-mode-transition` in progress; prove terminal target handoff/source restoration replaces rather than generically completes it. (Capabilities: `playbook-execution`, `node-specification`.)
- [ ] 4.3 Add mocked E2E flows for Image2-to-HTML and HTML-to-Image2, including decline/failure recovery, source-work preservation, offline HTML-to-Image2 preview, and normal Image2 post-publication quality/authorization boundaries. (Capabilities: `run-bundle-management`, `pipeline-orchestration`.)
- [ ] 4.4 Run focused state/CLI/pipeline/controller suites, `npm test`, `npm run test:e2e`, CLI return and docs-consistency audits, `git diff --check`, and strict OpenSpec validation; prove no HTML visual-quality evaluator or Image2 preview provider call was added. (Capabilities: all modified capabilities.)
