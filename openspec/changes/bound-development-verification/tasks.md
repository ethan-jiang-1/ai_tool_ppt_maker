## 1. Core Tier Contract And Admission

- [ ] 1.1 [framework-script-layout] Add `tests/contracts/development-verification-core-v1.json` as the versioned checked-in core inventory and sole `npm test` membership authority, with exact `{ schema: "pptmaker-development-verification-core-v1", budget_ms: 60000, entries: [...] }` keys and a nonempty lexically sorted unique array of at most 16 entries; populate it only with repository-relative, pure-Node entries and the static architecture check whose repeated run fits the total 60,000-ms budget. Cap audited files at 1 MiB and the local closure at 256 files/8 MiB; add schema/fixture coverage for valid, duplicate, unsorted, empty, missing, escaping, malformed, unsupported-name, and every input-limit breach.
- [ ] 1.2 [framework-script-layout] Implement the JS-owned inventory reader and a linear, Node-built-in-only conservative ESM lexer for the bounded local `.mjs` import closure; do not reuse the architecture helper that intentionally ignores computed imports. Tokenize comments/strings/template literals while scanning `${...}` expressions as code; recognize side-effect/binding imports, `export { ... } from`, `export * from`, `export * as name from`, and local/declaration exports; admit only exact `vitest` and non-prohibited `node:` built-ins as non-local boundaries; resolve only relative `.mjs` local edges beneath `tests/contracts/`, `PPTMAKER_FRAMEWORK/scripts/contracts/`, or `PPTMAKER_FRAMEWORK/scripts/shared/cli/`; and reject all other bare/package/local roots, absolute/file URLs/extension probing, import attributes, `require`/`createRequire`/`node:module`, Canvas/Chromium/Playwright/PPTX/ECharts/HTML-renderer/provider, `node:child_process`, `fetch`/WebSocket/Node-network surfaces, and every other dynamic or unclassifiable edge before any test child starts, with no parser package, per-inventory exception, or fallback inventory.
- [ ] 1.3 [framework-script-layout] Add mock-named test entries and fixtures whose basename matches `(?:^|[_.-])mock(?:[_.-]|$)` (for example `test_mock_*.mjs`), then prove all supported static import/re-export/declaration forms and comment/string/template handling, including rejection of a dynamic import inside a template expression; prove exact `vitest`/safe-`node:` admission and rejection of another bare package, `node:child_process`, import attributes, `require`/`createRequire`/`node:module`, absolute/file URL/extensionless local edges, or an out-of-root local edge; prove prohibited direct, transitive, network-primitive, and dynamic imports each produce `invalid_inventory` with zero Vitest/browser/renderer/provider child startup; prove a malformed inventory short-circuits before a prohibited mock fixture is read or Vitest is resolved; and prove an admitted public dependency-free seam remains selectable.

## 2. Bounded Runner And Commands

- [ ] 2.1 [framework-script-layout] Implement `tests/contracts/run_development_verification.mjs` as the Node-owned core verifier (repository-verification tooling, not a production CLI or Vitest test entry). Start the total 60,000-ms clock at runner process entry; use the bounded linear admission audit with a 5,000-ms preflight deadline, child execution limited to 50,000 ms, and owned shutdown limited to the remaining 5,000 ms; return `invalid_inventory` for admission input limits and `timed_out` without a child when completed preflight exceeds its deadline; and write exactly one verifier-stdout JSON line with exact `schema`, `tier`, `result`, integer `duration_ms` <=60000 from process entry, nonempty `next_action`, and optional serialized-UTF-8/JSON-escaped <=8192-byte `failure_tail` fields for `passed`, `failed`, `timed_out`, `invalid_inventory`, and `unavailable` outcomes, including local-Vitest resolution/owned-child startup failure.
- [ ] 2.2 [framework-script-layout] Split `PPTMAKER_FRAMEWORK/scripts/contracts/framework_coherence.mjs` at its existing static/subprocess boundary: move the legacy-token registry and validator consumed by `framework_architecture.mjs` to one static-only contract module, retain subprocess documentation-command auditing in an opt-in module, migrate `test_docs_consistency.mjs` imports as needed, and update the architecture required-contract/source-to-test ownership inventories accordingly. Split `tests/contracts/test_framework_architecture.mjs` so its deterministic tree/import/ownership/legacy-token assertions are core-eligible while its base/HTML-local/markerless-provider subprocess load-closure probes live in one focused-only real-process entry. Add mock-named inert child fixtures and mock runner test entries with a standalone `mock` basename token for assertion failure, proportionally short injected preflight/execution/grace timeout, local-runner-unavailable, exact-summary-schema/uniqueness/no raw-progress passthrough/8192-byte-tail cap, nonzero/zero exit mapping, no retry/no worker escalation, and independent same-check rerun without scanning or killing unrelated processes.
- [ ] 2.3 [framework-script-layout] Implement `tests/contracts/run_selected_verification.mjs` and change `package.json` so `npm test` invokes the bounded core verifier; `test:sweep` retains recursive unit/integration discovery; remove the broad `test:watch` script; documented `test:focused` and `test:render` invocations use `--` to pass exactly one `tests/**/test_*.mjs|test-*.mjs` path; and `test:e2e` replaces broad E2E behavior with `--` plus exactly one `tests_e2e/**/test_mock_*.mjs` journey. Make the dispatcher accept exactly one path argument and reject additional flags/selectors, missing, multiple, escaping, unsupported-name, and tier-incompatible paths before child startup; define visual-engine closure as Canvas/Chromium/Playwright/PPTX/ECharts/HTML compositor/runtime, require it for render and reject it for focused; prove the mocked journey initializes zero browser/compositor/Canvas/PPTX/ECharts/provider/network boundaries.

## 3. Development Guidance

- [ ] 3.1 [framework-script-layout] Update the direct maintenance verification guidance to distinguish core, focused, render, single mocked journey, and unit/integration sweep tiers; state that a framework change runs core plus the minimum individually selected affected seams (`focused` and/or `render`) and at most one selected mocked journey when needed, never a broad E2E command.
- [ ] 3.2 [framework-script-layout] Document hard-stop recovery for invalid core admission, unavailable local runner, failed core assertion, and timeout; state the protected bounded/dependency-free feedback invariant and that renderer/browser/provider/load-closure/full sweep work is opt-in diagnostic or release sampling, not default completion evidence.

## 4. Validation

- [ ] 4.1 [framework-script-layout] Run the new core verifier repeatedly and demonstrate each successful run finishes within 60 seconds with the final summary and definitive exit; record the selected inventory and duration without claiming full release coverage.
- [ ] 4.2 [framework-script-layout] Run the minimum individually selected focused verifier/admission and architecture load-closure diagnostics plus one explicitly selected mocked representative journey. The selected load-closure diagnostic uses only command help and forbidden-module sentinels; do not run browser, HTML compositor, Canvas/PPTX, real provider, all-E2E, or broad sweep commands as the change's default validation.
- [ ] 4.3 [framework-script-layout] Run `openspec validate bound-development-verification --strict`, `git diff --check`, production-data scope audit, and review all tasks/spec scenarios against the guide/hard-stop and single-checkpoint requirements before marking the change apply-ready.
