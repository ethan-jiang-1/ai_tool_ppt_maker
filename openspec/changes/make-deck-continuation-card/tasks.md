## 1. Continuation-card content and deck-root file roles

- [ ] 1.1 **(run-bundle-layout)** Designate `deck-guide.md` as the static portable continuation card in `bundle_layout.mjs`: update the module header tree comment (around `GUIDE_FILE`/`DECK_ROOT_ALLOWED`, ~L18, L113, L228, L1570-1572) and `renderTree()` output text to state that `deck-guide.md` carries deck identity + the fixed `../PPTMAKER_FRAMEWORK` relation + the attach invitation, and that `README.md`/`AGENTS.md`/`CLAUDE.md` are thin pointers to it. Admit **no** new deck-root file. Completion: `node …/bundle_layout.mjs` tree text names the card and the pointer roles; `DECK_ROOT_ALLOWED` is unchanged in membership.
- [ ] 1.2 **(run-bundle-management)** Add continuation-card content to the `initBundle` deck-guide seed in `bundle_layout.mjs` (~L1495 `_writeIfAbsent(... GUIDE_FILE ...)`): a plain-language "attach this file to a new chat and say what you want" invitation, the deck identity, the fixed `framework_relation: ../PPTMAKER_FRAMEWORK`, and a statement that the Agent obtains the current version/position from `_state/state.yaml` (`ppt_flow state`/`status`) automatically. Reframe the existing `当前版本：v1` line as the **non-authoritative init-time** value pointing to `_state` for the current version. The seeded card MUST contain no current node, next action, gate status, or digest. Completion: a fresh temp deck's `deck-guide.md` contains all four content pieces and no status fields.
- [ ] 1.3 **(run-bundle-management)** Mirror the exact same continuation-card content in `PPTMAKER_FRAMEWORK/workflow/00-setup/template-deck-guide.md` so the manual/Expert seed and `initBundle` output do not contradict each other. Completion: template and seed agree on the attach invitation, identity, framework relation, and non-authoritative version framing.
- [ ] 1.4 **(run-bundle-layout / run-bundle-management)** Optional doc alignment: add a one-line note in the deck-root `README.md` seed (`_DIR_READMES['.']` in `bundle_layout.mjs`) and the `AGENTS.md`/`CLAUDE.md` pointer seeds naming `deck-guide.md` as the attach-to-resume continuation card; keep them thin pointers, no second procedure. Update `reference/glossary.md` Where Map with a `continuation card` / `deck-guide.md` entry. Completion: pointers route to the card without duplicating its content.

## 2. Agent entry/resume contract

- [ ] 2.1 **(framework-charter)** Extend `charter/AGENT_CONTRACT.md` §1 入口顺序 (the entry-order iron law) with the **continuation-card attachment entry**: when a user attaches a deck's `deck-guide.md` to a fresh session with a plain-language request, the Agent follows a bounded, zero-write read path — attachment parent = claimed deck root; verify a real regular `deck-guide.md` whose parent is a marker-bearing deck root; resolve the fixed `../PPTMAKER_FRAMEWORK` to the project's real direct-child soft bundle; run the existing read-only `bundle_layout --check --structure-only`; then load `deck-guide.md` and read `_state/state.yaml` + `ppt_flow state`/`status` before responding in ordinary language. State the outcomes explicitly: valid → proceed; structure-invalid → bounded read-only diagnostic (no auto-repair, no force/waiver — `guide` only); copied bytes with no original path → bounded request for the real file/explicit deck path (never guess by name/recency/search). Add: a card for a terminal deck resolves read-only and routes to rerun/new-version/new-deck; it does not reopen the closed bundle or treat a chat request as delivery approval. Completion: §1 contains the full attach-card procedure with the three outcomes and the terminal-routing clause.
- [ ] 2.2 **(framework-charter / bootstrap-env-guidance)** Add a short attach-card note to `BOOTSTRAP.md` (entry/resume surface) so the resume flow mentions dragging the continuation card into a new chat; keep it a pointer to the §1 procedure, not a second procedure. Completion: BOOTSTRAP names the continuation-card entry without duplicating AGENT_CONTRACT.

## 3. Tests

- [ ] 3.1 **(run-bundle-management, unit/integration)** Extend the existing seed-coherence / first-look-README test suite to assert, across generic init and every active deck-type template (`keynote`, `pitch`, `report`, `training`) in **fresh temporary decks**, that the seeded `deck-guide.md` and `template-deck-guide.md` contain the continuation-card content (attach invitation, identity, fixed `../PPTMAKER_FRAMEWORK` relation, Agent-reads-`_state` statement) and remain byte-aligned. No production `deck_*` data used. Completion: suite passes for all five fixtures.
- [ ] 3.2 **(run-bundle-management, negative)** Add assertions that the seeded `deck-guide.md` contains no authoritative current version, current node, next action, gate status, or digest, and that the seeded version reference is labeled non-authoritative. Completion: negative assertions pass.
- [ ] 3.3 **(run-bundle-management, focused negative)** Assert `bundle_layout --check --structure-only` remains zero-write and does not claim/verify chat-attachment provenance; assert create-if-absent (an existing deck is not rewritten by ordinary `status`/`build`/`pipeline` commands). Reuse existing zero-write/create-if-absent coverage where present, add focused cases where absent. Completion: zero-write and no-incidental-rewrite proven.
- [ ] 3.4 **(e2e evaluation)** Confirm per the design verification strategy that no new public CLI, MD Controller node, or state transition is introduced, so no `tests_e2e/` state-machine case is required; the attach-card contract is verified via the §1 doc + seed-coherence assertions. Record the skip rationale in the change. Completion: e2e skip justified and noted.

## 4. Release validation

- [ ] 4.1 Run the targeted seed-coherence tests, full regression `npm test`, `openspec validate make-deck-continuation-card --strict`, and `git diff --check`. Completion: all green.
- [ ] 4.2 Confirm no production `deck_*` data changed (the golden deck, e.g. `deck_ai_sdlc_keynote`, is untouched) and that fresh-scaffold tests — not a hand-patched deck — prove the new behavior. Completion: diff contains only framework source (`PPTMAKER_FRAMEWORK/`, `openspec/`) plus tests; zero production mutation recorded.
