## 1. Correct Root Roles

- [ ] 1.1 **(run-bundle-layout)** Add `RUN_BUNDLE.md` as an allowed optional root control and update tree/role text without making legacy absence invalid.
- [ ] 1.2 **(run-bundle-management)** Replace the erroneous continuation-card seed with a producer-owned `RUN_BUNDLE.md` renderer containing closed static absolute anchors and measured relation.
- [ ] 1.3 **(run-bundle-management)** Restore `deck-guide.md` and its setup template as the operating guide; route README, AGENTS, and CLAUDE through locator then guide without duplicating either procedure.

## 2. Bounded Locator Entry

- [ ] 2.1 **(framework-charter)** Replace the path-required `deck-guide.md` entry with the verified `RUN_BUNDLE.md` byte/locator resolution order, including deck-path-parent fallback and framework relation fallback.
- [ ] 2.2 **(node-specification)** Revalidate the existing `continuation_target_version` owner/visibility/terminal behavior as the post-locator exact-version selector; remove no state authority and add no new CLI.
- [ ] 2.3 **(framework-charter / docs)** Align bootstrap, quick start, glossary, constitution, NODE-SPEC, framework README, and coherence exceptions with the separate locator and guide roles.

## 3. Focused Verification

- [ ] 3.1 **(run-bundle-management, unit/integration)** Test fresh generic and all active mode/deck-type seeds, absolute anchors for sibling/external decks, closed manifest fields, and restored guide content.
- [ ] 3.2 **(run-bundle-management, negative)** Test create-if-absent cards/guides, legacy no-card structural validity, exact-run zero-write checks, stale/conflicting roots, and no version/deck search.
- [ ] 3.3 **(framework-charter / node-specification)** Add a black-box locator fixture from unrelated cwd using card bytes and accessible declared paths; cover active precedence, terminal target, card-parent fallback, explicit-root guides, and no state write.
- [ ] 3.4 **(documentation contract)** Assert no card claims live status or approval, no guide loses operating ownership, and no generic remote-chat support is claimed.

## 4. Release Validation

- [ ] 4.1 Run targeted locator/state/layout/doc tests, full `npm test`, `openspec validate make-deck-continuation-card --strict`, and `git diff --check`.
- [ ] 4.2 Confirm the diff changes only framework/OpenSpec/tests and does not modify production `deck_*` data.
