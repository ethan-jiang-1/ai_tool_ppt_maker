## 1. Protocol spine (doc)

- [ ] 1.1 Add AGENT_CONTRACT §11「交互节律」as one section with shortest executable bullets; title/count → 11; keep §9
- [ ] 1.2 Update BOOTSTRAP: gate-before-open rules + pre-key degraded show; change「10 条」→「11 条」
- [ ] 1.3 Sweep entry docs citing iron-law count (`CLAUDE.md`, root `CLAUDE.md`, and any other「10 条」/「10 non-negotiable」tied to AGENT_CONTRACT)
- [ ] 1.4 Patch `create-deck` `setup` gate: MUST open `style_master.jpg`; multi-round polish → `switchPlaybook(iterate-style)`
- [ ] 1.5 Patch `edit-visual` pilot/confirm review: MUST open pilot artifacts (same show law)

## 2. Explore playbooks + routing

- [ ] 2.1 Create `playbook/iterate-style.md` (nodes per D4; entry modes A/B per D11; LOCK→approve+optional 2k+resume; round extra; round≥5 advice; open on review)
- [ ] 2.2 Create `playbook/quick-preview.md` (gates prerequisite; validate→pilot→review; PROCEED/RETRY/BACK; open contact sheet; no partial PPTX claim)
- [ ] 2.3 Insert COMMANDS.md「探索 & 预览」between 全量创建 and 迭代打磨; include ordering note (visual lock → quick-preview → build)

## 3. Verify + close plans

- [ ] 3.1 Grep for stale「exactly six」/「five MD Controllers」playbook claims and「10 条」CONTRACT refs; fix stragglers in-scope
- [ ] 3.2 `npm test` green (e2e optional unless runtime touched — expect not)
- [ ] 3.3 `git mv` both plans → `_done/_closed_plans/`; update plans / `_closed_plans` / `_done` README (CLS-IDs); leave Change 2 pointer clear
- [ ] 3.4 Confirm design Acceptance; archive syncs `framework-charter` + `playbook-execution` (incl. Purpose「six files」→ eight / seven controllers)
