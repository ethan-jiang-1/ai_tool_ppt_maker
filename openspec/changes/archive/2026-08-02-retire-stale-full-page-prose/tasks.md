## 1. Correct The Active Pure Pilot Contract

- [x] 1.1 Update the `create-deck` Pure Pilot review step to present the exact Pure raw page bytes and current bindings without changing its owner-issued decision or workflow boundary.
- [x] 1.2 Update the two affected `playbook-execution` main-spec scenarios to use current Pure raw-page evidence wording while retaining Framed/Pure isolation and every existing Pilot/Expansion behavior.
- [x] 1.3 Update the source backlog plan and active-plan index to link this scoped follow-up change and keep the plan open until it is archived.

## 2. Prove The Existing Audit Is Green

- [x] 2.1 Run the focused process-document coherence test and confirm no active retired-production vocabulary remains in the reported Pure Pilot locations.
- [x] 2.2 Run `npm test`, strict validation for this change and all OpenSpec artifacts, plus `git diff --check`; confirm no CLI, state, run-bundle, or provider behavior changed.
