## 1. Establish Exact Retirement Boundaries

- [x] 1.1 Add a ledger-driven contract audit that maps every exact requirement title to `Replace`, `Retire`, `Collapse`, or `Keep`, and fails on an unclassified direct legacy-vocabulary Requirement block. Done when the audit can report coverage before and after spec sync without reading production data.
- [x] 1.2 Create `page-authority/internal/framed_capture_runtime.mjs`, `framed_runtime.mjs`, and `notes_runtime.mjs` from the three retained `03-html-production/internal/*` seams; extract only `extractNoteRecordsFromMarkdown` and `injectNotes`, then move `finalizer.mjs`, `operations.mjs`, and `notes.mjs` to those owners. Done when no current Page Authority import crosses an HTML deck contract/renderer and focused browser/font/network/cleanup evidence still passes.
- [x] 1.3 Define the command triage in code and tests: shared current command names fence a recognized historical run at adoption; legacy-only commands, flags, help, and approval choices are absent. Done when CLI help and return-audit tests prove both cases.

## 2. Collapse Current Routing And State

- [x] 2.1 Restrict the normal production resolver, init, and bundle validation to a single successful `image2-page-authority` / `page-authority-image2-v1` route. Done when legacy source/state pairs can reach only observer/adoption or repair/export guidance before provider/generated work.
- [x] 2.2 Remove HTML/header/refinement current state records, gates, recovery paths, and workflow-inspection projections while keeping the existing observer and adoption transaction byte-preserving. Done when state/inspection has one current evidence graph and one bounded historical action.
- [x] 2.3 Restrict orchestration, assembly, and notes to Page Authority raw/final/receipt lineage. Done when stale or historical final artifacts cannot be accepted as current input.

## 3. Delete Retired Production Owners

- [x] 3.1 Delete HTML-first deck contracts, renderers, object stores, review projections, and generated owners after the retained Framed runtime and notes seams are live. Done when active imports/inventory have no HTML deck-output path.
- [x] 3.2 Delete whole-page generation, Header-Lock, visual-slot refinement, their executables, public exports, and state/provider entrypoints. Done when a static import/executable audit finds no active adapter other than Page Authority.
- [x] 3.3 Replace `ppt_flow` registrations, Controller manifests/playbooks, bootstrap/COMMANDS/workflow/layout guidance, examples, and source-to-test ownership with current Page Authority/adoption terms. Done when active help/controller/document scans expose no retired choice outside labelled observer or fixture context.
- [x] 3.4 Remove obsolete legacy-route fixtures/tests and retain only explicitly labelled observer/adoption fixtures plus current Page Authority tests. Done when test ownership has no retired production test entrypoint and retained historical fixtures are consumed only by observer/adoption coverage.

## 4. Synchronize Accepted Contracts

- [x] 4.1 Apply every exact non-`Keep` ledger entry as the planned removal/replacement in the corresponding main spec, preserving `Keep` clauses. Done when the main-spec diff and ledger audit agree on all 258 rows and each `Replace` row has its current Page Authority owner.
- [x] 4.2 Update the retirement ledger/audit record with the final title and direct-vocabulary scans. Done when active executable, CLI help, Controller, state gate, and main-spec checks have no unlabelled retired route.

## 5. Focused Verification And Delivery

- [x] 5.1 Run syntax/import checks and focused contract/unit tests for the changed resolver, runtime seam, CLI/help, state/inspection, architecture, and retirement audit. Done when every changed seam has its named provider-free proof.
- [x] 5.2 Run `npm test` as the declared bounded core tier. Run the deterministic mock legacy-adoption journey only if its observer/adoption/CLI boundary changed. Do not retain local fake-provider/Canvas/PPTX lifecycle tests; do not run broad E2E, real E2E, a provider relay, or aesthetic assertions as routine evidence.
- [x] 5.3 Run `openspec validate retire-legacy-production-surface --strict`, `openspec validate --all --strict`, and `git diff --check`; review the merged main-spec diff, archive the completed change, then commit the archive and implementation together.
