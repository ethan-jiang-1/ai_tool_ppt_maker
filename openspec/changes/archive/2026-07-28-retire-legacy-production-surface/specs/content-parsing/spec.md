## ADDED Requirements

### Requirement: Current source parsing is Page Authority-only
Current production source parsing SHALL accept only the Page Authority source grammar and shall bind
each slide to its resolved Pure or Framed authority. Historical source parsing SHALL be confined to
the read-only legacy observer and SHALL NOT publish a current plan or adapter.

#### Scenario: Legacy source cannot produce a current plan
- **WHEN** a source carries a recognized retired pipeline marker
- **THEN** normal production parsing returns the adoption boundary before producing a plan, prompt, or generated owner


## REMOVED Requirements

### Requirement: Stage 1 parses markdown to JSON
**Reason**: The legacy contract is replaced by the current owner Page Authority parser.
**Migration**: Use the current contract owned by Page Authority parser.

### Requirement: Render policy requires an explicit current pipeline
**Reason**: The legacy contract is replaced by the current owner Page Authority resolver.
**Migration**: Use the current contract owned by Page Authority resolver.

### Requirement: Hero guard operates on canonicalized VISUAL TYPE values
**Reason**: The legacy production contract is retired; no current production route retains it.
**Migration**: Use the Page Authority lifecycle for new work; use the read-only observer/adoption boundary for recognized historical runs.

### Requirement: Current whole-page render mode resolves from explicit policy
**Reason**: The legacy production contract is retired; no current production route retains it.
**Migration**: Use the Page Authority lifecycle for new work; use the read-only observer/adoption boundary for recognized historical runs.

### Requirement: Content full-page prompts carry the shared header placement contract
**Reason**: The legacy production contract is retired; no current production route retains it.
**Migration**: Use the Page Authority lifecycle for new work; use the read-only observer/adoption boundary for recognized historical runs.

### Requirement: Body-lock image prompts are independent of deterministic header text
**Reason**: The legacy production contract is retired; no current production route retains it.
**Migration**: Use the Page Authority lifecycle for new work; use the read-only observer/adoption boundary for recognized historical runs.

### Requirement: Stage 1 parses VISUAL ASSETS field
**Reason**: The legacy production contract is retired; no current production route retains it.
**Migration**: Use the Page Authority lifecycle for new work; use the read-only observer/adoption boundary for recognized historical runs.

### Requirement: VISUAL ASSETS validation integrates with validateSpecRecords
**Reason**: The legacy production contract is retired; no current production route retains it.
**Migration**: Use the Page Authority lifecycle for new work; use the read-only observer/adoption boundary for recognized historical runs.

### Requirement: Stage 1 blocks identity and heading invariant violations
**Reason**: The legacy contract is replaced by the current owner Page Authority parser.
**Migration**: Use the current contract owned by Page Authority parser.

### Requirement: Content parsing gates the opt-in HTML-first branch
**Reason**: The legacy production contract is retired; no current production route retains it.
**Migration**: Use the Page Authority lifecycle for new work; use the read-only observer/adoption boundary for recognized historical runs.

### Requirement: HTML-first source diagnostics identify owned fields
**Reason**: The legacy production contract is retired; no current production route retains it.
**Migration**: Use the Page Authority lifecycle for new work; use the read-only observer/adoption boundary for recognized historical runs.

### Requirement: HTML-first slide metadata and concept remain explicit Markdown inputs
**Reason**: The legacy production contract is retired; no current production route retains it.
**Migration**: Use the Page Authority lifecycle for new work; use the read-only observer/adoption boundary for recognized historical runs.
