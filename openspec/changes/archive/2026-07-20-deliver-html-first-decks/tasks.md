> Apply progress: 17/39
> Planning review: apply-ready
> Mandatory fail-closed experiments before dependent implementation: Tasks 1.1, 1.2, and 3.4

## 1. Renderer Contract and Deterministic Composition

- [x] 1.1 (`environment-check`, `html-slide-rendering`, `bootstrap-env-guidance`) Pin exact direct `echarts@6.1.0` and lock/transitives; record license/audit evidence; add blocking base-doctor discovery/repair/root handoff; probe SSR SVG on current supported Node/Mac and profile compatibility for other majors; prove repeated canonical bytes, freeze the closed option/structural SVG validator, and deterministic `pm-<first16hex>-` instance rewriting before adapter implementation.
- [x] 1.2 (`html-slide-rendering`, `html-render-runtime`) Before compositor implementation, probe the exact paired Playwright/Chromium on the maintained Mac and prove: `1000 x 562.5` at DSF 2 with `GEOMETRY_EPSILON_CSS_PX=0.5`, nonblank `2000 x 1125`, repeated byte-identical PNG SHA, timeout/finally cleanup; CDP per-role actual bundled-font glyph evidence for HTML text, permitted inline-SVG text, Source Sans 3, Noto Sans SC, and mixed English/Hans; usable Range/SVG bbox evidence; and observable fail-closed interception/denial for document/resource navigation, `http|https|file|ftp|blob|ws|wss|custom` schemes, and service-worker attempts. If any evidence primitive, capture/tolerance, or denial hook is incomplete/nondeterministic, pause and revise artifacts to a tested profile/evidence/re-encode path before compositor code rather than weakening to `document.fonts.ready`, CSP-only confidence, rounding, or unstable bytes.
- [x] 1.3 (`html-slide-rendering`) Implement the deep `buildHtmlPages`/`composeHtmlSlides` seam over immutable opaque framework-issued validated-run contexts with exact `canonical-run|migration-preview` scope, canonical current/null versus migration-null reset ID, and closed slide-ID/dry-run/variant request; allow only canonical validation or the closed migration projected-run validator to issue a context, keep publication roots/reset IDs/theme/assets/fonts/runtime/receipts/fingerprints/manifests internal, reject forged/mutated/path/reset-injected contexts, escape authored strings, embed only page-used verified WOFF2/assets, inject fixed CSP, and reject active external/executable resources without parsing source or alternate controls.
- [x] 1.4 (`html-slide-rendering`) Implement one closed component registry for all ten families and every typed block/header/callout using only resolved named geometry/theme tokens; enumerate all 68 variants and reject missing/extra boxes, reordered collections, undeclared overlays, and arbitrary HTML/CSS/coordinates.
- [x] 1.5 (`html-slide-rendering`) Implement deterministic chart, asset, icon-composition, abstract-pattern, fallback, selected, and stale adapters; keep `effective|forced-fallback` explicit; structurally validate URL-bearing SVG/CSS, allow inert namespaces, namespace/rewrite inline SVG IDs/references, forbid typed-icon text, and verify any permitted SVG text through bundled-font measurement without widening Change-2 semantics.
- [x] 1.6 (`html-slide-rendering`) Implement pinned sequential composition with exact caps/timeouts/profile, strict line-break/normal overflow-wrap/no-hyphen/no-synthesis, abort-any-route/CSP, Unicode/font-CDP proof, exact expected leaf markers, Range/SVG text bounds, visibility/opacity, registry-only cross-owner overlap, crop/chart bounds, decoded nonblank pixels, bounded diagnostics/finally cleanup, and English+Hans horizontal-LTR scope only.
- [x] 1.7 (`html-slide-rendering`) Implement `composition_fingerprint_v1` and slide-local `composition_input_receipt_v1` with exact variant/current membership but no whole-plan order, plus effective-only ordered `html_delivery_digest_v1`; prove reorder/unrelated-slide reuse, removed-ID manifest eviction, position/source-locator/unreferenced-catalog exclusions, renderer/runtime/theme/geometry/fallback/font/version inclusions, and forced-fallback review isolation.
- [x] 1.8 (`html-slide-rendering`, `run-bundle-layout`) Implement schema-closed immutable raw-byte-SHA-addressed objects (never composition/delivery-digest-addressed) and separate atomic current manifests/receipts/locks with exact `pptmaker-html-pages-manifest-v1|pptmaker-html-final-slides-manifest-v1`, `publication_scope: canonical-run|migration-preview`, and canonical nullable `html_production_reset_id`, hard cross-scope/reset/schema-owner mismatch rejection and canonical-only delivery authority, atomic lock-dir plus exact `owner.json`/random 64-hex token/owner-kind/scope/reset/host/PID/epoch/input-scope/prior-manifest schema, one implementation-private host/PID/age/liveness classifier reusable by later journal/reset protocols, exact same-host-dead-plus-60000ms automatic recovery, invalid/empty/PID-reused/cross-host lock `CONFLICT`, reset-pending pre-lock/precommit fencing, and scope-specific confirmed whole-owner recovery (gate-journal-absent state-first idempotent full canonical HTML reset versus whole migration scratch transaction, never single-lock deletion), exclusive same-directory token-bound object temps with close/digest/absence recheck/atomic rename so partial bytes never occupy final addresses, token-bound manifest temp, prior-manifest CAS, collision/receipt/realpath checks, exact-temp-only recovery, plan-ordered effective merge that revalidates and drops stale/deleted/carried cross-scope entries outside scoped IDs, object-only forced-fallback preparation followed by same-scope preview-owner commit, fully write-free dry-run, explicit incomplete-set reporting, crash/orphan preservation, no normal-publication final-object deletion/GC beyond confirmed whole-owner reset, and no multi-file atomicity claim.

## 2. HTML Production, Delivery, and Local Maintenance

- [x] 2.1 (`cli-surface`, `html-slide-rendering`) Add registered `stage2_render_html.mjs` and `stage3_compose_slides.mjs` ESM CLIs with only canonical `--run-dir`, shared selectors, exact/default-effective variant, dry-run/help; derive all plan/control/object/manifest paths internally, reject arbitrary path/provider/browser/package overrides, preserve failure envelopes/secret-safe diagnostics, and extend exact executable/return audits.
- [x] 2.2 (`pipeline-orchestration`) Replace the temporary HTML delivery-unavailable route with one complete HTML branch adapter for validate/preview/Stages 1-5/build while preserving Change-2 Stage-1 publication/validation restrictions and keeping marker probe before all branch readiness/writes.
- [x] 2.3 (`pipeline-orchestration`, `html-slide-rendering`, `image-generation`, `run-bundle-layout`) Add preview/contact-sheet orchestration plus immutable scope-owned `preview/plans/<hash>.json` with exact `pptmaker-html-review-plan-v1` and atomic `pptmaker-html-preview-manifest-v1` slots carrying exact `canonical-run|migration-preview` scope and canonical nullable reset ID for content/visual plans and visual-review/delivery sheets; include reset ID in review-plan hashing but exclude it from raw bytes/composition/delivery identity; content plans carry full ordered projection, visual plans carry coverage/outstanding/dependency/effective-forced artifact SHAs and approvable completeness; preserve fresh same-scope/reset carried slots/clear stale or cross-scope/reset ones, allow ordinary approval/final review only from canonical matching slots, publish no PPTX, echo no content in failure diagnostics, and never resolve provider prerequisites.
- [x] 2.4 (`pptx-assembly`) Deepen `render_artifacts.mjs` to the exact provider-neutral `final-slide` entry (`slide_id`, kind, producer, `final_slide_fingerprint`, confined path, SHA-256, dimensions/profile) computed by `final_slide_fingerprint_v1`; adapt HTML effective-only and legacy private manifests; make Stage 4 consume plan order without engine/private-schema/glob/position/variant knowledge, require current reset-bound HTML gates/manifests, and publish assembly `schema_version: 2` with current HTML reset ID/delivery digest or null/null legacy; add mutually exclusive canonical `--run-dir` (HTML read-only gate check/internal paths) versus markerless-only explicit legacy artifact mode with HTML rejection and no non-canonical current receipt; preserve fully verified markerless v1 completion/history read compatibility without allowing v1 into HTML/new lineage.
- [x] 2.5 (`pipeline-orchestration`, `pptx-assembly`, `image-generation`, `notes-injection`) Complete HTML Stage 4/5/provider-neutral delivery contact-sheet and reset-bound assembly/notes receipt inputs required by later human final review, with atomic publication but no `html-delivery-review` decision yet; make new Stage 5 require current reset lineage and publish notes `schema_version: 3` with current HTML reset ID/delivery digest over common assembly lineage, retain fully current markerless v2 completion and eligible v2/v3 notes-only input migration, reject v1/v2 or pre-reset v3 for HTML, and prove delete/reorder cannot shift stable-ID notes.
- [x] 2.6 (`pipeline-orchestration`, `commands-reference`, `cli-surface`, `node-specification`) Implement pipeline-first classification and public HTML Local Slide Rebuild, Local Deck Rebuild, Notes-Only Refresh, and scoped dry-run/build paths with smallest stale sets, exact review obligations, provider traps, and markerless legacy behavior unchanged; add only `refresh --kind reset-html-production --confirm-run-version <vN>` for confirmed canonical whole-owner recovery, reject explicitly supplied selectors/dry-run/resolution/provider/base-url/model/style/force/reuse flags before writes without mistaking ordinary parser defaults for user input, reject the confirmation flag on other kinds, and route the whole owner-claim/reset transaction through `resetHtmlProduction` rather than caller-managed deletion.
- [x] 2.7 (`slide-identity-and-ordering`, `pipeline-orchestration`, `run-bundle-management`) Keep structural source publication renderer-free and emit HTML `needs_local_materialization` versus legacy remote `needs_render`; add a later explicit target-local HTML materializer that recomputes producer/fingerprints, verifies prior immutable receipts, copies reusable bytes into target-owned objects/manifests bound to target reset ID null/current only, and makes zero remote/Image2 writes or cross-version path/reset references; treat stable IDs as byte-reuse identity only, never copy/relabel source-version reset epoch, content/visual gates, mirrors, delivery review, or node decisions; publish target reset-null Stage-1/2/3 review-ready artifacts and typed `review_required`, then only after target-version decisions revalidate and continue through delivery contact sheet/Stage-4/5/final review, including reorder-only reuse.
- [x] 2.8 (`run-bundle-management`, `slide-identity-and-ordering`, `cli-surface`, `image-generation`, `html-slide-rendering`) Add 14th `migrate-html preview/apply` with a complete candidate in exact isolated `_scratch/html-migration/projected-run/`, renderer-issued reset-null `migration-preview` context, complete proposal, exact `verified-current|degraded-missing|degraded-stale` old-side mode, and plan binding base/candidate receipts, anticipated target, old evidence, ordered composition/final PNG/contact-sheet SHAs; require explicit mode/hash plus exact active source `migrate-import` apply execution, one complete atomic apply journal whose random token predetermines exact target-reservation/staging basenames and binds execution ID/host/PID/age, reuse the shared internal owner-liveness classifier while retaining migration-specific schema/actions, fresh reset-null canonical hidden-target rerender and exact target `_generated/qa/html_migration.json` publication-only receipt, rechecks before every hidden/write/rename step, exact output equality before no-replace atomic publish, 60000-ms same-host dead automatic recovery, 300000-ms human-confirmed exact-token cross-host recovery, full rerender after owned cleanup when target is absent, and idempotent exact-target completion; forbid journal/execution drift, partial-staging resume, foreign cleanup, scratch/canonical authority crossover, generated-copy, provider/prompt inference, and reset/provider/gate/delivery-review authorization inheritance.

## 3. Run-Bundle Default and Physical Ownership

- [x] 3.1 (`run-bundle-layout`, `run-bundle-management`) Extend layout constants/tree/whitelists/resolvers/checks for HTML page/final/preview immutable objects, preview review plans, current manifests and transient locks/temps, migration scratch, lazy Image2 partitions, pipeline-specific ownership, conformant absence, and rejection of ¨¨candidate/authorization history under HTML/upstream paths.
- [x] 3.2 (`run-bundle-management`) Prepare and test candidate HTML-first/mnemonic generic plus `keynote|pitch|report|training` init seeds, `html_first` controls, empty v2 catalog/README, state/lessons scaffolding, reset ID null by absence with no seeded reset record, exact pending v1-bound HTML mirror fields separate from pending legacy compatibility fields, and absence of style-master/legacy/generated/Image2 paths without activating the default before Task 4.9.
- [x] 3.3 (`run-bundle-management`, `pipeline-orchestration`) Preserve markerless old decks and v1/absent asset catalogs without mutation; make checks/readiness/status pipeline-aware and prove stray opposite-branch generated artifacts cannot satisfy gates or delivery.
- [x] 3.4 (`run-bundle-layout`, `html-slide-rendering`, `node-specification`) Before Task 4.9, benchmark worst-valid and representative fresh/repeated-rebuild pages/decks on the maintained Mac; prove the 64 MiB serialized-page cap, sequential peak memory, exact timeout formula, receipt/current-manifest counts, immutable-object disk growth, and state-first full generated-tree reset/rebuild including byte-identical-output non-resurrection; record measured acceptance evidence and revise artifacts before implementation if a valid page cannot fit or bounded/reset operation fails.

## 4. Workflow, Playbooks, Gates, and State Migration

- [x] 4.1 (`framework-directory-layout`) Atomically migrate `workflow/` to exact final directories `00-setup`, `01-content`, `02-visual-system`, `03-html-production`, README-only unavailable `04-image2-refinement`, and `05-iteration`; remove old active directory aliases and update every active file/link/frontmatter reference.
- [x] 4.2 (`framework-directory-layout`, `framework-charter`) Rewrite/move the roadmap-mandated workflow files/templates/presets: structured content/families in Phase 1, renderer-neutral visual system in Phase 2, local Stages 1-5 delivery in Phase 3, no executable Phase 4, HTML/local iteration in Phase 5, and legacy whole-page guidance only in `reference/legacy-image2-first-maintenance.md`.
- [x] 4.3 (`playbook-execution`) Rewrite `create-deck`, quick preview, content/visual/notes/structure/style iteration, migrate-import, shared classifier, and human-confirmed uncertain-journal plus scope-specific canonical-full-generated/migration-whole-scratch reset interactions for the complete HTML path; make canonical recovery call only the exact reset command, handle `started|resumed|already-complete|deletion_pending` without manual path/state edits, and force new reset-bound reviews even for identical bytes; add isolated `legacy-image2-maintenance`; keep `probe-image-channels` off-path Phase 0; register exactly no modern Image2-refinement execution and validate a checked-in controller/node manifest.
- [x] 4.4 (`node-specification`, `playbook-execution`) Migrate node metadata to lifecycle `0|1|2|3|4|5` and final method-module enums; reject removed names and Change-3 Phase-4 executable nodes; update NODE-SPEC, parser, condition catalog, fixtures, generated controller controls, and consumer semantics together.
- [x] 4.5 (`node-specification`) Implement state schema v3 and an exhaustive checked-in old-playbook/node/module mapping keyed by detected pipeline; expand exact reserved IDs to header/content/visual/delivery/reset and store every HTML record only under existing `nodes[id].by_version["3_versions/vN"]` with internal/mirror `vN`, never a new top-level container; preserve executions/status/evidence/decisions/waits/reserved records/stacks, retain markerless legacy gates, preserve HTML scalar gates only as audit/mirrors without fabricating exact HTML review evidence, interpret pre-reset state as reset ID null without fabricating a reset record, map markerless work only to legacy ownership and HTML work only to new controllers, and return non-writing `replacement_required` for every ambiguous/missing/conflicting case; add the sole exact-receipt/active-source-execution exception `migration_handoff_pending` and one atomic source-migration-complete to target reset-null `migration-target-review` Phase-3 handoff with all target reviews pending; prove idempotency and mismatch non-mutation.
- [x] 4.6 (`node-specification`, `pipeline-orchestration`, `cli-surface`, `playbook-execution`, `run-bundle-layout`, `run-bundle-management`) Add synchronous local-only deep interfaces `inspectHtmlReviewReadiness`, `recoverHtmlGatePublication(runDir,{confirmedOwnerToken})`, `publishHtmlGateDecision`, `publishHtmlDeliveryDecision`, and `resetHtmlProduction(runDir,{confirmedRunVersion})`; keep inspect read-only, reuse the implementation-private owner-liveness classifier while retaining gate/reset-specific schema/actions, publish only exact `pptmaker-html-gate-approval-journal-v1` and `pptmaker-html-gate-review-v1`, automatic recovery same-host-dead-plus-60000ms, exact-token human-confirmed cross-host/uncertain recovery after 300000ms, journal as an exclusive fence against every non-owner state/heal/delivery/mirror write, and version-scoped `html-production-reset` as the state-first `deletion_pending|complete` fence for canonical production; add closed `state --recover-gate-journal`; require expected-state CAS/recheck for every HTML state writer; make the sole allowed third-state recovery an exact reset-pending-only state/mirror projection that proves the gate new state absent and yields the uncommitted journal; make reset atomically rotate a random 64-hex semantic ID plus separate random owner claim, pending only matching HTML mirrors, verify state/metadata CAS, expose bounded owner status without tokens, reject live owners, claim same-host-dead after 60000 ms or human-confirmed cross-host/uncertain after 300000 ms while retaining reset ID, let one CAS claimant safely delete only the canonical owner, complete idempotently, and keep deletion failure visibly fenced; use one cycle-free internal evaluator core with bundle-layout-owned trusted context so synchronous `checkBundle` and the public facade share implementation without caller-injected paths; implement shared 1024-byte secret-safe human-reason normalization plus authoritative reset-bound HTML gates with disjoint global system, canonical component-recipe hashes, code-unit-smallest position-independent reps plus effective/forced evidence, page dependency with chart shape/no ordinary copy, complete no-partial plans/full waiver, exact journal recovery, global/uncovered/covered/page freshness, caller/test seam reuse, and strict mirror isolation (HTML-only `html_*` + run-version fields never mutate/satisfy legacy `content|visual` fields).
- [x] 4.7 (`node-specification`, `cli-surface`, `playbook-execution`, `commands-reference`) Add version-scoped non-gate exact `pptmaker-html-delivery-review-v1` under `html-delivery-review`, bound to current nullable reset ID/contact sheet/delivery digest/assembly-v2/PPTX/notes-v3 and typed `proceed|repair|redirect`, with required durable reason for repair/redirect and none for proceed; add the only public publisher `state --record-delivery-review <decision> [--reason]` with no evidence overrides and exact marker/current-node/reason/stale/journal-or-reset-conflict returns, atomically writing both system evidence and evidence-referenced current-execution node decision; route repair through pipeline-first ownership, create-deck redirect back to checkpoint intake, and iteration redirect through exact `edit-text|edit-visual|edit-notes|restructure-slides|create-deck|stop` target selection with HTML legacy rejection; make state/status/resume/check-gates marker-first, expose HTML review/reset freshness/outstanding recipe/page coverage, deny scalar-only or pre-reset HTML readiness, keep delivery review outside the two-gate check, avoid writes for historical markerless observation, and update next-node/final completion/replacement so every HTML-producing controller re-reviews changed/reset delivery, complete HTML has no refinement debt, and legacy resumes without greenfield restart.
- [x] 4.8 (`framework-charter`, `bootstrap-env-guidance`, `commands-reference`, `run-bundle-layout`, `playbook-execution`) Update root/charter/BOOTSTRAP/COMMANDS/AGENTS/glossary Where Map/classifier/scripts READMEs/reference/templates/OpenSpec context to one HTML-complete narrative, pipeline-specific preview/artifact paths and structural debt (`needs_local_materialization` vs `needs_render`), base local readiness, observable local/remote long jobs, real-artifact visual gates, unavailable modern refinement, explicit migration, and legacy-only optional Image2 troubleshooting.
- [x] 4.9 (`run-bundle-management`, `pipeline-orchestration`, `framework-directory-layout`, `playbook-execution`, `node-specification`) After Tasks 1.1-4.8 focused tests pass, atomically activate HTML-first across `bundle_layout --init`, `ppt_flow init`, generic/all four deck-type templates, final workflow/controller indexes, public orchestration, and state heal; never leave active defaults pointing at an unavailable delivery path.

## 5. Verification and Apply-Ready Closure

- [x] 5.1 (`html-slide-rendering`) Add unit/golden fixtures for all families/min/max/English+Hans/callout/fallback-selected-stale/chart kinds/formatters, all 68 geometry variants, HTML escaping/CSP/self-containment, structural URL checks and inert namespaces, SVG ID/reference isolation/text-font handling, exact font-shard selection, component order, deep-interface request closure, and variant/fingerprint classifications.
- [x] 5.2 (`html-slide-rendering`, `html-render-runtime`) Add current-Mac pinned-browser tests for exact/nonblank/deterministic PNG, custom fonts, overflow/overlays, every forbidden `http|file|blob|ws|custom` route/navigation plus service-worker/CSP defense, timeout/cleanup, locale/line-break stability, and explicit no cross-OS pixel claim.
- [x] 5.3 (`html-slide-rendering`, `pipeline-orchestration`, `pptx-assembly`, `image-generation`, `notes-injection`) Add integration tests for immutable-object/current-manifest scope/reset schemas, raw-byte address enforcement, partial object-temp crash with no final-path corruption, exact-token temp cleanup, empty/invalid lock denial, receipt/atomic/reset drift/crash, concurrent publishers/locks/object collision/prior-reader retention, exact 60000ms same-host dead lock recovery, active/PID-reused/cross-host lock denial, reset-pending pre-lock/precommit denial, canonical full-tree state-first reset/gate-journal ordering/symlink confinement/live-owner denial/60000-ms dead-owner takeover/300000-ms cross-host confirmation/competing-claim CAS/same-ID crash resume/complete+absent-without-authority idempotence/absent-owner-with-current-authority epoch rotation/absent-owner-without-authority ordinary rebuild/deletion-failure fence/byte-identical non-resurrection, migration whole-scratch reset with canonical non-mutation, forced-fallback object-only preparation/preview commit failure/delivery-manifest immutability/dry-run zero writes, target-owned reuse/reorder digest, exact common final-slide/contact-sheet/PPTX lineage, Stage-4 canonical/legacy-mode mutual exclusion and HTML gate/artifact-mode bypass denial, notes v3 completion/rerun plus markerless-v2 compatibility/migration and HTML-v2 rejection, forced-fallback delivery rejection, missing/stale/ambiguous inputs, and exact write sets.
- [x] 5.4 (`pipeline-orchestration`, `node-specification`, `cli-surface`, `playbook-execution`, `run-bundle-management`) Add content/visual completeness/freshness plus exact reset-ID equality, metadata-only/check-gates denial, exact HTML `html_*`+run-version versus legacy `content|visual` mirror non-overwrite/non-cross-satisfaction across versions, synchronous checkBundle/public-facade shared-core and import-cycle regression, rejection of caller-injected contexts/reset/owner IDs, read-only inspect versus explicit gate-journal recovery/concurrency, exact journal and reset 60000/300000-ms recovery age/token-or-version/live-owner matrices, gate-journal and reset-pending non-owner node/heal/render/delivery/metadata write-fence denial, expected-state stale-writer denial, exact reset-yield third-state acceptance, and every other third-SHA rejection, plain-status zero-write/bounded-reset-owner-status and build/check-gates recovery/no-reset-auto-completion matrices, reset command explicit-vs-parser-default flag/version/marker/state/CAS/owner-claim/new/resume/idempotent/owner-loss/failure return audit, pre-reset plan/gate/delivery/node-decision staleness despite identical output or external owner loss, delivery-review digest/receipt/reset staleness and typed routing across create/edit/structure/notes, exact migration-handoff-pending receipt/state match plus atomic source-complete/target-continuation/idempotency/mismatch tests, classifier/state-v3/old-stack/resume/replacement/non-writing markerless status/approval-hash/14-command envelope tests, HTML↔legacy isolation, and zero provider access on every HTML path.
- [x] 5.5 (`slide-identity-and-ordering`, `run-bundle-management`, `html-slide-rendering`) Add structural and migration integration/E2E fixtures for source-only move/delete/insert publication, target-owned zero-remote reuse/rebuild and review-required-before-Stage4 continuation, raw-fence/notes/identity preservation, exact verified/degraded old-side modes with no stale pixels/parity claim, isolated projected-run scope and forged/path-injected context rejection, scratch evidence inability to satisfy canonical gates/build/state, no scratch generated-byte copy, exact canonical hidden-target rerender equality, explicit mode/hash decline/confirm, missing/stale legacy evidence provider traps, target drift, apply-journal concurrency/age/token/active-owner/foreign-path matrices, crash-before-publish owned cleanup plus full rerender, crash-after-publish exact-target idempotence, conflicting-target denial, pending target reviews, and unchanged legacy version.
- [x] 5.6 (`framework-directory-layout`, `playbook-execution`, `run-bundle-management`) Extend full active-link/path/frontmatter/node-enum/controller-manifest/generic-plus-four-deck-type-template/init/bundle self-check tests using checked-in temporary fixtures; assert final six workflow directories, README-only Phase 4, zero old active paths, and no production `deck_*`/`dpt_*` fixture dependency.
- [x] 5.7 (`pipeline-orchestration`, `run-bundle-management`, `framework-charter`) Add a fresh-user E2E from init through structured authoring fixture, content/real visual approval, local HTML preview/build, contact sheet/PPTX/notes/final completion with no Image2 env/style master/provider call/lazy refinement directory/pending node.
- [x] 5.8 (`pipeline-orchestration`, `playbook-execution`) Run markerless legacy create/pilot/build/title/generated-image/structural/resume E2E under the migrated framework and prove current remote prerequisites/review/materialization behavior remains isolated and functional.
- [x] 5.9 Run focused renderer/orchestration/state/migration suites, full `npm test`, relevant `npm run test:e2e`, CLI executable/return audits, dependency/license/lock/`npm audit --omit=dev`, benchmarks, bundle self-check, docs coherence, `git diff --check`, strict change/all OpenSpec validation, and a final scope audit confirming no modern Image2 command/adapter/authorization/candidate/promotion/cleanup or extra workflow root was introduced.

## 6. Requirement Traceability

- [x] 6.1 Audit every proposal capability, delta requirement, design decision, roadmap Change-3 inclusion/completion bullet, and `06-framework-directory-impact.md` file/path rule against tasks/tests; record any intentional deferral by reopening artifacts rather than silently omitting it, then mark this traceability task complete only when the fresh zero-Image2 vertical slice and legacy compatibility completion lines are both fully covered.
