## 1. Repair Main-Spec Structure

- [x] 1.1 Capture the baseline `openspec validate --all` failures and map each invalid requirement index to its exact main-spec heading.
- [x] 1.2 For `framework-charter`, `node-specification`, `playbook-execution`, and `cli-surface`, preserve the full still-valid requirement content and add the missing scenarios.
- [x] 1.3 Replace the `workflow-inspection` placeholder Purpose and complete its two invalid requirements with their existing behavior plus scenarios.
- [x] 1.4 Review the structural-repair main-spec diff and prove no unrelated requirement text or scenarios were dropped.

## 2. Reconcile The Change Contract

- [x] 2.1 Record the interrupted implementation baseline: structurally valid OpenSpec artifacts, contradictory current/compatibility semantics, retired identifiers, stale registries, and failing core/sweep verification.
- [x] 2.2 Complete requirement-level delta coverage for the missing proposal capabilities: `bootstrap-env-guidance`, `environment-check`, `framework-directory-layout`, `framework-script-layout`, `header-lock`, `html-slide-contract`, `html-slide-rendering`, `slide-identity-and-ordering`, `visual-asset-management`, and `visual-config`. The initial file-completeness pass is insufficient: the merged-preview audit found surviving authorization for old state/marker projection, `migrate-import`, old HTML-migration scratch/receipts, and historical whole-page maintenance in adjacent main requirements. Add complete `MODIFIED` or explicit `REMOVED` operations for every such requirement, while recording only owner/reason-bounded non-whole-page exceptions.
- [x] 2.2a Copy and revise the complete `commands-reference` blocks for refresh/structural paths, iteration feedback, title edits, and structural command guidance; remove historical whole-page compatibility while preserving current Image2 refresh, render-mode, and target-materialization behavior.
- [x] 2.2b Copy and revise or remove the complete `pipeline-orchestration` blocks for semantic refresh, Stage-2/pilot/header behavior, structural impact/materialization, current whole-page entry, and migration preview. Do not leave an `ADDED` current route beside an unmodified old compatibility authorization.
- [x] 2.2c Copy and revise the complete `cli-surface` pilot/force, approve/header, status/resume, and build blocks so current Image2 behavior remains while state-absent/historical whole-page behavior cannot route or satisfy evidence. Reconcile its current transition block with the literal `production-mode-transition` state identity.
- [x] 2.2d Copy and revise or remove the complete `playbook-execution` controller-registration, route/gate, exploration, resume-card, recovery, and historical-transition blocks; then audit `image-generation`, `pptx-assembly`, `notes-injection`, `run-bundle-layout`, and every remaining proposal capability by requirement heading. Record the exact delta operation that consumes each retired-route authorization before sync.
- [x] 2.3 Synchronize the main-spec Purpose prose for `bootstrap-env-guidance` and `environment-check` after their delta operations are complete.
- [x] 2.4 Preview the merged main specs and prove they contain one current source/state/Controller/CLI/receipt model without dropping unrelated scenarios. Report every allowed retained legacy/compatibility occurrence by requirement heading; reject any surviving current-route authorization, `migrate-import` identity, or HTML-migration scratch/receipt authority.
- [x] 2.4a Apply the control-policy audit to that merged preview: each current control path uses direct owner facts and one evaluator; a current lossless repair is a `guide`, an actual risk-waiver `confirm` remains reason-bounded, the exact production-mode target-intake commit remains outside that gate taxonomy, and identity/integrity/authorization/recoverability uncertainty is a non-bypassable `hard-stop` with one owner-issued next action. Do not add a control field, command, cache, or Controller route merely to satisfy the audit.

## 3. Close Source, State, And Controller Identity

- [x] 3.1 Make content parsing accept only direct `html-first-v1|whole-page-image2-v1`, report both supported values, and reject missing, malformed, indirect, retired, or unknown markers before branch work.
- [x] 3.2 Make init, layout, and state seed/require the exact source/mode pairs `whole-page-image2-v1 -> image2-only` and `html-first-v1 -> html-only|html-then-image2`; fail closed on mode/source drift.
- [x] 3.3 Put the state read boundary ahead of repair: remove old-schema/node alias migration, marker/topology/controller inference, and the legacy whole-page fallback projection from `state.mjs` and `state-migration-map-v3.json`. Preserve current schema-5 lossless canonicalization behind identity/schema validation and existing gate/reset/transition fences; for unrecoverable current bytes, return the state owner's one bounded typed next action rather than raw YAML instructions or a synthetic continuation.
- [x] 3.3a Make every `replacement_required` CLI-facing branch a valid final `cli_error.mjs` envelope with one existing `next` action rather than sanitizer fallback. For a historically unsupported protocol, reuse `repair_prerequisite` with a default that preserves old bytes and directs fresh explicit `ppt_flow init`; fresh initialization starts normal current authoring and cannot carry state, receipts, approvals, provider authority, generated artifacts, or execution evidence. Do not add a recreate/replacement/migrate action, recovery command, or durable state field.
- [x] 3.4 Remove the legacy-maintenance Controller and aliases; rename `migrate-import` in the playbook file, controller manifest, state records/stack frames, workflow ledger, and state tests to `production-mode-transition`. Re-home `05-iteration/migration/production_mode_transition.mjs` behind the state-owned public interface so Phase 5 does not own a second transition adapter.
- [x] 3.4a Close the existing target-intake decision tuple after the identity rename: the active transition record must validate the explicit target-intake object and its digest together with `transition_confirmation: { kind: "user", decision: "proceed", at }` before apply, recovery, registration, or handoff. Preserve that decision time in the target `checkpoint-intake` decision/evidence, and reject missing, malformed, or mismatched fields before journal, reservation, staging, target, or provider mutation. The named `--confirm-production-mode-transition` operation is an exact plan-hash transaction commit, not a policy waiver: do not add a reason field, `waived` status, continuation, force path, command, or alternate writer.
- [x] 3.5 Delete the retired HTML-migration overlay as one owned slice: candidate-overlay asset lookup, migration palette projection/validation, scratch renderer context, `_scratch/html-migration/` layout allowance, old plan/journal/receipt readers, and their tests. The current transition may use only its declared target-authoring/candidate contract and cannot convert source prompts, pixels, generated artifacts, approvals, or provider authority into target inputs.
- [x] 3.6 Replace Phase-5 `legacy*` whole-page export/probe bridges, old relocation/audit baselines, and legacy-named test ownership with the public `04-image-production/whole-page` adapter; retain current generation, structural reuse, assembly, and notes lineage while deleting old whole-page receipt readers.

## 4. Reconcile CLI And Agent-Facing Guidance

- [x] 4.1 Make `ppt_flow`, `COMMANDS.md`, `cli-surface`, executable/return audits, and baselines agree on exactly 14 top-level commands and only state-owned cross-pipeline transition operations.
- [x] 4.2 Remove `migrate-html`, any top-level transition compatibility grammar, old HTML-migration nodes/fields/scratch/help/routes, and historical maintenance recovery paths. Reconcile `state.mjs` headers/diagnostics, including the invalid `replacement_required` envelope and its old controller-migration hint, `COMMANDS.md`, the change classifier, BOOTSTRAP/workflow references, and direct-module export names together so a user cannot discover a retired route through a diagnostic or import surface.
- [x] 4.3 Update Charter, BOOTSTRAP, workflow, playbooks, references, script docs, and main specs to describe current whole-page Image2 as first-class `image2-only`/`whole-page-image2-v1` work through `create-deck`; describe the transition's exact plan-hash commit as a target-intake user decision, never as the Human-Centered Gate Policy's risk-waiver `confirm`.
- [x] 4.4 Add an owner/reason-bounded retired-token exception registry and reject exact retired identities, current whole-page legacy/compatibility labels, and malformed phrases such as `whole-page whole-page` across active surfaces.

## 5. Rebuild Verification Inputs And Coverage

- [x] 5.1 Convert canonical whole-page unit/integration/E2E fixtures to explicit source markers and durable state; retain markerless/retired values only in named negative tests.
- [x] 5.2 Reconcile Controller manifests, state migration maps, workflow-control ledgers, source-test ownership, executable inventories, CLI return audits, relocation baselines, and command-count assertions with the current owner set.
- [x] 5.3 Replace positive pre-current state-migration tests with byte-preserving negative coverage. For missing/retired markers, pre-current state, topology-only version identity, and old Controller/node/receipt identities, hash `state.yaml` and `history.jsonl` before and after observation and execution entry; prove no state/journal/staging/target/provider write or current-route inference occurs and the diagnostic exposes exactly one bounded owner-issued typed `next` action from the existing producer contract. For historical unsupported input, assert the valid final envelope uses existing `repair_prerequisite`, its default preserves old bytes and directs fresh explicit init, and sanitizer never falls back to `internal/report_internal`; never accept a recreate/replacement/migrate value, action menu, raw-state edit, or recovery command. Separately prove a current-v5 one-to-one defect is repaired only by its owning execution path, while plain observation stays non-writing and an unrecoverable current state returns one owner-issued typed next action without asking the user to edit YAML. For the current cross-pipeline transition, prove `--reason` and `--force` fail with `USAGE` before writes; prove a missing, malformed, or digest-mismatched target-intake/`transition_confirmation` tuple hard-stops before journal, reservation, staging, target, or provider mutation; and prove target handoff retains the original user decision time without creating a waiver or continuation.
- [x] 5.3a Test uncertain-owner recovery as a fact-attestation hard-stop: an old-enough cross-host journal may record only the existing exact `no-active-apply` binding, then the owner must revalidate journal bytes, source/target identity, plan, and CAS before takeover. Journal/plan/identity drift, a live/same-host writer, missing attestation, `--reason`, or `--force` SHALL leave recovery blocked with no takeover/write; no test may characterize this attestation as a waiver or use it to create target-intake approval.
- [x] 5.4 Add focused ownership tests that old HTML migration paths cannot be imported, selected as an asset/palette/renderer context, laid out, or used as transition evidence; verify the current transition target baseline contains only target-owned source/control/intake and no source prompt/pixel/generated/review/provider authority.
- [x] 5.5 Update affected E2E journeys for current whole-page creation, structural versioning, production-mode transition, refinement isolation, assembly, and notes lineage.

## 6. Verify And Prepare Archive

- [x] 6.1 Run syntax/import checks over changed ESM/tests and fail on malformed identifier replacements before broader suites.
- [x] 6.2 Run `openspec validate --all`, `openspec validate realign-specs-with-framework --strict`, and inspect the synchronized main-spec diff.
- [x] 6.3 Run focused source/state/CLI/Controller/receipt/documentation tests, including the byte-preserving historical-state and retired-overlay tests, then `npm test`, `npm run test:sweep`, and the affected E2E journeys.
- [x] 6.4 Confirm the final supported surface contains no retired whole-page identity or compatibility behavior, no Phase-5 migration/legacy bridge import path, and no changes under `deck_*`, `dpt_*`, or `_generated/` production artifacts.
