## 1. Protocol spine (doc) — paste from design D13

- [x] 1.1 Paste AGENT_CONTRACT §11 from D13.1; title/count → 11; keep §1–10
- [x] 1.2 BOOTSTRAP: gate-before-open + pre-key degraded show;「10 条」→「11 条」
- [x] 1.3 Sweep entry docs iron-law count (`CLAUDE.md`, root `CLAUDE.md`, other「10 条」/「10 non-negotiable」)
- [x] 1.4 Patch `create-deck` setup Gate with D13.6 (open + switchPlaybook + approve 双写)
- [x] 1.5 Patch `edit-visual` pilot review with D13.6 open sentence

## 2. Explore playbooks + routing — paste from D13

- [x] 2.1 Create `playbook/iterate-style.md` per D13.3 (+ D11 modes; LOCK = D13.2)
- [x] 2.2 Create `playbook/quick-preview.md` per D13.4
- [x] 2.3 Insert COMMANDS section from D13.5 between 全量创建 and 迭代打磨

## 3. Verify + close plans

- [x] 3.1 Grep stale「exactly six」/「five MD」/「10 条」CONTRACT refs; fix in-scope stragglers
- [x] 3.2 `npm test` green (e2e optional — expect unit only)
- [x] 3.3 `git mv` both plans → `_done/_closed_plans/`; update README indexes (CLS-IDs); Change 2 pointer clear
- [x] 3.4 Confirm Acceptance (incl. Copy Deck fidelity); archive syncs charter + playbook-execution Purpose
