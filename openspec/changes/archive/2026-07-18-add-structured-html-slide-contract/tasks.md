## 1. Source Branch and Document Model

- [x] 1.1 (`content-parsing`, `html-slide-contract`) Define the closed direct-node/no-alias leading `production.pipeline: html-first-v1` marker, single canonical run-context input/no alternate Stage-1 control paths, exact `slide-specifications.md`-only source selection with sibling-backup rejection, and exact case-sensitive/unindented/no-trailing-space `**SLIDE BODY**:` + adjacent `yaml` fence grammar per slide; reject mixed/multiple HTML inputs, missing/near-miss/duplicate bodies, duplicate/indirect `production`, multiple YAML documents/directives/markers/comments/aliases/explicit tags/unquoted timestamp-like scalars/duplicate or unknown keys, and mixed legacy `render`/`RENDER MODE`/`IMAGE PROMPT`/`VISUAL ASSETS` controls with bounded diagnostics without changing legacy defaults or legacy multi-input behavior.
- [x] 1.2 (`html-slide-contract`, `slide-identity-and-ordering`) Extend the shared structured slide-document model with owned-fence ranges and the deep branch adapter seam (`probeProductionMarker`, `validateHtmlFirstRun`, `buildHtmlFirstPlan`); produce one `pptmaker-html-slide-plan-v1` projection containing exact deck-root/framework-relative receipts for source/effective config/geometry/v2 catalogs/assets/all verified font inputs, stable IDs/spoken keys, derived positions, fully resolved theme/font-profile/catalog/geometry, typed records, exact passed preflight shapes, exact fallback/selection evidence, fingerprints, and ordered digest while retaining frontmatter, preamble, notes, epilogue, and non-owned bytes.
- [x] 1.3 (`html-slide-contract`) Pin the existing `yaml` dependency/lock to exact `2.9.0`; implement exact YAML-1.2-core parsing plus a byte-identical parse/serialize no-op and checked-in schema-order canonical serializer options/goldens, including explicit double-quoted scalar style for timestamp-like strings, only for one semantically edited or explicitly canonicalized fence; preserve its label/fence lines, document newline convention, every unrelated slide/Markdown byte, and all legacy sources.
- [x] 1.4 (`pipeline-orchestration`, `content-parsing`, `run-bundle-layout`) Implement one shared read-only leading-frontmatter marker probe used by every public path that can reach readiness or writes; make marked direct validation accept only one canonical `--spec` and reject `--input`, repeated specs, overrides, and unknown flags at the usage layer while preserving those legacy options markerlessly; update `ppt_flow validate` to delegate the canonical source with `--spec` (not the legacy `--input` alias); make `ppt_flow validate`, direct Stage-1 `--validate`, and unified Stage-1 `--dry-run` write-free; make only literal unified `--stage 1` publish by rechecking every byte/realpath receipt and atomically replacing only `slide_plan.json`; reject arbitrary direct Stage-1 HTML output with `html_first_projection_requires_run_dir`; and make direct/public style-master, header approval, pilot/build/all refresh kinds, unified selections containing Stage 2-5 (including dry-run), and legacy structural materialization fail on the marker before readiness, dotenv/provider prerequisites, implicit Stage 1/prompt lookup, artifact/state writes, or any stage with `FAILED` + `html_first_delivery_unavailable` until Change 3, while leaving artifact-only standalone Stage 2-5 interfaces unchanged.

## 2. Layout Families and Content Preflight

- [x] 2.1 (`html-slide-contract`) Create the exact v1 family registry (`hero`, `split`, `cards`, `kpi`, `comparison`, `flow`, `timeline`, `data`, `quote`, `visual-focus`) with direct-root field shapes, calibrated header/block collection/grapheme limits, primary-visual permissions, split discriminated-union rules, exhaustive 68-key enumeration, and immutable concrete `PPTMAKER_FRAMEWORK/scripts/contracts/html-family-geometry-v1.json` `{schema,canvas,variants}` records generated/validated against the normative formulas and bound by exact-file plus canonical-semantic SHA-256 goldens.
- [x] 2.2 (`html-slide-contract`) Add deterministic unknown-field, missing-field, domain, cross-field, family, and geometry validation with bounded field-path diagnostics and no renderer/browser dependency.
- [x] 2.3 (`html-slide-contract`) Implement `Intl.Segmenter("und")` grapheme/collection/explicit-line capacity preflight and exact visible-source Unicode-scalar declared-range coverage by composing Change 1's existing exported `verifyHtmlFontBundle()`, `buildFontInventory()`, and `parseUnicodeRanges()` authorities without modifying or loading its browser runtime; check in the probed cross-major corpus/expected counts and cover headers, every visible block/chart label/callout, decomposed accents, punctuation/currency, numerals, Simplified Chinese, CJK punctuation, LF-only structural-line handling, CR/TAB/C0/NEL/U+2028/U+2029/unpaired-surrogate rejection, deterministic passed evidence, and no-normalization behavior without claiming glyph/pixel proof.
- [x] 2.4 (`visual-config`, `html-slide-contract`) Extend all five existing preset `color_palette.json` files with the exact required-on-branch recursively closed `html_first` schema v1, whole-document strict-JSON/duplicate-key validation, semantic palette path mappings, exact `{families,weight,size,line_height,color}` bundled-font roles, calibrated spacing/component/image-language tokens, and `geometry.registry: html-family-geometry-v1`; add a separate fully resolved HTML-first projection from the existing override-first effective palette while keeping `loadVisualConfig()`'s legacy object deep-equal/fingerprint-compatible, keep family/capacity/geometry records solely in the contract registry, validate every preset, and add no second theme file.

## 3. Visual Contract and Asset Resolution

- [x] 3.1 (`visual-asset-management`, `html-slide-contract`, `run-bundle-layout`) Add exact direct dependencies/lock `saxes@6.0.0`, `fast-png@8.0.0`, and `jpeg-js@0.4.4`; implement optional closed one-document v2 backbone plus sparse version manifests that add/replace by exact ID grammar, validate effective-count/byte caps, POSIX lexical/realpath confinement, regular file, exact extension/signature/intrinsic-dimension media coherence, every asset SHA, sequential CRC/strict/memory-capped full raster decode, and namespace-aware SVG depth/element/attribute/fragment bounds rejecting DTD/PI/CSS/active/foreign/external/data content plus `<text>` in icon contexts; retain origin evidence, reject unregistered same-path shadowing, whitelist/validate the canonical override assets subtree, and leave legacy loader/version/override-first semantics unchanged. Add a worst-case catalog fixture/benchmark covering the 512 MiB aggregate and 20 MiB per-raster limits with an explicit runtime and peak-memory assertion.
- [x] 3.2 (`html-slide-contract`, `visual-asset-management`) Implement the exact `asset|icon-composition|abstract-pattern` fallback mappings/recipes/family matrix, normalized icon-composition box formula/evidence, common asset-evidence shape, and nullable `{asset_id, accepted_for, output_sha256}` selection; reject empty/`none` fallback and require optional-visual families to omit the whole field when unused; after global catalog integrity, validate fallback first, selected path/type/bytes/catalog+selection SHAs second, and applicability last to produce only the specified publishable `fallback|selected|stale` evidence or diagnostic-only `broken`.
- [x] 3.3 (`html-slide-contract`, `visual-config`) Extract one dependency-light canonical-JSON authority from the equivalent existing image-provenance/slide-edit helpers with compatibility re-exports and byte-identical legacy hash tests; implement semantic, per-slide visual-acceptance, deck-level style-reference, and complete renderer-consumed ordered-plan projections including passed preflight but excluding receipts/unstructured planning prose; prove copy edit, reorder, fallback binding/byte/layout, selection-state, visual-semantic, global image-language, renderer-only token, field-classification, and excluded source-locator behavior.
- [x] 3.4 (`run-bundle-layout`, `pipeline-orchestration`) Keep source/manifests in canonical deck-root-relative backbone/version paths, physical slide blocks as the sole order truth, and `_generated/` rebuildable; prove all three general Stage-1 validation routes write nothing, structural preview remains edit-scoped and publishes no plan, invalid/drifted canonical Stage 1 creates no new generated directory and failed receipt recheck preserves the prior plan/directory set, and only canonical unified Stage 1 writes `slide_plan.json`, while test spies/directory diffs prove this change never creates legacy prompts, HTML pages, screenshots, PPTX, or Image2 refinement outputs.

## 4. Identity, Integration, and Guidance

- [x] 4.1 (`slide-identity-and-ordering`, `html-slide-contract`, `visual-asset-management`, `visual-config`) Integrate the HTML branch with existing stable ID/spoken-key/order and structural preview/apply validation; preview SHALL use the current run's effective control tree with only projected canonical-source bytes substituted and no alternate controls, while apply revalidates the copied hidden staged target as a real run context; validate both through the same local branch/family/config/catalog/font core, prove reorder/delete preserves raw fences, identity, notes, history, and per-slide fingerprints, require insert to be a complete locally valid structured block, and keep structural apply remote-free without implicit plan projection.
- [x] 4.2 (`content-parsing`, `html-slide-contract`, `visual-asset-management`, `pipeline-orchestration`) Add one validation-only `PPTMAKER_FRAMEWORK/reference/html-first-v1-authoring.md` plus a narrow reference/scripts index link and checked-in source/catalog fixtures covering the exact fence/canonical validation routes, single-source rule, v1-empty-manifest-to-v2/delete migration, passive CSS-free SVG subset, local fallback, two-pass selection binding, unavailable style-master/header/production routes, and Agent review/brief-concept propagation after material `VISUAL TYPE` or global style changes; distinguish legacy prompt source from structured source without editing the active deck template, advertising browser rendering, or switching production defaults.
- [x] 4.3 (`html-slide-contract`, `content-parsing`, `visual-asset-management`, `visual-config`, `pipeline-orchestration`) Add focused unit/integration/golden tests for near-miss fence/one-document YAML grammar, canonical-source sibling rejection, marked direct-CLI alias/unknown-flag rejection with markerless compatibility, byte-bounded round-trip, exact plan/preflight/resolution envelopes including code-unit receipt sorting and duplicate-pair rejection, validation-only trim semantics and no Unicode normalization, every family min/max/invalid and every geometry variant/file+semantic SHA, bilingual capacity/range coverage and Unicode separators, v2 catalog additions/overrides/integrity/symlink escape/raster header-dimension plus CRC/strict full-decode corruption rejection/SAX entity-decoded URLs/CSS-active-complexity rejection, worst-case catalog runtime/peak-memory bounds, fallback/selection resolution order/layout, all digest projections, one shared marker-probe behavior across public routes, legacy isolation, every earliest unavailable-production guard, arbitrary direct-output rejection, atomic drift failure, failed-run no-directory-creation, and exact write sets.
- [x] 4.4 (`html-slide-contract`, `content-parsing`, `visual-asset-management`, `visual-config`, `run-bundle-layout`, `slide-identity-and-ordering`, `pipeline-orchestration`) Run full regression tests, CLI return audits, dependency/lock/audit checks, and structural E2E for public source/identity/orchestration boundaries; run strict OpenSpec validation and a final scope/diff audit confirming no browser/Playwright load, page HTML/screenshot, PPTX/default workflow, Image2 transport/refinement, workflow migration, run-bundle state migration, or additional direct dependency beyond exact `saxes@6.0.0`, `fast-png@8.0.0`, `jpeg-js@0.4.4`, and exact existing `yaml@2.9.0` was introduced.

## 5. Requirement Traceability

| Task | Normative owner(s) |
|---|---|
| 1.1 | `content-parsing`: canonical source selection/single input; `html-slide-contract`: authoritative marker probe, exact source grammar, and closed YAML |
| 1.2 | `html-slide-contract`: explicit plan/preflight/resolution/receipt envelope; `slide-identity-and-ordering`: stable identity/round-trip interface |
| 1.3 | `html-slide-contract`: exact YAML semantics and structured source round-trip |
| 1.4 | `pipeline-orchestration`: shared marker probe/validation/publication/unavailable routing; `content-parsing`: canonical run-context Stage 1; `run-bundle-layout`: exact write boundary |
| 2.1 | `html-slide-contract`: header/typed-block capacities, closed family union, and sole immutable 68-record geometry registry |
| 2.2 | `html-slide-contract`: closed JSON-like YAML and canonical/total geometry |
| 2.3 | `html-slide-contract`: source capacity and declared font-range preflight |
| 2.4 | `visual-config`: closed preset tokens/projections/registry ID; `html-slide-contract`: sole 68-record geometry authority |
| 3.1 | `visual-asset-management`: v2 catalogs/raster/SAX-SVG integrity and worst-case bound; `run-bundle-layout`: canonical layer paths |
| 3.2 | `html-slide-contract`: local fallback, sub-layout/evidence, and resolution order; `visual-asset-management`: selection integrity |
| 3.3 | `html-slide-contract`: fingerprints/digest/classification; `visual-config`: disjoint projection allowlists |
| 3.4 | `run-bundle-layout`: rebuildable topology; `pipeline-orchestration`: validate/Stage-1/unavailable write sets including failed-run directory preservation |
| 4.1 | `slide-identity-and-ordering`: identity/order/notes/staged publication; `html-slide-contract` + config/assets: full local target validation and raw-fence preservation |
| 4.2 | `content-parsing`: diagnostics/Markdown ownership; `html-slide-contract`: authoring grammar; `visual-asset-management`: v1-to-v2/SVG rules; `pipeline-orchestration`: validation-only/unavailable routes |
| 4.3 | All scenario and coherence obligations above |
| 4.4 | Cross-capability regression, CLI constitution, and proposal/design scope guardrails |
