## REMOVED Requirements

### Requirement: Script layout confines Framed runtime to Page Authority

**Reason**: The capability identity uses the retired Framework term.
**Migration**: Use `harness-script-layout` for the retained runtime boundary.

#### Scenario: Framed runtime ownership is located after the rename

- **WHEN** a maintainer checks the Framed compositor seam
- **THEN** it uses `harness-script-layout`

### Requirement: Direct executables have explicit current ownership

**Reason**: The capability identity uses the retired Framework term.
**Migration**: Use `harness-script-layout` for executable inventory ownership.

#### Scenario: Executable ownership is located after the rename

- **WHEN** architecture validation checks executable inventory
- **THEN** the active Harness script capability supplies the contract

### Requirement: TARGET script layout enforces sibling adapters and shared semantic boundaries

**Reason**: The capability identity uses the retired Framework term.
**Migration**: Use `harness-script-layout` for target adapter boundaries.

#### Scenario: Target adapter ownership is located after the rename

- **WHEN** a maintainer checks Framed, Pure, and Delivery boundaries
- **THEN** it uses the active Harness script capability
