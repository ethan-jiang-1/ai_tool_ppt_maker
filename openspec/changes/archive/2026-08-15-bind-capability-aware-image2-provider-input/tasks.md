## 1. Capability Source And Exact Budget Evaluator

- [x] 1.1 [run-bundle-layout/run-bundle-management] Add the canonical `image2-provider-profile.yaml` backbone/override constants, visual-style whitelist entries, tree/Where-Map projection, neutral pending init seed, and ordinary override copy behavior. Done when new temporary Bundles contain only the non-authorizing pending template, existing temporary Bundles without the file remain byte-preserved at layout validation, and no init/new-version operation creates a resolved profile or lifecycle record.
- [x] 1.2 [run-bundle-management/harness-script-layout] Implement one confined override-first profile resolver/parser at a registered shared Image2 seam for the exact pending and confirmed YAML shapes, including direct-YAML restrictions, exact two-operation vocabulary, positive arbitrary limits, closed units, path-free canonical digest, and selected-invalid-override hard-stop. Done when missing/pending/mixed/unknown/extra/symlink/escape/unreadable/malformed cases return the one bounded source repair without backbone fallback, inference, mutation, partial binding, or provider work, and repaired input passes the same checkpoint.
- [x] 1.3 [image-generation/style-master-generation/harness-script-layout] Implement one import-safe runtime profile-ID grammar helper and one pure final-prompt budget evaluator for `utf8-bytes`, `utf16-code-units`, and `unicode-code-points`, retaining the independent 32,768 UTF-8-byte safety ceiling. Done when ASCII, CJK, BMP/non-BMP emoji, exact-boundary/one-over, 4,000, 16,000, and 12,347 fixtures prove ordinary data-driven `measured <= limit` behavior with no truncation, token estimate, numeric special branch, adapter-local evaluator, or pre-install YAML import.
- [x] 1.4 [focused tests] Add resolver, layout/init/new-version, identifier, and evaluator unit controls using OS-temporary synthetic Bundles only. Done when every authority/confinement failure occurs before derived/lifecycle writes, diagnostics contain no source prose/secret/base URL, and no `deck_*`, `dpt_*`, remote provider, or hand-edited `_generated/` data participates.

## 2. Runtime Identity And Environment Readiness

- [x] 2.1 [environment-check] Add `IMAGE2_PROVIDER_PROFILE_ID` to raw-generation readiness through the zero-dependency identifier helper; keep base and Framed-local checks profile-free and keep direct pre-install env-check limited to presence/syntax. Done when direct startup still runs without `node_modules`, imports neither YAML nor a production adapter/provider, and never locates a Deck or claims source/profile equality.
- [x] 2.2 [environment-check/run-bundle-management] Make installed exact-run doctor resolve the owning confirmed profile and exact-match its `profile_id`, while preserving API-key/base-URL checks and connectivity-only smoke/probe meaning. Done when missing/malformed/mismatched identity blocks live diagnosis before every POST with one environment/profile repair and a valid match does not claim prompt fit, remote route verification, authorization, or media compatibility.
- [x] 2.3 [shared Image2 runtime] Extend credential/provider initialization to return and validate the non-secret runtime selector separately from credentials, without persisting the ID in State or inferring it from key/base URL/model. Done when authorization can match the selector without requiring credentials or network access and generate can reuse the existing credential precedence only after profile/budget preflight.
- [x] 2.4 [environment tests] Add focused direct env-check, installed doctor, JSON/human output, smoke/probe short-circuit, scoped dotenv, and secret-safety controls. Done when all readiness modes preserve their existing output contracts/submission counts and a repaired runtime ID returns to the same exact-run checkpoint without source or lifecycle mutation.

## 3. Style Master Capability Binding

- [x] 3.1 [style-master-generation] Replace the process-global fixed candidate generation profile with one canonical profile built from the resolved `style-master-text-generation` operation, including full profile digest, endpoint/route/model/operation, and prompt budget. Refactor validators so immutable historical records prove their own digest chains while current admission receives the freshly expected dynamic profile. Done when changing any confirmed capability fact changes `candidate_generation_profile_sha256` throughout plan/grant/request/attempt/provenance/selection binding without adding State or duplicate record fields.
- [x] 3.2 [style-master-generation] Remove the former fixed 4,000-byte brief branch and admit the final exact Style Master prompt through the shared safety/capability evaluator during provider-free plan publication. Done when authored intent and semantic summary remain exact, arbitrary profile limits behave data-first, and overflow writes no plan, grant, attempt, provider request, or remote `known_failure`.
- [x] 3.3 [style-master-generation] Re-resolve compiler/profile/budget facts and exact-match `IMAGE2_PROVIDER_PROFILE_ID` before authorization grant creation/replay and again before a new claim; retain credential initialization after the existing claimed CAS and submission recording before transport. Done when profile/runtime drift cannot grant or claim work, credential failure remains the existing resumable pre-submit claim, and submitted uncertainty/abandonment semantics are unchanged.
- [x] 3.4 [Style Master tests] Cover valid dynamic profiles, arbitrary limits/count units, profile/runtime mismatch, plan/selection drift, exact replay, credential failure ordering, former fixed-profile plans, and an unresolved submitted attempt. Done when former work remains immutable audit evidence, safe work uses the existing successor path, unresolved cost retains abandonment precedence, and repair reruns the same checkpoint with zero implicit retry/fallback.

## 4. Page Image Compact Provider Compilers

- [x] 4.1 [image-generation] Build the canonical Page Image generation profile from the resolved `page-image-reference-generation` operation and bind it through existing `provider_profile_sha256`, item generation-profile digest, authorization-scope, provider request, attempt, and invalidation closures. Done when Pure and Framed consume the same operation facts without sharing a compiler shape, model no longer comes from a Harness alias constant, and no capability State/authorization duplicate is added.
- [x] 4.2 [image-generation/Pure] Change Pure compilation to the exact semantic-only top-level shape with `page_presentation: { profile }`, retaining instruction, exact design-system/rendered content/visual/identity semantics and removing generation profile, page class/profile ID, binding digest, provenance, path/origin, and all other lineage-only prompt fields. Done when the raw contract and plan retain full lineage, compact bytes directly become `compiled_provider_input.utf8`, and exact-shape negative tests reject every structured leak before publication.
- [x] 4.3 [image-generation/Framed] Change Framed compilation and its private exact validator to the independent semantic-only shape with byte-exact reservation instruction, design system, rendered content, subject restrictions, normalized protected composition, and visual/identity semantics. Done when generation profile/provenance/local header/Pure presentation facts are absent, protected composition remains exact, and weakened instruction or cross-workflow leakage hard-stops before publication.
- [x] 4.4 [image-generation] Apply the shared final-prompt safety/capability evaluator to both adapter compilers and bind `{ operation, limit, unit, measured }` into local request inspection/derived `image2-request` projections only. Done when exact-boundary/one-over and multilingual fixtures measure final canonical bytes, derived edits cannot affect authority, and overflow never truncates/summarizes/reroutes or becomes a remote failure.
- [x] 4.5 [Pure/Framed tests] Extend Page Image Core, raw-contract, provider-input-binding, ordinary/progressive plan, Page Design System, identity role-clause, presentation, inspection, and invalidation tests for compact exact bytes. Done when all provider-useful semantics remain exact, every digest/path/provenance exclusion is proven, Framed and Pure stay isolated, and source/profile edits take the existing Generated Image Rebuild path without historical mutation.

## 5. Authorization, Transport, And Cutover

- [x] 5.1 [image-generation] Rebuild current profile/compiler/budget facts and exact-match the non-secret runtime profile ID before Pilot/Expansion grant creation or replay; keep authorization provider-free and credential-free. Done when profile/source/runtime drift short-circuits before grant/attempt/provider initialization with one nearest repair and a repaired source/environment returns to the same authorize checkpoint.
- [x] 5.2 [image-generation] Reuse the progressive owner's existing pre-attempt `preflight` seam to repeat exact source/profile/compiler/budget/runtime checks and resolve credential readiness before a claim; keep submitted-attempt, idempotency, known-failure, and uncertainty ordering unchanged. Done when every deterministic precondition failure writes no attempt/request, while a post-submission ambiguity still uses only exact reconciliation.
- [x] 5.3 [transport] Keep Page Image and Style Master submitters opaque: send the bound operation model and byte-for-byte authorized compact prompt while attaching Style Master/identity reference media separately. Done when fake-fetch and architecture controls prove submit never reads profile YAML, raw contracts, inspection, or source files to derive prompt bytes and never injects local generation metadata.
- [x] 5.4 [compiler/profile cutover] Make current adapter/Style Master expected-plan comparison classify former metadata-heavy/fixed-profile unsubmitted work through existing stale successor/rebuild actions without adding an old prompt/profile parser, dual writer, patcher, or migration. Preserve the existing narrow progressive historical bridge only for its already declared reconciliation/head-lineage scope. Done when former plans cannot create a batch/grant/claim/submit/review/finalization, predecessor audit hashes remain intact, and no former materialization/review is reused.
- [x] 5.5 [cutover integration tests] Seed synthetic former Pure, Framed, Style Master, and progressive records covering no work, accepted historical evidence, runtime/profile drift, malformed unrelated corruption, and unresolved submitted attempts. Done when valid cutover takes only fresh-plan/successor or exact reconciliation/abandonment routes, corruption remains an integrity hard-stop, all historical bytes remain unchanged, and no implicit resubmit/provider call occurs.
- [x] 5.6 [mock E2E] Extend the public-CLI mock workflow for Style Master plus at least one Pure and one Framed Page Image request under confirmed profiles. Done when the mock server observes the selected operation model, exact authorized prompt bytes, separate references, no metadata leakage, one submission per eligible invocation, and zero calls for every profile/budget/runtime preflight failure.

## 6. Schemas, Diagnostics, And Guidance

- [x] 6.1 [production-schema-conformance] Update `serialization-contracts.yaml` and affected layout-config, Style Master/Page Image generation-profile, plan, `image2-request`, and provider-request stage definitions for the source binding, closed operations/units, dynamic digest closure, compact workflow shapes, measurement projection, runtime selector boundary, and opaque transport. Done when declarations contain no credential/base URL, State capability ledger, second prompt authority, or numeric limit kind.
- [x] 6.2 [production-schema-conformance/harness-script-layout] Extend pure/static conformance and architecture tests with synthetic valid chains plus planted unknown unit/operation, non-positive/special-case limit, secret leakage, prompt metadata leakage, transport projection, fallback profile, duplicate resolver/evaluator, pre-install YAML import, and former-plan submission violations. Done when each planted violation is detected at its exact scope, 4,000/16,000/12,347 all use one contract, and restoring fixtures passes without runtime/provider work.
- [x] 6.3 [cli-surface] Route profile-source, runtime-identity, budget-overflow, and stale-plan failures through existing secret-safe CLI envelope fields and action vocabulary only. Done when source failures use `source_validation` / `edit_source`, runtime mismatch uses `environment` / `repair_environment`, overflow exposes at most safe profile/operation/measurement facts without a speculative locator, and stale work retains the existing lifecycle-owner successor/rebuild or unresolved-attempt recovery; diagnostics never expose prompt/source prose, secret/base URL, provider body, stack, waiver, retry, or fallback, and the producer schema is not expanded.
- [x] 6.4 [harness-charter/playbook-execution] Update active Run Bundle tree/reference and applicable existing Controller guidance so the Agent obtains one Deck Author capability declaration within visual-system source work, edits only the owning source/environment, reruns the same checkpoint, and treats smoke as connectivity-only. Done when guidance asks no repeated Task Mandate/cost confirmation, creates no new Controller node/manifest entry or second controller/state authority, and distinguishes provider profile, Page Design System, presentation profiles, credentials, inspection, and Complete Page Review.

## 7. Validation, Spec Sync, And Handoff

- [x] 7.1 Run focused resolver/layout, count-unit, environment, Style Master, Pure, Framed, provider-binding, progressive owner, transport, derived-data, invalidation, CLI diagnostic, schema-conformance, architecture, and mock E2E tests; fix only regressions attributable to this change. Done when positive and negative controls prove the direct source -> exact compiler -> bound plan/grant/attempt -> opaque transport loop with no real provider calls.
- [x] 7.2 Run `npm test`, then `npm run test:sweep`, `git diff --check`, `openspec validate bind-capability-aware-image2-provider-input --strict`, and `openspec validate --all --strict`; resolve all change-caused failures and record unrelated pre-existing failures without broadening scope.
- [ ] 7.3 With the exact `deck_ai_sdlc_bpm_keynote/3_versions/v8` path explicitly authorized for read-only Run Bundle validation, run provider-free current compilation after the Harness implementation and record all 25 final prompts' Unicode-code-point, UTF-16-unit, and UTF-8-byte measurements against the selected confirmed profile. Done when no production source/state/generated bytes are edited, no provider is initialized, and any over-budget page is reported as an owner profile/content decision rather than automatically changed. If that exact Run Bundle access is not authorized, leave this operational verification for the Deck Agent and do not use it as a Harness fixture.
- [x] 7.4 Sync all ten delta specs to their main capability specs only after implementation/tests pass, rerun strict validation, and review the final active surface for hardcoded provider limits, duplicate prompt/profile authority, duplicate resolver/evaluator seams, migration readers, retries/fallbacks, new State or expanded CLI contracts, Controller-node/manifest drift, or production-data edits. Done when the active change is ready for the separate OpenSpec archive workflow and normal Deck Controller handoff; do not archive merely because code was written.
