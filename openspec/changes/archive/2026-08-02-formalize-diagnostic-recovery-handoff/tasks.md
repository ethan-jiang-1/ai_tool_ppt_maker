## 1. Lock The Consumer Contract With Focused Tests

- [x] 1.1 Add a fail-first diagnostic-recovery contract suite that asserts one canonical Agent Contract handoff, exactly four ordered novice-facing parts, bounded producer-derived facts, explicit no-human-action wording for mechanical work, and no invented retry, shell command, authorization, persistence, or second recovery policy.
- [x] 1.2 Extend routing/document contract coverage for the fixed precedence: a valid current envelope wins; otherwise a startable known exact run uses state --json; otherwise a startable unlocated request uses the supported locator; pre-install or unavailable-main-entry work alone uses direct env-check. Prove no deck scanning or raw stderr recovery policy is introduced.
- [x] 1.3 Extend the temporary-bundle generated-guide coverage to prove a located runtime Agent receives the same four-part diagnostic boundary, final-envelope-only rule, argument preservation, and human stop without locator or pre-install claims.

## 2. Establish The Canonical Handoff And Its Presentations

- [x] 2.1 Add the non-persistent Diagnostic Recovery Handoff to AGENT_CONTRACT.md with the four-part translation contract, the literal recovery decision tree, producer-next preservation, guide/confirm/hard-stop behavior, and explicit direct-env-check limits.
- [x] 2.2 Replace any overlapping create-deck diagnostic prose with a concise reference to the canonical handoff so Controller guidance retains producer ownership without maintaining a second diagnostic routing table.
- [x] 2.3 Update COMMANDS.md so the novice "I am stuck" result promises the bounded four-part outcome without exposing protocol mechanics, and make its Agent-facing reference link to the canonical handoff rather than restating precedence.
- [x] 2.4 Update NODE-SPEC.md and the generated deck-guide source/template so MD consumers and newly initialized located bundles preserve the producer control boundary and the four-part explanation. Do not edit any production deck_* bundle, state, receipt, grant, attempt, history, or generated artifact.

## 3. Verify Without Widening The Control Surface

- [x] 3.1 Run the new focused diagnostic-recovery, intent-route/document, and generated-guide contract tests; verify valid mechanical, human-required, invalid-envelope, known-run, locator, and pre-install branches remain bounded and provider-free.
- [x] 3.2 Run npm test as the core tier. Do not run mock or real E2E unless a changed public executable journey independently warrants it; real-provider work remains unauthorized.
- [x] 3.3 Run openspec validate formalize-diagnostic-recovery-handoff --strict, openspec validate --all --strict, and git diff --check. Confirm all delta requirements have an observable documentation/contract-test result and that cli-surface grammar/schema and durable authority files remain unchanged.
