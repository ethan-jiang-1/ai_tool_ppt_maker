## 1. Native Media Contract

- [x] 1.1 Add one shared Page Authority media-contract owner for the established
  `2000x1125` request parameter, exact `2048x1136` native provider PNG, and
  workflow-specific final PNG checks. Register its public shared interface and
  source-test ownership. Capability: `image-generation`.
- [x] 1.2 Update the selected Image2 raw profile, safe request inspection,
  provider ingress validator, direct CLI bounded media facts, and progressive
  raw-owner failure facts to use the shared native response contract without
  changing the proven request parameter. Capabilities: `image-generation`,
  `cli-surface`.
- [x] 1.3 Update Framed underlay validation to accept verified native raw bytes
  while retaining its existing local final composition contract. Capability:
  `image-production`.

## 2. Finalization And Delivery

- [x] 2.1 Require Pure final publication to preserve accepted native raw bytes
  exactly, and validate target final bytes against the selected workflow's
  media contract before manifest/delivery publication. Capability:
  `image-production`.
- [x] 2.2 Update bounded CURRENT Page Authority assembly validation to accept
  the verified native Pure media contract and preserve full-slide placement,
  source bytes, ordinal footer, and lineage. Capability: `pptx-assembly`.

## 3. Regression Coverage

- [x] 3.1 Add focused Image2/runtime/raw-owner and direct-process tests for a
  successful `2048x1136` result, non-native rejection, secret-safe bounded
  facts, and the distinct request-versus-response inspection projection.
  Capabilities: `image-generation`, `cli-surface`.
- [x] 3.2 Add Pure/Framed finalization, delivery, and PPTX tests proving native
  Pure bytes and hashes are preserved, malformed or wrong workflow media stops
  before publication, and native image bytes are embedded without a raster
  rewrite. Capabilities: `image-production`, `pptx-assembly`.

## 4. Verification And v7 Recovery

- [x] 4.1 Run focused regression suites, `npm test`, strict validation for this
  and the two active recovery changes, and `git diff --check`.
  Verified 2026-08-03: focused Pure/Framed/delivery/raw-owner/media-contract
  suites, process diagnostics 12/12, non-process sweep 362/362, core verifier,
  all three strict validations, and whitespace check passed.
- [x] 4.2 Rebuild the user-specified v7 plan only through its current owner,
  then continue its owner-issued Pilot, authorization, generation, review, and
  delivery actions. Do not hand-edit `_generated/`, state, receipts, grants,
  or historical attempts.
  Status 2026-08-03: the zero-remote owner replan completed with Pure plan
  `a905c2ffd916b25f308ed6764c43eecdbbdda568cd14f7374e4154c6a15a0f5a`;
  its 25 items are all `unsubmitted`, its maximum submissions is 25, and its
  local request-inspection projection is
  `a2550f077deb2e572cd07288ba059c8faf4af4771cdd91d739d83cd1143009a3`.
  This replaces the obsolete `6dde...` profile scope without rewriting any
  historical evidence. The owner then created batch
  `271616afcb1b9889ec251d02b75cc49c75f5dd5c4f41e63c702082db9792f9b2`
  for `InfoRev`, `NewPart`, and `TwinChn` (maximum 3 submissions), then granted
  `e70588fb8f7ba6ddc94f1da087f3add32ac08e6faed8f31eab1ebb6aef7f2ccd`.
  `InfoRev` then succeeded and materialized under the native contract (attempt
  `8c16f6a6bba311810db68014f0bab190bd110f70c46abcbed99e05eafaa6bd62`,
  provenance `247dfedcb3248677d2ed7244959941e7effd4362ab50a7ca2438fbf83e450880`).
  `NewPart`, the former false-dimension rejection case, then also succeeded
  (attempt `748fce19da19b2e686fdd82be60c2b6e30b5d1052484f3d6417ea0d0c435984b`,
  provenance `8e12043b91c7ddb7ab30b3210d253d0ad405d13af04f0b51881d188f96876ff9`).
  `TwinChn` also succeeded and materialized (attempt
  `77ef17704c9805e3f460e06d86146f7d94bd85ce38deb475b0283eadbca3abbe`,
  provenance `ec54496f2678b64d41da91aad3dac4bf5fa3814045c6da69bef92001b2875439`).
  The Pilot is complete: progress is 3/25 materialized with no `known_failure`
  or `unknown`. Pilot review evidence
  `9635c5e0032eb8fd85a9eb2f8bd348ad881a20d5b2cd32b5a49430df6c56fd34` is current;
  the owner now requires `accept_progressive_pilot` before expansion. A
  visual inspection of the owner-published `01_InfoRev`, `05_NewPart`, and
  `17_TwinChn` PNGs found complete canvases, readable Chinese display text,
  intact diagram/detail structure, and no crop or blank-media defect. The
  Pilot was accepted with `proceed` as
  `8f0556716cea04adbbc0e045de095aae83f2ae97aa6e360f875cd22e0a7f795e`;
  the owner then created expansion batch
  `8b17d792f3d9d1762b0f4f594a77847d23e660941d4d230fb5bee78a7f6c5ef2`
  for the remaining 22 formal IDs, with maximum 22 submissions. The current
  owner issued grant
  `702b355374da8412789ee2ae3941d2d93ce060f70f417b81e13abb71df98bf15`
  for that exact batch. The current action is one-at-a-time
  `generate_progressive_raw_item`.
  All 22 expansion items then succeeded; together with the Pilot, v7 is 25/25
  materialized with zero `known_failure` and zero `unknown`. The current owner
  action was `prepare_progressive_raw_review`; complete review evidence
  `f426bd07295b6763ac8fe1d86c5eda7dc7db28abf67166ae5bed0c158b160a69` is now
  current, and the owner required `accept_progressive_raw_review`. Before that
  owner action, the dense raw pages `03_WhyCode`, `19_FourLyr`, `21_MaerAI`, and
  `23_MeasNot` were inspected at their native `2048x1136` resolution: all had
  complete canvases, readable text and diagrams, and no crop, blank-media, or
  obvious distortion defect. The visual conclusion was accepted by owner
  `proceed`: complete review receipt is
  `d7853817fb86d0c7e7c721f1ea1e67911d337894bcedffe96b63877fccb08464` and
  accepted raw evidence is
  `c0bd7393e4c70f061cff3916bcebb10862eb96b44ae225f6532c19eca27d206e`.
  The current owner action is Pure `publish_target_final_manifest`, followed by
  delivery evidence. The official `build` then completed Pure finalization and
  delivery assembly, producing `_generated/page_authority_image2/final/deck.pptx`.
  The delivery receipt records final-manifest digest
  `511cb363a9f61d88d273176bb3dd9910d1bea00759140c147793c6cd8557ae87` and
  the PPTX SHA-256 is
  `4bdc605caa94d4a682ff3318c55d741294c1bc8f1eeebcd9962d313dc8edf375`.
  All 25 Pure final files exactly match accepted raw bytes; all 25 packaged
  PPTX media match those final files in order; 25 notes, ZIP integrity,
  run-bundle layout, and the complete final contact sheet passed review.
- [x] 4.3 Synchronize the Page Authority backlog plan and all active change
  task records after framework verification and every material v7 action;
  record the final closure state before archive.
  Status 2026-08-03: synchronized through the fresh owner plan, Pilot batch,
  its new three-submission grant, all three successful Pilot items, current
  review evidence, visual check, `proceed` acceptance, expansion scope, and
  its 22-submit grant, successful 25/25 raw materialization, and complete
  review evidence plus the four-page native-resolution visual review and formal
  `proceed` acceptance, successful Pure final/delivery assembly, and all final
  receipt/PPTX/visual verification. The backlog now records the final evidence
  and this change is ready to archive.
