## 1. Authorized Trial Scope

- [x] 1.1 Obtain one explicit human Work Request that names a new disposable Framed probe run, caps raw-page provider cost at one to three one-item samples, and explicitly excludes C7 and every existing production target; retain the existing content, Style Master, and page-review decisions as separate owner gates.
- [x] 1.2 Under that Work Request, initialize the named probe run with exactly the authorized count of distinct stable `mnemonic` slide IDs and identical non-identity probe facts; preview and obtain the existing exact narrative-plan confirmation before source publication, without hand-editing `_generated/` or creating a provider attempt.
- [x] 1.3 Use the existing Style Master inspect, plan, review, and promotion lifecycle for the new Framed scope before raw planning. A zero-generated plan is legal only for its own valid local candidate and still needs its existing human review; a missing local candidate stops at the owner action and the raw-page Work Request does not authorize generated Style Master cost or production-candidate reuse.
- [x] 1.4 After the current Style Master selection exists, establish the selected profile, protected composition, and provider-free full raw plan through existing owners without creating a provider attempt.
- [x] 1.5 Create and inspect one exact selected-ID batch/grant whose distinct probe pages and `maximum_submissions` equal the authorized sample count; do not create a one-page retry loop or a second grant for the same selected sample.
- [x] 1.6 If an actual source/configuration, Style Master, protected-composition binding, plan, grant, identity, or sample-capacity precondition fails, use only the existing owner-issued repair path and rerun that checkpoint. Do not induce a failure in the authorized probe solely for this task; prove the no-attempt/no-fallback property through focused existing negative tests.

## 2. Bounded Existing Lifecycle

- [x] 2.1 Use only the existing Task-Mandate, exact multi-item grant, one-item attempt, provenance, and reconciliation owners to submit at most three distinct probe pages for the authorized scope; after a page submits, never reopen, resubmit, or replace it to obtain another sample.
- [x] 2.2 For every completed sample, inspect the existing exact provider-page and production-equivalent composite through Complete Page Review, retaining its existing human `proceed | repair` ownership; neither the Work Request nor any trial observation may make an automatic occupancy/OCR or trial-level acceptance decision.
- [x] 2.3 Prove scope isolation without reading C7 or another production run: show every command, lifecycle locator, and `evidence.md` reference is rooted in the authorized disposable probe, and use the repository change inventory only to confirm this change did not modify another target.

## 3. Secret-Safe Evidence And Outcome

- [x] 3.1 Create this change's `evidence.md` from the design template, using only stable identifiers, current Style Master selection and page-specific request/composition digests, exact batch/grant/attempt locators, submitted count, observed transport field names, and `observed` / `not_observed` / `indeterminate` values for the four named composition phenomena; exclude credentials, prompt prose, provider bodies, and raster bytes.
- [x] 3.2 State the result strictly as bounded empirical evidence for the sampled current request and transport; do not claim page acceptance, a general provider guarantee, a native primitive, or authority for a transport extension.
- [x] 3.3 If evidence and primary provider documentation identify a native constraint primitive, capture the verified contract boundary as a follow-up-change input only; otherwise retain the current bounded-best-effort contract without a compatibility or fallback path.

## 4. Focused Proof And Validation

- [x] 4.1 Run the focused existing lifecycle proof: `npx vitest run tests/01-content/test_page_image_source.mjs tests/03-framed-image/test_framed_plan_lifecycle.mjs tests/03-framed-image/test_framed_workflow.mjs tests/03-framed-image/test_framed_review_contribution.mjs tests/shared/image2/test_style_master_plan.mjs tests/shared/image2/test_progressive_raw_owner.mjs tests/shared/image2/test_complete_page_review.mjs tests/contracts/test_cli_surface.mjs tests_e2e/shared/workflow/test_mock_narrative_authoring_journey.mjs`. Confirm the tests cover current source identity, provider-free Style Master and raw planning, exact grant/one-item-at-a-time discipline, owner-issued negative recovery, and Complete Page Review; do not treat mocked output as empirical provider evidence.
- [x] 4.2 Run `npm test`, `openspec validate validate-framed-provider-constraint --strict`, `openspec validate --all --strict`, `node ppt_maker_harness/scripts/shared/run-bundle/bundle_layout.mjs --check <authorized-probe-run>/3_versions/v1`, and `git diff --check`; record any external provider result and any non-change baseline failure separately in `evidence.md`.

## Execution Log

- 2026-08-12: Task 1.3 is in progress. The disposable probe's canonical Style Master intent was added, and the existing owner published one Framed candidate plan for `candidate-001` with `maximum_submissions: 1`.
- 2026-08-12: The separately authorized Style Master candidate submission succeeded through the existing one-item grant. `candidate-001` is the only submitted candidate; no page-raw submission has occurred.
- 2026-08-12: `candidate-001` was reviewed from verified real bytes and promoted through the existing `proceed` lifecycle. The current Framed Style Master selection is bound to this disposable probe only; raw planning may now begin.
- 2026-08-12: The existing Framed raw-plan owner published the current two-page plan for `BodyMap` and `BodyRun` with one shared protected-composition binding and no raw provider attempt.
- 2026-08-12: One exact current pilot batch and its only raw provider grant now bind `BodyMap` + `BodyRun`, with `maximum_submissions: 2`. No one-page retry loop or second grant was created.
- 2026-08-12: Both permitted raw samples are materialized. `BodyMap` was reconciled from its exact submitted attempt without resubmission; `BodyRun` is independently materialized. The grant has no remaining capacity and no third page was submitted.
- 2026-08-12: Complete Page Review inspected both provider pages and Framed composites. The existing human decision is `repair`; its bounded negative observations are recorded in `evidence.md`, without automated occupancy/OCR or trial-level acceptance.
- 2026-08-12: No provider-native constraint primitive was verified. Current transport observations and unavailable primary-document retrieval leave the bounded-best-effort contract unchanged; no compatibility or fallback path was introduced.
- 2026-08-12: The actual submitted-outcome interruption was resolved only through the raw owner's exact reconciliation action and did not resubmit `BodyMap`. Focused lifecycle suites passed (8 files, 129 tests); the narrative E2E passed separately through `vitest.e2e.config.mjs` (1 test). These mocks prove lifecycle controls, not provider behavior.
- 2026-08-12: `npm test`, change-strict OpenSpec validation, all-strict OpenSpec validation (29/29), and `git diff --check` passed. The required bundle layout check reported only missing legacy metadata gates; that non-change baseline is recorded in `evidence.md` and was not concealed by a source or state edit.
