## 1. Provider Outcome Boundary

- [x] 1.1 Update the selected Page Authority adapter to classify a received
  non-success HTTP response and a fully read invalid success envelope as
  bounded `known_failure` facts, while retaining `unknown` for fetch/body-read
  interruption and preserving the existing PNG media validator.
- [x] 1.2 Extend the shared progressive raw owner to project only safe
  response-failure facts in its existing terminal outcome without persisting a
  response body, new schema field, raw bytes, or a retry route.

## 2. Partial Pilot Recovery

- [x] 2.1 Add one shared direct current-coverage check for terminal partial
  Pilot samples and use it in lifecycle next-action selection to route missing
  coverage with paid debt to existing successor Pilot planning.
- [x] 2.2 Make Pilot-review fail through the same owner-issued successor action
  before it can publish evidence or a decision; retain terminal predecessor and
  new exact-authorization requirements.

## 3. Regression Coverage

- [x] 3.1 Add focused adapter and CLI tests for HTTP/non-JSON known failures,
  transport/body-read unknown outcomes, bounded output, and sentinel response
  secrecy.
- [x] 3.2 Add raw-owner and workflow-inspection coverage for terminal partial
  Pilot missing coverage, legal successor batch planning, no old-grant reopen,
  and no evidence/state mutation during inspection.

## 4. Verification And Closeout

- [x] 4.1 Run focused Page Authority tests, applicable mock workflow tests,
  full repository verification, strict OpenSpec validation, and diff checks.
- [x] 4.2 Sync accepted delta specs and archive this recovery change after its
  tests pass, then resume the designated v7 run only through its current
  owner-issued successor scope.
