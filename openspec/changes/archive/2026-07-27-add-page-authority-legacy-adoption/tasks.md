## 1. Direct Observation And Candidate Contract

- [x] 1.1 Add the state-owned `inspectLegacyProtocol` evaluator with the four closed classifications, bounded observation digest, and no-write/no-provider guarantees.
- [x] 1.2 Extend the existing production-mode transition candidate owner with an adoption-only Page Authority source validator and exact per-slide adoption matrix validator.
- [x] 1.3 Bind recognized-legacy facts, candidate source/default, target intake, and each matrix row into an immutable adoption preview hash; reject drift, partial pairs, external paths, and legacy material.

## 2. Transaction And Target Publication

- [x] 2.1 Extend the single transition state record, confirmation, journal, receipt, and recovery validation with a discriminated `legacy-adoption` plan kind while preserving existing generic transition behavior.
- [x] 2.2 Publish a clean `image2-page-authority` target at `source_epoch: 1`, hand it off to Page Authority raw authorization, and prove that no legacy execution, provider, raw/review/final, PPTX/notes, or delivery evidence crosses the boundary.
- [x] 2.3 Revalidate observer, matrix, candidate, source/state, target collision, and recovery facts at apply/takeover so every stale or ambiguous condition returns the existing one-owner recovery path.

## 3. Controller And CLI Surface

- [x] 3.1 Add closed state CLI observe/prepare/preview/confirm/apply/recovery forms for legacy adoption, prohibit Page Authority as a generic transition target, and emit `LEGACY_PROTOCOL_ADOPTION_REQUIRED` for fenced legacy production commands.
- [x] 3.2 Make resolver, workflow inspection, and pipeline orchestration consume the direct observer before normal legacy dispatch while preserving current Page Authority repair and lifecycle routes.
- [x] 3.3 Update the controller/playbook and COMMANDS guidance for human-authored matrix intake, target confirmation, clean post-publication raw debt, and the separation between adoption and later Image2 quality review.

## 4. Focused Verification

- [x] 4.1 Add focused unit and contract coverage for all observer outcomes, candidate/matrix rejection, exact hash confirmation, CAS/source drift, target cleanliness, and uncertain recovery.
- [x] 4.2 Add one deterministic provider-free adoption E2E journey with a provider-call counter, and expose its narrow test entrypoint without a real Image2 request or aesthetic assertion.
- [x] 4.3 Run the focused adoption suite, fast core regression suite, selected provider-free E2E journey, and `openspec validate --strict`; record that broad E2E remains explicit release-bound work.
