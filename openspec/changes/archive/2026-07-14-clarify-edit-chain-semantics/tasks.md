## 1. Establish canonical terminology and compatibility boundaries

- [x] 1.1 Update the exact compatibility registries — `openspec/config.yaml`, `PPTMAKER_FRAMEWORK/charter/WORKFLOW.md`, `reference/glossary.md`, and `scripts/change-classifier.md` — to define the English canonical names, optional Chinese explanations, and former aliases in one explicit mapping. Define Header Text & Style Refresh as KICKER/TITLE/SUBTITLE plus Stage-3-owned font/color/position/spacing changes with an unchanged raw-image contract (Stage 1 then 3,4,5); Generated Image Rebuild as logical Stage 1 + forced selected Stage 2 + review + 3,4,5/reuse; Notes-Only Refresh as Stage 5; Structural Versioning Path as `--new-version` followed by affected-slide refresh. [capabilities: `framework-charter`, `pipeline-orchestration`; no runtime command or generated-artifact edit]
- [x] 1.2 Update root `AGENTS.md`, `PPTMAKER_FRAMEWORK/AGENTS.md`, and `charter/AGENT_CONTRACT.md` to use canonical English path names without bare A/B/C/Structural aliases and to preserve render-aware title routing. Correct any claim that raw `--only` automatically forces regeneration. [capabilities: `framework-charter`, `playbook-execution`, `pipeline-orchestration`; Generated Image Rebuild examples use `--only <ids> --force-images`; no execution]
- [x] 1.3 Record the controlled policy in `PPTMAKER_FRAMEWORK/README.md`, `BOOTSTRAP.md`, and `workflow/README.md`: users speak naturally, English names are maintainer/Agent lookup terms, Chinese is explanatory, and legacy aliases live only in compatibility registries/history. [capabilities: `framework-charter`, `commands-reference`; no pipeline flags executed]

## 2. Resolve structural and intent-routing contradictions

- [x] 2.1 Rewrite the structural branch in `scripts/change-classifier.md` so add/delete/reorder always enters Structural Versioning Path first; remove the contradictory add-slide→Generated Image Rebuild-only example; state that the clean version subsequently refreshes affected slides by ownership/render mode. [capabilities: `commands-reference`, `playbook-execution`; Structural uses `--new-version`; affected generated pages may later require `--only <ids> --force-images`; no actual version created]
- [x] 2.2 Update `PPTMAKER_FRAMEWORK/COMMANDS.md` so case replacements, bulk data changes, KPI/card/chart labels, and other body text route by content ownership rather than automatically to `edit-text`; retain `edit-text` for structured KICKER/TITLE/SUBTITLE intent only. [capability: `commands-reference`; body-owned changes resolve to Generated Image Rebuild, header-owned changes to Header Text & Style Refresh]
- [x] 2.3 Update all iteration controllers — `playbook/classify-change.md`, `edit-text.md`, `edit-visual.md`, `edit-notes.md`, and `restructure-slides.md` — to separate intent route, structural versioning, and resolved refresh path. Ensure Structural Versioning Path composes with downstream refresh paths and full-page title work remains pilot/review/reuse. [capability: `playbook-execution`; Header Text & Style Refresh = 1→3,4,5; Generated Image Rebuild pilot uses `--only <ids> --force-images`; Notes-Only Refresh = 5; structural uses `--new-version`]

## 3. Align production and force/regeneration guidance

- [x] 3.1 Update production overview files `workflow/04-production/README.md`, `00-the-pipeline-philosophy.md`, `04-stage-4-build-the-pptx-container.md`, and `05-stage-5-inject-speaker-notes.md` to use canonical names, teach artifact ownership, and describe Generated Image Rebuild as a logical reviewed workflow rather than necessarily one all-stage command. [capabilities: `framework-charter`, `pipeline-orchestration`; no generation performed]
- [x] 3.2 Correct `workflow/04-production/02-stage-2-generate-images-with-anchoring.md` and `reference-pipeline-scripts.md`: raw `unified_pipeline --only` scopes Stage 2 but does not force; intentional rebuild of an existing selected image requires `--force-images`; `ppt_flow refresh --kind visual` supplies force for its explicit scope. [capability: `pipeline-orchestration`; tests: `tests/test_unified_pipeline.mjs`, `tests/test_ppt_flow.mjs`]
- [x] 3.3 Update `PPTMAKER_FRAMEWORK/scripts/README.md` and `workflow/00-setup/template-deck-guide.md` so every actionable single-image rebuild example includes the correct public route or explicit `--force-images`, while Header Text & Style Refresh and Notes-Only Refresh examples retain their current Stage behavior. [capabilities: `pipeline-orchestration`, `framework-charter`; no `_generated/` edits]
- [x] 3.4 Update explanatory comments in `scripts/unified_pipeline.mjs` to canonical English refresh-path names and clarify that the names are descriptive, not CLI enums. Leave `ppt_flow.mjs` generic “smallest safe refresh path” wording unless an actual legacy alias is found. [capability: `pipeline-orchestration`; tests: `tests/test_unified_pipeline.mjs`, `tests/test_ppt_flow.mjs`; no behavior change]
- [x] 3.5 Correct `workflow/05-iteration/03-pipeline-change-workflow.md` so header font/color/position changes inside the existing reserved zone may use Header Text & Style Refresh, while header safe-zone height or render-mode changes that alter the Stage 2 prompt contract use Generated Image Rebuild with forced regeneration and review. [capabilities: `pipeline-orchestration`, `framework-charter`; no pipeline execution]

## 4. Migrate all remaining active methodology and templates

- [x] 4.1 Update `workflow/05-iteration/README.md`, `00-openspec-capabilities-for-ppt.md`, `02-style-iteration-workflow.md`, `03-pipeline-change-workflow.md`, and `04-end-to-end-walkthrough.md` to canonical names and the two-layer structural/refresh model. [capabilities: `framework-charter`, `playbook-execution`, `commands-reference`; no runtime execution]
- [x] 4.2 Update setup/convention guidance `workflow/00-setup/00-run-bundle-concept.md`, `04-conventions.md`, and `README.md` so lifecycle diagrams and lookup descriptions use canonical English terms; keep unrelated migrate/import A/B/C strategy choices unchanged. [capability: `framework-charter`; no `--new-version` executed]
- [x] 4.3 Update `reference/anti-patterns.md` and any other active non-historical framework Markdown found by the impact audit so no operational file outside the exact compatibility registry retains a bare editing-chain alias. Leave `reference/version-log.md` and archived OpenSpec changes unchanged as history. [capability: `framework-charter`; documentation only]
- [x] 4.4 Inspect tracked run-bundle `deck-guide.md` copies for legacy editing-chain aliases. Update only tracked active copies that actually contain legacy terminology; do not rewrite project-specific guides that already use neutral “smallest refresh path” language. [capability: `framework-charter`; no run-bundle source or `_generated/` artifact changes]

## 5. Synchronize normative specs without parallel old requirements

- [x] 5.1 Apply the `framework-charter` delta by replacing the old A/B/C/Structural workflow requirement and render-aware scenario with the canonical three-refresh-path + Structural Versioning Path model. [capability: `framework-charter`; no runtime flags]
- [x] 5.2 Apply the `commands-reference` delta by renaming/replacing the old four-chain requirement and modifying classification, feedback, and title-routing requirements; remove the normative “bulk text = Chain A” shortcut. [capability: `commands-reference`; Generated Image Rebuild requires force/review when image-owned]
- [x] 5.3 Apply the `pipeline-orchestration` delta by renaming/replacing the editing-chain requirement while preserving the existing separate `--force-images` and `--only` requirements unchanged. [capability: `pipeline-orchestration`; no CLI surface change]
- [x] 5.4 Apply the `playbook-execution` delta by renaming/replacing the iteration-chain requirement and updating the full-page header-remedy requirement to Generated Image Rebuild. [capability: `playbook-execution`; no state schema change]
- [x] 5.5 Apply the new `cli-surface` delta to the full title-refresh requirement, preserving selector behavior, `TITLE_REVIEW_REQUIRED`, exact forced-pilot next invocation, and reviewed-image reuse. [capability: `cli-surface`; tests: `tests/test_ppt_flow.mjs`, `tests/test_cli_error.mjs`]
- [x] 5.6 Before final completion, use the OpenSpec spec-sync workflow to merge this change's deltas into all five main specs without archiving, then update Purpose and summary prose that delta requirement merging does not cover. Do not consider the change complete while active main specs retain a bare editing-chain alias outside a locally paired compatibility mapping. [capabilities: `framework-charter`, `pipeline-orchestration`, `playbook-execution`, `commands-reference`, `cli-surface`]

## 6. Add coherence and regression gates

- [x] 6.1 Extend `scripts/lib/framework_coherence.mjs` with explicit compatibility-registry and historical exception sets. Require every legacy alias occurrence in a registry file to be paired with its canonical English term in the same definition/sentence/table row; reject `Chain A/B/C`, `链 A/B/C`, and bare editing-chain A/B/C stage tables elsewhere; do not match unrelated local A/B/C choices. [capability: `framework-charter`; test: `tests/test_docs_consistency.mjs`]
- [x] 6.2 Add documentation fixtures for: accepted canonical terms; accepted paired aliases inside registry files; accepted header text/overlay-style→Header Text & Style Refresh; rejected safe-zone/render-mode→Header Text & Style Refresh; rejected legacy-only operational prose; accepted migrate/import A/B/C choices; rejected add-slide→Generated Image Rebuild-only classification; rejected body-data→Header Text & Style Refresh; and rejected raw `--only`-implies-force guidance. [capabilities: `framework-charter`, `commands-reference`, `pipeline-orchestration`; test: `tests/test_docs_consistency.mjs`]
- [x] 6.3 Rename behavior-test descriptions that use legacy aliases while keeping assertions unchanged. Preserve coverage that body-lock title refresh omits Stage 2, full-page title refresh requires explicit scope/review, visual refresh forces selected images, and notes refresh uses Stage 5. [capabilities: `pipeline-orchestration`, `cli-surface`; tests: `tests/test_ppt_flow.mjs`, `tests/test_unified_pipeline.mjs`]

## 7. Verify semantic closure

- [x] 7.1 Run a repository-wide active-file audit and produce a checked manifest: compatibility registries contain paired aliases; operational guidance contains canonical names only; history is excluded; no relevant file is left legacy-only. [all five modified capabilities; no pipeline execution]
- [x] 7.2 Verify structural consistency: every add/delete/reorder route enters Structural Versioning Path before affected-page refresh, and no active source calls it a fourth peer refresh chain. [capabilities: `commands-reference`, `playbook-execution`, `framework-charter`]
- [x] 7.3 Verify force/review consistency: every Generated Image Rebuild example that operates on existing images uses the public forced refresh route or explicit `--force-images`; full-page title examples preserve pilot → review → reuse. [capabilities: `pipeline-orchestration`, `cli-surface`, `playbook-execution`]
- [x] 7.4 Run `openspec validate clarify-edit-chain-semantics --strict`, `npm test`, and the final active-spec/active-doc terminology searches. Confirm CLI help, Stage mappings, state schema, render-mode behavior, and generated-artifact ownership are unchanged. [all five modified capabilities; no style-master regeneration, `--new-version`, or production image generation required]
