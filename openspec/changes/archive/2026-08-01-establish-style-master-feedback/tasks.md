## 1. Style Master Authority And Candidate Plan

- [x] 1.1 [style-master-generation/run-bundle-layout] Audit the current static `style_master.jpg`, including its layout resolver's declared override-first/backbone-default path rule, retired `style-master` surface, single-node fresh-v2 authoring-draft resolver, node parser/controller-manifest active-node projection, current-pair resolver, Page Authority path/state writers, unified CLI inventory, and test fixtures; record the exact replacement map and confirm no production `deck_*` or `dpt_*` path becomes an implementation input or fixture. See `implementation-audit.md`.
- [x] 1.2 [style-master-generation/run-bundle-layout] Implement the owner path constants and record validators for canonical confined `_staging/plan-<unique>/` initial publication, `plans/<plan-sha256>/` history, and one CAS `scopes/<run-version>/<workflow>/head.json`; define the non-self-referential `page-authority-style-master-plan-identity-v1` canonical JSON and its digest, the exact `page-authority-style-master-head-v1` field set/cross-binding/byte-CAS, the exact write-once `page-authority-style-master-candidate-grant-v1` field set/external digest/create-or-exact-match rule, exact candidate-attempt and abandonment field sets/external digests/state transitions, immutable candidate image plus local/generated canonical `provenance.json` addressing, immutable decision records, monotonic attempts bound to the grant digest with derived progress, and the version/workflow-scoped effective-selection/acceptance reference. Done when partial staging, unreferenced bytes/provenance or a complete plan, timestamp/host path/directory scan, or `style_master.jpg` presence cannot affect plan identity or select a plan/accepted style.
- [x] 1.3 [node-specification/style-master-generation/slide-identity-and-ordering] Extend schema-v5 validation and atomic/CAS state persistence with the optional `page_authority_style_master.by_version` record by reference to the Style Master-owned field schema; validate canonical keys plus record run-version/workflow binding, and implement the read-only effective-selection resolver and Boolean `style_master_accepted` condition. Preserve missing-record support and structurally-valid-stale false readiness while making malformed present records a non-writing state hard-stop. Preserve fresh-draft promotion without page lineage, first raw-plan materialization, first structural-vNext non-inheritance, and source-version records. Update the existing target structural preview/apply owner so an already-visible target can replay only the original exact plan after revalidating canonical source bytes/plan hash, target source/receipt, target workflow/source-epoch evidence identity, and selection-map structure; route that branch before the normal target-absent/next-version and source-execution fences. It must accept later nonempty target-owned evidence only when it remains valid and bound to the same target tuple, use no staging/publication branch, do not create vNext, and preserve a later valid target-owned record plus active target Controller execution byte-for-byte. Any changed target tuple remains a non-writing fresh-preview hard-stop.
- [x] 1.4 [style-master-generation/node-specification] Implement exact scope resolution for an active fresh-v2 authoring draft by adding the optional node `draft_route: true` parser contract (key absent or literal true only), ordered per-workflow `draft_route_nodes` manifest schema/exact-agreement validator, normalized Controller-reader projection, and draft-router consumption that replaces the literal workflow-selection-node check without name/phase/order inference or a copied router list. Reject false/string/number/null and duplicate-key forms. Prove the generic validator/router contract with fixture routes here; task 4.3 supplies the built-in workflow-selection/content/visual-system/selected-Style-Master/first-raw node lists. Reuse the selected workflow's existing read-only candidate-source resolver without calling `ppt_flow validate` / `resolveSource`; retain exact current-pair resolution for existing runs and expose the canonical local-existing source path. Reject another controller, manifest disagreement, unknown/sibling/post-raw nodes, bound mode, bare-file authority, arbitrary prompt/path/provider inputs, stale scope, unsupported pairs, and invented historical provenance before mutation.
- [x] 1.5 [style-master-generation] Implement provider-free canonical Style Master candidate-plan compilation from the resolved scope/style intent, selected workflow, `canonicalJsonSha256` of the exact fixed provider/output/prompt-contract profile, ASCII-stable-ID-sorted unique style context, deterministic compiled-provider-prompt digest, copied local-byte/media/dimension identity and noncyclic canonical local-provenance digest, candidate slots/count, `previous_selection_sha256`, and owner-issued plan generation/predecessor. Resolve only the canonical local path from 1.4: omit it only when absent, otherwise require a confined regular stable supported-image snapshot and hard-stop invalid presence before plan/head/provider work. Stage and validate the complete initial bundle, atomically rename it into canonical history, exact-validate a concurrent destination, and only then CAS the head; cleanup stays inside `_staging/`. Return a matching nonterminal plan idempotently and allocate a distinct same-input successor only after the exhaustive direct-record terminal predicate and terminal head CAS. Keep unchanged claimed, incomplete, unreviewed, pending-proceed-CAS, and un-abandoned-unknown states on their same-plan actions. Expose bounded cost/progress with no page raw write or provider work.
- [x] 1.6 [tests] Add focused unit coverage for absent versus structurally-valid-stale versus malformed-present selection records, canonical key/run-version/workflow validation, persistence/currentness, raw/structural preservation boundaries, exact plan-identity field set/non-self hash/recomputed digest and exclusion of time/path/runtime state, `{slide_id, projection}` ASCII stable-ID ordering/uniqueness/membership binding, exact generation-profile object/digest and compiled-prompt digest drift, exact local-provenance field set/noncyclic digest, plan staging crash windows/concurrent destination exact-match/unreferenced-plan nonauthority/confined mutation-only cleanup plus non-writing inspection, exact head field/cross-binding/byte-CAS, exact candidate-grant field set/external digest/atomic create-or-exact-replay and plan/profile/slot cross-binding, every terminal/nonterminal successor case, same-input successor allocation, scope/workflow binding, empty/malformed style intent, absent local input, rechecked local-byte snapshotting, invalid-present local hard-stop, file-presence non-authority, fresh-draft/current-pair distinction, and zero provider/page-raw side effects on planning failures. Add exact target structural replay coverage through both the owner and persisted `slides apply-plan`: after target Controller activation and a valid target-owned selection, replay must make no target filesystem/state/controller mutation; target source/receipt/mode/evidence/map drift must fail before mutation.

## 2. Exact Candidate Authorization And Recovery

- [x] 2.1 [style-master-generation] Implement one immutable canonical `page-authority-style-master-candidate-grant-v1` with exact scope/plan, ordered generated slots, equal positive maximum submissions, and generation profile; compute and return its external `candidate_grant_sha256`, atomically create-or-exact-match it after current-head validation, and reject divergent/malformed existing bytes without overwrite. Bind every generated attempt to that exact digest and derive consumption only from its `submitted` attempts; reject grants for zero-generated plans, and ensure the grant cannot authorize page raw work, Pilot/Expansion, another plan, or a retry.
- [x] 2.2 [style-master-generation] Implement serialized canonical `page-authority-style-master-candidate-attempt-v1` `claimed -> submitted -> succeeded|failed|unknown` CAS transitions using the existing Image2 credential/transport owner; validate its exact field set/external digest and persist claim before provider initialization, bind that attempt to the exact current `candidate_grant_sha256`, resume an interrupted pre-submit claim only by advancing that same attempt after exact plan/grant/scope/input and recompiled-prompt-digest validation, and CAS-persist the canonical provider-request digest at `submitted` before the one transport call consumes its slot. Atomically place valid bytes and exact generated `provenance.json` individually, then use terminal-attempt CAS as the single publication point binding both digests and exact-matching the submitted request digest. Make terminal publication lose cleanly if abandonment won the attempt CAS, leaving preplaced files non-authoritative; publish derived progress before another candidate starts, and stop the sequence at the first known failure without spending later slots on a plan that can no longer reach review.
- [x] 2.3 [style-master-generation] Implement the immutable exact canonical `page-authority-style-master-candidate-abandonment-v1` writer and terminal-head successor path: treat orphan `submitted` as unknown without observation mutation; normalize the human reason to NFC with Unicode-whitespace runs folded to one ASCII space, trim it, reject remaining C0/C1 controls and normalized UTF-8 outside `1..512` bytes, and digest those exact bytes before mutation. CAS the exact attempt from `submitted` to abandonment-owned `unknown` with that reason digest (or exact-match transport-owned `unknown`) as the sole arbiter against a late terminal response; then bind and external-digest the current scope head/plan/grant/terminal unknown attempt/provider request/normalized reason text and digest in the write-once abandonment that derivationally closes only that plan. Make a crash between abandonment-owned unknown CAS and record creation same-reason recoverable, make losing preplaced bytes non-authoritative, allow exact replay after successor head advancement, and require a new plan plus new authorization for later generated candidates. Do not invent provider reconciliation, hidden retry, attempt outcome editing, or a second provider route.
- [x] 2.4 [tests] Add unit and integration tests for prerequisite-first grant checks, zero-generated grant rejection, immutable grant exact field/digest validation, atomic create-or-exact-replay, malformed/divergent-grant no-overwrite, exact attempt/abandonment field/digest validation, attempt-grant binding, and derived consumption, same-attempt `claimed` resumption with exact compiled-prompt match and no early consumption/double submit, compiler/profile/input drift before submit, exact generated-provenance/provider-request field sets, submitted-before-call request persistence, terminal/request digest exact matching, preplaced image/provenance nonauthority, successful provenance request binding and first-known-failure stop with later capacity unconsumed/unusable, crash/partial-write containment at every transition, unknown hard-stop retaining request identity, reason normalization/bounds/digest stability, abandonment-vs-late-response CAS races, same-reason abandonment recovery/replay after head advancement, different-reason conflict, same-input successor before new cost, bounded progress, and no page raw materialization or source-epoch mutation.

## 3. Real-Byte Review, Promotion, And Raw-Profile Binding

- [x] 3.1 [style-master-generation] Implement exact-`--plan-hash` candidate review preparation that verifies the scope head and one exact authority chain before projecting real candidate images: every generated slot must have its matching grant-bound `succeeded` attempt, submitted provider-request digest, bytes, and generated provenance, while a local-existing slot must have only its plan-bound copied bytes/local provenance. Expose that same validator for `accept` to rerun before any decision write or promotion without creating a second review receipt. Make historical, missing, stale, preplaced, failed, unknown, or incomplete evidence a hard-stop with one owner action.
- [x] 3.2 [style-master-generation] Reuse the task-3.1 validator before a new typed `proceed | repair | redirect` decision write or selection CAS; implement timestamp-free canonical `page-authority-style-master-review-decision-v1` write-once/exact-match records, then CAS/atomically promote the effective style. Bind the acceptance receipt to exact selected bytes/media, scope, profile, decision digest, previous selection, and the sole `accepted_at` timestamp chosen by the CAS winner. Concurrent semantic replay must return that winner record rather than conflict on clock time. After CAS, ensure the format-correct JPEG compatibility projection at the layout-resolved existing override-or-backbone path without creating an override; on projection failure emit no success receipt on stdout and emit a nonzero owner diagnostic whose existing `subject` names the committed selection digest, whose reason classifies compatibility projection failure, and whose non-human `next.invocation` preserves the same exact accept arguments. Make replay for the plan still named by current selection return the original record/timestamp and repair only a missing or stale derived payload even after head advancement, while rejecting changed/newer selection bindings and leaving current selection unchanged on conflict or non-proceed decisions.
- [x] 3.3 [image-generation/pipeline-orchestration] Replace bare style-file raw-profile and provider-transport reference resolution with the current accepted effective-selection resolver; bind selection identity and exact immutable bytes into the page raw profile and pass those same bytes to the submitter without reopening `style_master.jpg` or `style_master_path`. Route missing/stale intent/context/bytes/profile/scope/selection back to Style Master before page raw planning, and classify that style drift plus pre-change raw lineage without a selection binding as Generated Image Rebuild debt without creating source epoch or silently rebinding old bytes.
- [x] 3.4 [run-bundle-layout] Extend layout validation and ownership documentation over the path/constants from 1.2 for confined plan staging, append-mostly plan history, mutable per-scope head, format-correct JPEG compatibility payload at the declared override-first/backbone-default resolved path, optional schema-v5 selection/acceptance record, and affected scope/staleness projection. Verify staging is never authority and cleanup cannot escape its root; verify the state writer from 1.3 presents first-vNext absence and exact-replay preservation correctly. Do not create a version override, cross-version rebind, filesystem-current heuristic, or second evidence ledger.
- [x] 3.5 [tests] Add promotion/invalidation coverage for generated review eligibility requiring a matching grant-bound `succeeded` attempt/provider request/bytes/provenance chain and local review eligibility requiring only copied bytes/local provenance, reviewed-byte-only projection, revalidation at accept before decision/CAS, exact decision field set/digest and timestamp exclusion, concurrent identical decision exact-match, decision-write-to-selection-CAS interruption, exact promotion replay with stable timestamp/receipt, post-CAS JPEG failure diagnostic fields/no-success-stdout and same-replay repair, override-first and backbone-default compatibility projections without override creation, raw provider transport receiving selected immutable bytes rather than a reopened compatibility path, selection and head CAS success/conflict, repair/redirect non-promotion, intent/context/bytes/scope/profile/selection drift, pre-change raw-lineage migration fence, raw-plan fence, stale downstream evidence, first structural-vNext non-inheritance plus exact no-publish replay preservation of later target acceptance and target Controller state, action-scoped payload rebuild that leaves sibling version records unchanged, and no silent rebind or page provider call.

## 4. Current-V2 CLI And Selected-Workflow Controller

- [x] 4.1 [cli-surface] Restore the top-level current-v2 `ppt_flow style-master inspect|plan|authorize|generate|review|accept|abandon` command family with exact plan-hash review and normalized-reason abandonment; make optional inspect plan hash a current-head assertion rather than historical action projection; route every operation through the one Style Master owner, preserve provider-free inspect/plan/review, reject inapplicable zero-generated cost operations and all legacy/bypass inputs, and atomically update the 11-command registered unified CLI inventory/help/coherence checks plus the main `cli-surface` Purpose to the resulting fixed 12-command surface, correcting its stale fixed-14 claim.
- [x] 4.2 [cli-surface] Map Style Master failures through `cli_error.mjs` using the earliest owner root and one secret-safe next action; verify final stderr JSON ownership, committed-selection `subject`, exact structured replay invocation, and no premature success stdout without copying the producer schema into Controller consumers.
- [x] 4.3 [playbook-execution/pipeline-orchestration] Insert separate Framed and Pure Style Master inspect/plan, conditional generated-slot cost authorization/progress, exact-hash real-byte review, promotion, and unknown-abandonment handoffs after visual-system configuration and before existing page raw authorization. Mark only the exact workflow-selection/content/visual-system/selected Style Master/first-raw nodes as `draft_route: true`, bind their per-workflow order in the controller manifest, and complete the built-in route integration from 1.4 so fresh routing reaches read-only candidate-source validation and first raw-plan handoff without first invoking materializing `ppt_flow validate`; retain exact-pair validation for existing runs. Skip rather than approve an absent cost gate for zero-generated plans, preserve one selected workflow per version, and keep `proceed` distinct from every page-level decision.
- [x] 4.4 [playbook-execution] Update Controller-facing task projection/documentation to reference owner-issued Style Master inspect/head/progress facts only, with current `proceed | repair | redirect` routing, reasoned unknown-plan abandonment, and no task-checkbox resume authority.
- [x] 4.5 [tests] Add process-level CLI and Controller tests for provider-free current inspect plus stale `--plan-hash` assertion, plan/review, exact candidate grant, zero-generated routing, unknown abandonment diagnostics, post-CAS projection partial-success envelope, command inventory/help, draft/current/non-v2 and bypass handling, parser rejection of noncanonical `draft_route` values/duplicates, manifest exact agreement, and fresh routing at workflow-selection/content/visual-system/each Style Master/first-raw boundaries with another controller plus unknown/sibling/post-raw nodes fenced, fresh source validation through the read-only candidate resolver with no `resolveSource` call or source/state write, optional schema-v5 state, structural-vNext selection boundary including persisted exact-plan no-publish replay after target activation, `style_master_accepted` gating, Framed/Pure isolation, promotion-before-raw authorization, and successful same-check recovery.

## 5. Integration, E2E, And Change Validation

- [x] 5.1 [integration] Exercise the complete fresh-draft Style Master lifecycle with minimal test fixtures: plan -> authorize -> ordered candidate commits -> exact-hash real-byte review -> `proceed` CAS promotion -> first allowed page raw plan/source-state materialization; also exercise zero-generated local review. Assert zero page raw provider submits and zero Pilot/Expansion/final/delivery artifacts throughout Style Master work.
- [x] 5.2 [integration] Add negative lifecycle coverage for stale intent/profile/scope/selection, copied or missing candidate bytes, preplaced generated bytes/provenance without the successful grant/attempt/request chain, lost head/selection CAS, incomplete or historical review, known failure, unknown attempt and abandonment, same-input successor, existing schema-v5 bundle migration, concurrent candidate/selection ownership, and target structural exact-plan replay versus target tuple drift; assert no wrong-owner writes, old-plan rebind, inferred outcome, hidden retry, vNext recreation, or target Controller reset.
- [x] 5.3 [tests_e2e] Add one Framed and one Pure mock Controller journey through their independent Style Master entries and acceptance handoff, asserting no sibling-workflow semantics or legacy CLI surface is exposed.
- [x] 5.4 [verification] Run the implemented focused Style Master, raw-profile, CLI, state, and Controller test files through `npm run test:focused -- <exact-test-path>` one at a time; run the Framed and Pure mock journey through `npm run test:mock-e2e -- <exact-journey-path>`; then run protected `npm test`, `openspec validate establish-style-master-feedback --strict`, `openspec validate --all --strict`, and `git diff --check`. Record exact commands/results and any intentionally unavailable real-provider evidence; do not run broad or real E2E without its separate authorization.
