## 1. Node and Browser Runtime Profile

- [x] 1.1 (`html-render-runtime`, `environment-check`, `pipeline-orchestration`) Raise the package engine floor to Node.js `>=22`, encode supported major lines `22.x`/`24.x`/`26.x` in the checked-in runtime profile and executable gate, pin production `playwright` exactly to `1.61.1` in package and lock files, and add tests proving 22/24/26 pass while 20 and undocumented 23/25 fail.
- [x] 1.2 (`html-render-runtime`) Add canonical `setup:chromium` and `setup:chromium:with-deps` package scripts using the pinned local Playwright CLI; document standard/custom cache and setup-only proxy/download-host behavior without adding install logic to doctor or runtime.
- [x] 1.3 (`html-render-runtime`) Add one checked-in runtime profile and internal ESM interface that accepts the canonical discovered Playwright package root/version, loads and verifies exact Playwright 1.61.1 plus paired Chromium revision 1228/browser 149.0.7827.55 through that same root, launches only Playwright Chromium, and returns normalized evidence without exposing a direct CLI.
- [x] 1.4 (`html-render-runtime`) Add unit tests for exact version/profile comparison, discovered-root loading with conflicting/shadow Playwright copies, standard and consistently configured custom cache discovery, missing/mismatched paired browser, and rejection of system channels or arbitrary executable overrides.

## 2. Official Font Distribution and Evidence

- [x] 2.1 (`html-render-runtime`) Acquire Source Sans 3 variable-normal WOFF2 weights 200-900 from Adobe's official `3.052R` release and record source URL/release, original filename, measured bytes, and SHA-256.
- [x] 2.2 (`html-render-runtime`) Snapshot the official Google Fonts Noto Sans SC 100-900 CSS response with fixed request parameters and recorded modern-browser user-agent class; commit the original response and every referenced WOFF2 shard unchanged, recording served version/path, retrieval date, URL, Unicode range, measured bytes, and SHA-256.
- [x] 2.3 (`html-render-runtime`) Generate local relative-URL CSS plus a canonical machine-readable inventory that proves original-CSS/local-CSS/file completeness, family/style/weight/range metadata, per-file integrity, snapshot identity, and total bytes; add no third-party font package or TTF conversion toolchain.
- [x] 2.4 (`html-render-runtime`, `framework-directory-layout`) Place binaries, original/local CSS, inventory, provenance, copyright notices, and complete OFL material under the sole canonical `PPTMAKER_FRAMEWORK/scripts/fonts/` tree; keep the five-directory framework root and the legacy Stage-3 canvas font contract unchanged.
- [x] 2.5 (`html-render-runtime`) Add a fixed ASCII/Latin-accent/punctuation-currency/numeral/Simplified-Chinese/CJK-punctuation sentinel corpus and validation for missing files, digest drift, CSS/inventory mismatch, malformed/conflicting ranges, missing legal material, and unsupported sentinel code points.
- [x] 2.6 (`html-render-runtime`, `framework-directory-layout`) Add directory and coherence tests proving no duplicate framework font authority, runtime font URL, run-bundle font distribution, third-party acquisition authority, or unsupported full-CJK claim exists.

## 3. Renderer-Independent Offline Smoke

- [x] 3.1 (`html-render-runtime`) Add a checked-in static HTML fixture with its own fixed viewport, local font CSS, deterministic geometry assertions, and explicit evidence that it does not import future slide-renderer code or alter legacy `1672x941` configuration/fingerprints.
- [x] 3.2 (`html-render-runtime`) Implement runtime smoke that launches paired headless Chromium, blocks service workers, aborts and records every HTTP/HTTPS attempt, waits for `document.fonts.ready`, rejects CSS `local()` sources, and combines manifest/CSS/file integrity with Chromium font-usage inspection proving the expected custom family and non-zero glyph use for dedicated Latin/Han sentinel nodes; `document.fonts.check(...)` remains supporting evidence only. Bound the whole operation with checked-in `HTML_RUNTIME_SMOKE_TIMEOUT_MS = 30_000`, report the last normalized phase on timeout, and close context/browser in `finally`. Fail on font, geometry, network, timeout, or cleanup violations.
- [x] 3.3 (`html-render-runtime`) Prove through unit tests that smoke/runtime never invokes a browser/font installer, downloads an asset, accepts OS-font fallback as readiness evidence, or claims actual-deck code-point/overflow coverage.
- [x] 3.4 (`html-render-runtime`) Add integration tests that launch installed paired Chromium against the fixture and verify zero network requests plus expected font/geometry evidence, and inject a stalled phase to verify bounded timeout and cleanup; allow the real-browser case to skip only through an explicit documented unavailable-browser test condition, never by treating absence as success.

## 4. Layered Environment and CLI Readiness

- [x] 4.1 (`environment-check`) Preserve built-ins-only startup and ancestor package discovery, diagnose missing packages before dynamic import, verify exact Playwright metadata, pass the exact discovered package root/version into the shared runtime, and enter it only after npm-backed prerequisites exist; do not allow runtime module-relative resolution to select a second copy.
- [x] 4.2 (`environment-check`) Implement deterministic modes: default base only; `--image2` base plus offline Image2 presence; existing `--smoke`/`--probe-vendors` imply Image2; live flags remain mutually exclusive and accept redundant `--image2`.
- [x] 4.3 (`environment-check`) Add blocking base records `chromium`, `html_fonts`, and `html_runtime_smoke`; omit `api_key`, `image_base_url`, and `stage2_generator` from base output so absent Image2 configuration cannot change base READY.
- [x] 4.4 (`environment-check`) Keep the existing single-canonical-entry Image2 credential/base-URL/Stage-2 SSOT in Image2 mode without adding a new multi-vendor config schema; report only a secret-safe resolved count, make `--image2` network-free, and preserve exactly one POST attempt for `--smoke` or one per resolver entry for `--probe-vendors`. Diagnostic submits SHALL set redirect handling to error/manual-safe behavior and SHALL NOT retry redirects, transient responses, timeouts, or ambiguous network failures. Include injected multi-entry, 307/308, transient-5xx, timeout, and network-error regressions proving no second POST.
- [x] 4.5 (`environment-check`) Preserve `env-check-v1` JSON/check-array semantics, selected-mode exit status, human READY/NOT READY endings, live progress/summary behavior, and secret safety without introducing a second diagnostic schema. In any accepted `--json` combination, emit exactly one JSON document on stdout and route live heartbeats/human summary text to stderr or structured check evidence.
- [x] 4.6 (`cli-surface`) Add text-only `ppt_flow doctor --image2` delegation and help; preserve old live flags, their implied Image2 mode, mutual exclusion, top-level command inventory, and the existing delegated parent failure envelope.
- [x] 4.7 (`environment-check`, `cli-surface`) Add tests for no-`node_modules` startup, ancestor resolution, version/browser/font failures, no-Image2 base READY, offline Image2 failure/success, safe vendor count, all live-flag combinations/submit counts, redirect/no-retry safety, help, parseable single-document JSON under base/`--image2`/`--smoke`/`--probe-vendors`, and secret-safe direct/delegated failures.

## 5. Legacy Image2 Submit-Boundary Guards

- [x] 5.1 (`pipeline-orchestration`) Inventory every current remote Image2 submit reached by style-master generation, pilot, build, visual rebuild, and unified Stage 2, recording the existing credential/base-URL and style-reference authorities used at each boundary.
- [x] 5.2 (`pipeline-orchestration`) Add or strengthen one shared fail-before-submit guard invoked only after actual remote work is known and immediately before the first adapter submit: all remote submits require transport prerequisites; legacy page generation additionally requires current style reference; style-master generation does not require a pre-existing style master.
- [x] 5.3 (`pipeline-orchestration`, `cli-surface`) Route missing prerequisites through the existing secret-safe diagnostic authority before provider-adapter invocation, without relying on a previous doctor result or emitting key/provider payload data.
- [x] 5.4 (`pipeline-orchestration`) Preserve local Stage subsets, notes-only work, assembly from reviewed images, dry runs, Structural Versioning analysis/materialization, no-op existing style-master reuse, and all-current Stage-2 image reuse with no Image2 transport resolution or remote call.
- [x] 5.5 (`pipeline-orchestration`) Add fake-adapter regressions proving zero submit for missing credential/base URL/page style reference, permitted style-master submit with valid transport and no prior style master, successful guarded page submit, no credential lookup for no-op/current-artifact reuse, and zero remote calls on local/dry-run/structural paths.

## 6. BOOTSTRAP and Phase-0 Diagnostic UX

- [x] 6.1 (`playbook-execution`) Update `probe-image-channels` in its existing Phase-0 / `00-setup` role to resolve vendor count offline, disclose one submit per resolver entry, obtain confirmation before `--probe-vendors`, relay progress, and keep report-only intent separate from optional configuration writes. After a write, recheck saved presence with offline `doctor --image2`; remove the unconditional follow-up `--smoke`, and permit a new smoke only for an uncovered saved combination or explicit human request after a fresh one-submit disclosure and confirmation.
- [x] 6.2 (`playbook-execution`, `bootstrap-env-guidance`, `image-generation`) Require any current playbook/entry-doc offer of `doctor --smoke` to disclose exactly one expected submit and obtain confirmation; remove style-master generation as a channel-diagnostic substitute, ensure decline makes zero calls, and ensure success creates no build approval or page-refinement authorization/state.
- [x] 6.3 (`bootstrap-env-guidance`) Rewrite BOOTSTRAP Step 1 base remediation for supported Node `22.x`/`24.x`/`26.x` (recommend current LTS `24.x` for fresh installs), one consolidated npm install, explicit Chromium setup, HTML font/runtime-smoke failures, current local/advisory checks, and beginner-copyable macOS/Windows/Linux paths.
- [x] 6.4 (`bootstrap-env-guidance`) Move `api_key`, `image_base_url`, `stage2_generator`, and live-probe remediation into a clearly optional Image2 subsection; distinguish offline `doctor --image2` from confirmed live flags and scope Image2 NOT READY only to an Image2 action.
- [x] 6.5 (`bootstrap-env-guidance`) Update active setup/runtime guidance to the new environment facts while retaining the current workflow topology and avoiding claims that structured HTML deck rendering, new-deck HTML defaults, modern Phase-4 refinement, or full CJK support already exists.
- [x] 6.6 (`bootstrap-env-guidance`, `playbook-execution`, `image-generation`) Add consistency tests comparing emitted base/Image2/live check names to the owning guidance sections and rejecting stale Node-18, universal-Image2 prerequisite, unconfirmed-live-probe, style-master-as-diagnostic, runtime-download, system-browser-fallback, and premature HTML-product claims.

## 7. Apply Verification and Boundary Audit

- [x] 7.1 Run focused Vitest suites for runtime profile/fonts/smoke, environment modes, doctor delegation/envelopes, legacy submit guards, playbook confirmation, and documentation/layout coherence; resolve every regression.
- [x] 7.2 Run the real paired-Chromium offline fixture smoke on the current maintained macOS machine; record only normalized profile/custom-font/network/geometry evidence with no local absolute paths or secrets. Unit/profile tests SHALL cover unsupported-major rejection. Windows/Linux execution is intentionally outside this change's acceptance gate.
  - Mac Node 22 evidence passed with Playwright `1.61.1`, Chromium revision `1228` / browser `149.0.7827.55`, custom Source Sans 3 and Noto Sans SC usage, fixed `960x540` geometry, and zero network requests. Cross-platform CI remains optional portability infrastructure, not completion evidence.
- [x] 7.3 Run full `npm test` and relevant `tests_e2e` when legacy public pilot/build behavior crosses an E2E boundary; record a concrete reason for any E2E category not required.
- [x] 7.4 Run `openspec validate upgrade-html-render-runtime-readiness --strict` and `git diff --check`; verify proposal capability names exactly match all eight delta-spec directories.
- [x] 7.5 Audit the final diff for scope: no structured slide source, actual slide HTML renderer, future 1600x900/DPR-2 profile activation, PPTX/default-workflow change, workflow-directory migration, run-bundle/state schema change, or modern Image2 refinement transaction may be implemented.
- [x] 7.6 Keep this checklist current during apply and mark the parent roadmap Apply/Validate stages only after their defined tests pass; do not use OpenSpec artifact status or chat progress as implementation evidence.
