## 1. Establish the v2-only retirement boundary

> Closeout status (2026-07-29): 23/23 complete. Active main specs were synchronized before archive and passed the main-spec retirement audit; OpenSpec archive then moved this change to `archive/2026-07-29-retire-current-v1-compatibility` with `--skip-specs` because its deleted capability deltas cannot be rebuilt by the generic archive sync. The framework change is committed after final diff verification. The recommended breaking-change version is `0.23.0`, but version files remain `0.22.0` because no explicit version-bump confirmation was given.

- [x] 1.1 [framework-charter, framework-directory-layout, framework-script-layout] Before deleting anything, create `openspec/changes/retire-current-v1-compatibility/retirement-matrix.md` as archived change evidence. Inventory every active v1/legacy-adoption source, import, receipt, state field, controller node, doc link, test, and main-spec clause; each row names the discovered reference, behavior, `v2 owner`/`shared invariant`/`delete` classification, absorbing owner or deletion target, retained-proof owner, and verification command. Exclude independently owned JSON/version/visual-language historical terminology; no unclassified row may be retained or deleted.
- [x] 1.2 [run-bundle-management, node-specification, workflow-inspection] Define the exact-v2 classification boundary from source/state direct facts: every non-v2 pair receives the same unsupported-protocol/export hard-stop, protecting identity/bytes/lineage; prove normal observation has no write authority or historical decoder.
- [x] 1.3 [cli-surface, playbook-execution, pipeline-orchestration] Remove every public command, controller node, and orchestration route that selects adoption or another protocol; retain one bounded non-v2 diagnostic without a migration command, provider call, or generated-artifact read.

## 2. Absorb retained behavior into the one v2/shared graph

- [x] 2.1 [content-parsing, node-specification] Move every reusable v1 source/receipt/state/evidence invariant into the selected v2 or shared owner, remove per-slide authority/default parsing from current source construction, and ensure non-v2 input fails before receipt or provider work.
- [x] 2.2 [image-generation, image-production, pipeline-orchestration] Preserve necessary raw-plan, evidence, final-manifest, and shared-delivery semantics through typed v2 workflow contracts; remove another-protocol adapter/finalization/foreign-evidence fallback from current execution.
- [x] 2.3 [slide-identity-and-ordering] Preserve stable-ID and exact structural-plan invariants solely through v2 workflow-bound preview/apply; remove v1 structural materialization and adoption-matrix execution from active structural paths.
- [x] 2.4 [workflow-inspection, playbook-execution] Make marker-first observation and controller routing resolve only selected v2 workflow facts; every non-v2 pair returns one non-mutating unsupported-protocol/export hard-stop, while a hybrid/corrupt pair remains its identity repair hard-stop.
- [x] 2.5 [workflow-inspection, node-specification] Add focused negative coverage proving status, ordinary state, inspection, controller resume, and target execution cannot initialize a v1 receipt/state, dispatch a v1 writer, decode a historical protocol, infer a workflow, or mutate a non-v2 bundle; prove each repair reruns the same direct checkpoint.

## 3. Delete retired active surfaces and Agent-visible noise

- [x] 3.1 [framework-script-layout, image-production] Delete `PPTMAKER_FRAMEWORK/scripts/compatibility/` and every v1 writer, adapter, receipt initializer, state branch, process dispatch, and inventory entry after its matrix row is absorbed; do not leave redirect/re-export stubs.
- [x] 3.2 [framework-directory-layout, framework-charter, commands-reference] Delete `PPTMAKER_FRAMEWORK/workflow/compatibility/`, v1 classifiers, compatibility links, and historical current-route prose; make root/framework README, BOOTSTRAP, AGENTS, Charter, COMMANDS, workflow roots, and playbooks expose only `03 XOR 04 -> 05 -> 06` plus the generic unsupported-protocol stop.
- [x] 3.3 [framework-directory-layout, framework-script-layout] Delete `tests/compatibility/` active proof and all v1/legacy adoption fixtures, manifests, ownership records, README placeholders, and E2E routes after converting retained behavior into v2/shared invariant proof.
- [x] 3.4 [framework-directory-layout, framework-script-layout] Update architecture, directory, source-to-test, executable/import, governance, retirement, controller-manifest, and documentation audits to fail closed if active roots register a compatibility/v1 owner, import, writer, classifier, receipt, decoder, or route.
- [x] 3.5 [bootstrap-env-guidance, cli-surface, run-bundle-layout] Delete legacy-adoption readiness wording, public CLI flags/diagnostics, and adoption scratch topology; update current CLI inventory/count and layout validation so only exact v2 current work is executable.
- [x] 3.6 [header-lock, html-render-runtime, html-slide-contract, notes-injection, pptx-assembly, visual-slot-refinement] Delete empty retired capability specs and protocol-specific historical prose; retain Framed runtime, notes, and PPTX contracts using only protocol-neutral current lineage language.

## 4. Synchronize the current specification surface

- [x] 4.1 [commands-reference, framework-charter, framework-directory-layout, framework-script-layout] Apply and verify the delta removals/rewrites so human guidance, active ownership, and architecture specs contain no current compatibility model.
- [x] 4.2 [content-parsing, image-generation, image-production, pipeline-orchestration] Apply and verify the delta removals/rewrites so parsing, raw evidence, finalization, and orchestration specs define only v2/shared current behavior.
- [x] 4.3 [node-specification, playbook-execution, run-bundle-management, slide-identity-and-ordering, workflow-inspection] Apply and verify the delta removals/rewrites so state, controller, bundle, structural, and observation specs use one v2 graph and one generic unsupported-protocol hard-stop rather than an active legacy route.
- [x] 4.4 [bootstrap-env-guidance, cli-surface, header-lock, html-render-runtime, html-slide-contract, notes-injection, pptx-assembly, run-bundle-layout, visual-slot-refinement] Apply and verify the remaining delta removals/rewrites so current bootstrap, public CLI, layout, runtime, delivery, and main-spec registry contain no legacy-adoption or retired-capability owner.
- [x] 4.5 [all affected capabilities] Add a main-spec and active-root audit that excludes OpenSpec archives but fails on remaining protocol-specific v1/compatibility current-contract language, paths, imports, executable ownership, empty retired capability specs, or active historical decoder.

## 5. Prove retirement and close out

- [x] 5.1 Run focused parser/state/inspection/structural/raw/final/delivery tests plus architecture, directory, ownership, controller, documentation, and main-spec negative audits; complete the archived retirement matrix with retained-proof results and record no-write snapshots for non-v2 and hybrid fixtures.
- [x] 5.2 Run process and selected E2E journeys for valid v2 Framed/Pure work and non-v2/hybrid hard-stops; confirm no provider authorization, historical decoder, production `deck_*`, `dpt_*`, or `_generated/` data was implicitly read or changed.
- [x] 5.3 Run `npm test`, `git diff --check`, and `openspec validate retire-current-v1-compatibility --strict`; resolve failures without restoring a compatibility fallback or hand-editing generated artifacts.
- [x] 5.4 After the user separately authorizes closeout, sync accepted delta specs, archive, commit, and record the deletion evidence. Per `project-versioning`, recommend the breaking-change MINOR bump from `0.22.0` to `0.23.0`; update version locations only after the user confirms that recommendation.
