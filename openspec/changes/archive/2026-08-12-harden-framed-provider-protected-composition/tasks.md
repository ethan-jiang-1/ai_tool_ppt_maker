## 1. Current Contract Cutover

- [x] 1.1 Update the schema README, current `layout-config`, `page-source-receipt`, `page-layout`, and `image2-request` stage declarations, and serialization inventory for the closed source restriction, CSS-pixel `header_region`, exact resolver-derived normalized `protected_composition`, body-safe provenance, and the removal of `protected_geometry`; retain no versioned or legacy reader path.
- [x] 1.2 Make the current Framed header contract cut over atomically across the parser/receipt, Page Image Core, Framed overlay/render-profile/raw-contract validators/compiler, raw-plan bindings, invalidation evaluator, review guide, architecture prompt-assembly inventory, run-bundle initializer's current source template, shared fixtures, and `BOOTSTRAP.md`: retain only local header literals and `header_region`/`protected_composition`, remove every `context_not_to_render` or `protected_geometry` reader/writer, converter, and fallback, and keep C5 serialization as an opaque adapter-request publisher.
- [x] 1.3 Preserve the parsed closed `subject_restrictions` fact as a Core semantic binding; project it into only the Framed raw/request lineage while retaining Pure's existing receipt and Visual Config identity-resolution semantics without a C6 Framed request field.
- [x] 1.4 Extend the opt-in production-schema conformance evaluator and synthetic tests for the all-workflow receipt restriction, `header_region`, exact composition formula/provenance, Framed request local-header/context-field absence while allowing independently sourced provider content with matching spelling, rejection of legacy geometry, and Pure's absence only of Framed C6 bindings; keep the evaluator provider-free and non-runtime.
- [x] 1.5 Add focused source/Core clean-cutover tests for the closed source-restriction grammar, local-only Framed headers, no legacy context/geometry reader, restriction retention, malformed/missing direct facts, and unchanged Pure header plus identity-resolution semantics.

## 2. Profile-Derived Composition

- [x] 2.1 Replace variable-length Framed `protected_geometry` with one in-canvas CSS-pixel `header_region` that contains every permitted local header field and leaves positive height below it; derive `reserved_header` and full-width `body_safe` in `normalized-canvas` by the exact C6 formula, with selected-profile and inherited-value provenance.
- [x] 2.2 Reject an absent, former, multiple, out-of-canvas, field-noncontaining, nonpositive-body-safe, overlapping, stale, or cross-workflow geometry/composition fact through the existing source/configuration repair path without a default, generated-file read, or prior-plan fallback.
- [x] 2.3 Add resolver/configuration tests for exact valid derivation, every invalid input class, unselected sibling isolation, and Pure's absence of Framed header-region/composition facts.

## 3. Framed Raw And Request Binding

- [x] 3.1 Carry the selected composition and Core-bound source restrictions into only the immutable Framed raw contract, raw-plan `protected_composition_sha256` binding, and C5 inspection data; validate their exact digest/lineage before plan publication without removing Pure's existing parser/Visual Config restriction use.
- [x] 3.2 Compile the exact Framed provider request from non-text composition guidance and source restrictions while retaining the full continuous provider canvas; omit `context_not_to_render`, every local-header field or header-derived context, and any native region/mask transport field while preserving independently sourced provider content even when its spelling matches a header literal.
- [x] 3.3 Add the exact `reserved_header` and `body_safe` composition guides to the existing Framed raw-review contribution and bind them to the selected profile/raw lineage without creating a second review decision, acceptance fact, gate, or state record.
- [x] 3.4 Preserve shared transport opacity and Pure's C6 Framed-field isolation; add focused Framed/Core/adapter/review tests for exact bytes, local-header/context-field absence alongside same-spelling independent provider content, restriction propagation, normalized guides, malformed binding short-circuits, no partial C5 publication, and no removal of Pure's pre-existing restriction semantics.

## 4. Existing Lifecycle And Review Path

- [x] 4.1 Replace `protected_geometry_sha256` with the exact Framed `protected_composition_sha256` binding in the existing invalidation evaluator so composition or source-restriction drift requires raw rebuild and a new Complete Page Review, never a local-header refresh or raw-evidence reuse.
- [x] 4.2 Preserve one Complete Page Review decision and add negative coverage proving the C6 composition guides cannot create approval, waiver, retry, state, or a second acceptance controller; add no occupancy, collision, OCR, or automated observation capability.
- [x] 4.3 Verify diagnostics use the existing earliest source/configuration repair action, protect canonical raw lineage as a hard-stop invariant, and return the same planning checkpoint after repair.

## 5. Integration And Regression Coverage

- [x] 5.1 Add temporary-bundle integration coverage from selected Framed profile through exact composition derivation, Core, raw contract, C5 publication, review guide, and mock transport; verify Pure remains free of Framed facts.
- [x] 5.2 Extend the mock-provider public CLI journey only as needed to prove the existing authorization/review handoff retains the new exact Framed request binding without calling a real provider.
- [x] 5.3 Run focused source/Core/Framed/review/schema suites, `npm test`, strict OpenSpec validation, the Run Bundle layout self-check, and `git diff --check`; record any non-C6 baseline failure separately.

## 6. Scope Closure

- [x] 6.1 Before marking C6 complete, record implementation evidence that it made no provider-native region/mask claim or transport change, paid or real-provider probe, v3 repair, production-bundle read/migration, or manual `_generated/` edit; preserve the separate explicit human Work Request and follow-up-change boundary for any future probe or native primitive.
