## 1. Retire the Harness-owned version axis

- [x] 1.1 Inventory every active root release, README/frontmatter, archive instruction, package metadata, state, structural-edit, Visual Language, asset-manifest, Framed compiler, and report/diagnostic generation marker within the approved maintenance roots; classify only Run Bundle Work Versions, scope-bound lifecycle ordinals with existing safety meaning (such as Style Master `plan_generation`), and external reproducibility facts as retained.
- [x] 1.2 Remove root `VERSION`, `VERSION_LOG.md`, Harness/repository version presentation, archive-bump guidance, root package-version metadata and lockfile mirror, and the active `project-versioning` capability; retain normal archive and Git history guidance.
- [x] 1.3 Remove numeric state and structural schema fields from active writers, readers, diagnostics, headers, and tests while retaining existing exact ownership, atomicity, validation, and byte-preserving unsupported-input behavior.
- [x] 1.4 Remove Visual Language revision, asset-manifest format marker, and Framed compiler version/history from seeds, resolvers, profile normalization, fixtures, digests, owner tests, and active specs; name `style_master.jpg` consistently as the current presentation JPEG projection and retain its non-authoritative bounded replay behavior, current identity, external facts, and digest-based invalidation.
- [x] 1.5 Reword affected active CLI, controller, environment, command, and Run Bundle specifications/docs to describe declared-current facts versus undeclared input, without an alternative input route.
- [x] 1.6 Verify Package 1 with focused state, structural-edit, visual-config, Framed-profile, Style Master projection, environment/CLI, and package-metadata checks; run a bounded generation-marker scan that proves retained Work Versions, explicitly declared lifecycle ordinals, and external facts do not suppress prohibited Harness contract markers, then run `git diff --check`.
- [x] 1.7 Immediately update this `tasks.md` to mark the verified Package 1 tasks complete and record any deferred item before beginning Package 2. No deferred Package 1 item.

## 2. Converge schema authority and mechanical proof

> Package 2 is verified: declared envelopes, direct rejection boundaries, source-only successor initialization, static conformance, affected mock E2E, and core regression all pass. Package 3 now removes completed route-planning scaffolding from the permanent schema home.

- [x] 2.1 Extend `schema/serialization-contracts.yaml` with the complete current stage-to-`artifact_role` relation, named shared report contracts, current state-shape ownership, required fields, and exact actual anchors; keep it descriptive and non-executable.
- [x] 2.2 Cut Page Source receipts and page/deck derived publications to the one declared `schema` plus `artifact_role` envelope; remove generic wrapper schema values and parallel `stage`/`role` discriminators without changing `kind` business semantics.
- [x] 2.3 Declare and cut the resolved Page presentation binding to `page-layout` / `resolved-presentation`; declare the Page Production Task Projection as its named shared report contract; correct development-verification contract ownership to its actual emitters.
- [x] 2.4 Update direct consumers, serialization validators, accepted specs, and focused tests for the new envelope; reject undeclared envelopes before dependent layout, state, provider, or derived-artifact work.
- [x] 2.5 Change `new-version` to validate current source ownership, copy only source and overrides, initialize fresh target state, and avoid parsing or copying predecessor state, receipts, derived output, lifecycle evidence, or provider records.
- [x] 2.6 Refactor the pure architecture evaluator and real conformance scan to consume one normalized schema snapshot; detect literals and constants, field assignments, roles, anchors, prohibited numeric Harness contract markers, and accepted-spec drift without prefix whitelists or shadow schema tables; classify Work Versions, scope-bound lifecycle ordinals, and external facts by declared meaning rather than a lexical allowlist.
- [x] 2.7 Add focused negative and integration coverage for undeclared input byte preservation, fresh successor initialization, schema/owner/test/spec drift, bad anchors, invalid stage-role envelopes, and numeric-marker detection; use synthetic fixtures only and no Run Bundle data.
- [x] 2.8 Verify Package 2 with focused owner tests, successor integration coverage, static conformance tests, affected mock/public CLI E2E assertions, `npm test`, and `git diff --check`.
- [x] 2.9 Immediately update this `tasks.md` to mark the verified Package 2 tasks complete and record any deferred item before beginning Package 3. No deferred Package 2 item.

## 3. Remove completed route scaffolding from permanent schema

> Package 3 is verified: the permanent schema home contains only current production definitions, active specs/tests use domain terms rather than delivery-route labels, the schema self-checks and bounded dangling-reference sweep pass, and no deferred Package 3 item remains.

- [x] 3.1 Remove `schema/recovery-route.yaml`, `route_ref` machinery, and C1-C7 planning references from the permanent schema home while preserving the nineteen conceptual stage definitions, flow, serialization inventory, and normal README orientation.
- [x] 3.2 Update schema-home validation, parent source maps, tests, maintained docs, and accepted specs so the Backlog route is the only planning history and no test implementation lives in `schema/README.md`.
- [x] 3.3 Verify Package 3 with the schema-contract and Harness-layout self-checks, a bounded dangling-reference sweep, strict OpenSpec validation, `npm test`, and `git diff --check`.
- [x] 3.4 Immediately update this `tasks.md` to mark the verified Package 3 tasks complete; do not begin C7 until this change is applied, verified, archived, and reflected in the governing route plan. No deferred Package 3 item.
