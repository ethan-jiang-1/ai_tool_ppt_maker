## 1. Establish the One Invalid-Protocol Contract

- [x] 1.1 [workflow-inspection] Update the existing invalid-current-protocol
  projection and all focused inspection expectations so its sole action is
  `production-protocol` / `repair-current-protocol-identity` / `repair` /
  `requires_human: false`; preserve deterministic repeated results and the
  existing zero-write, zero-provider guarantees.
- [x] 1.2 [node-specification] Converge invalid source/state protocol
  observation and byte-preservation guidance on the shared owner-issued repair
  action; keep an exact local Harness locator/binding failure with its binding
  owner. Retain current one-to-one canonicalization only for declared-current
  records and remove retired-protocol terminology without changing normal
  structural `run_version` semantics. Prove a declared-current one-to-one state
  defect remains with its state owner, and only a fence-clear owner path may
  write the repair.
- [x] 1.3 [pipeline-orchestration, image-production, pptx-assembly,
  notes-injection, cli-surface] Route any present foreign, unreadable,
  incomplete, or cross-lineage source, state, final-manifest, delivery-media,
  receipt, or evidence record to the shared hard-stop before classification,
  planning, authorization, artifact reads, finalization, assembly, notes, or
  delivery writes. Direct
  finalization/delivery owners emit one typed `current_protocol_invalid` cause;
  direct build/notes CLI catches reuse the existing protocol diagnostic producer
  to project the canonical owner/action fields. Do not add a reader, exporter,
  converter, migration, fallback, recovery branch, or
  duplicate consumer action schema. Retain the existing Harness-binding owner
  for an exact locator/binding failure, narrative/workflow owner for a declared
  fresh draft, state owner for a defect after current protocol identity is
  established (while only a one-to-one fence-clear repair may write),
  execution-version owner for an exact requested/active Work Version mismatch,
  and delivery rebuild owner only for attributable current lineage with
  missing/drifted derived output. Cover every row of that boundary.
- [x] 1.4 [cli-surface] Keep direct `status` and `state` diagnostics bounded,
  secret-safe, and producer-owned for invalid current protocol identity; make
  every run-scoped direct CLI route, including `state --validate-state`, project
  the shared owner action before dependent work. Make invalid runs ineligible
  for task-projection refresh. Cover an invalid source marker, undeclared
  state, and `state --validate-state`; verify no CLI path initializes a
  provider or writes source/state/history/generated artifacts.

## 2. Converge Active Consumer Surfaces

- [x] 2.1 [playbook-execution, commands-reference] Update active Controller,
  command-discovery, and handoff guidance to present the shared repair action
  before node or route selection; remove `unsupported-protocol/export` and do
  not create a competing Markdown authority. Keep exact Harness binding,
  declared fresh draft, declared-current state-owned defect, exact
  execution-version mismatch, and attributable current delivery drift with
  their existing owners.
- [x] 2.2 [harness-charter, workflow-inspection] Update
  `charter/WORKFLOW.md`, `reference/workflow-inspection-baseline.md`, and
  `reference/workflow-inspection-ledger.md` so a present foreign, unreadable,
  incomplete, or cross-lineage production record that cannot establish current
  identity projects the same `production-protocol` repair action. Preserve the
  distinct current-owner outcomes from 2.1 plus the documents' read-only/
  zero-remote claims; leave their later deletion to Change 3.
- [x] 2.3 [environment-check, harness-script-layout] Remove protocol-era names
  from environment and architecture contracts without weakening current
  endpoint normalization or the no-undeclared-adapter/reader import guard.
  Rename `Current adapters preserve sibling locality and no legacy imports` to
  `Current adapters preserve sibling locality and declared imports` through the
  delta's `RENAMED Requirements` operation.
- [x] 2.4 [harness-charter] Replace retired whole-deck renderer and visual-slot
  production terminology in active authority guidance/coherence expectations
  with positive current Page Image and private Framed-runtime ownership; retain
  the latter only through `html-render-runtime` and current finalization
  owners.
- [x] 2.5 [cli-surface] Delete the unreachable `reset-html-production` refresh
  branch and its retired wording. Prove the public refresh parser remains
  limited to `title`, `visual`, and `notes` with no command-inventory change.
- [x] 2.6 [workflow-inspection, cli-surface, environment-check] Remove or rename
  historical protocol fixtures/test descriptions, including contradictory
  `kind: export` expectations, the mock-E2E `production-mode` /
  `repair-current-route` mismatch, and `env-check v2 mode boundary`. Retain
  generic invalid-current-shaped fixtures, structural `3_versions/vN` coverage,
  and exact requested/active execution-version mismatch coverage; do not rename
  those current concepts merely because their test values contain `vN`.

## 3. Retire the Empty Capability

- [x] 3.1 [html-slide-rendering] Before removal, verify every retained Framed
  browser/font/capture/compositor behavior is owned by `html-render-runtime`,
  `harness-script-layout`, and selected Page Image finalization, with no live
  command, parser, adapter, writer, or consumer owned by the retired capability.
- [x] 3.2 [html-slide-rendering, harness-charter] After 3.1, run the sanctioned
  spec-sync workflow narrowed to `html-slide-rendering` and require its final
  `REMOVED Requirements` operation plus `retire_capabilities: true` to delete
  `openspec/specs/html-slide-rendering/`; never delete that main spec by hand.
  Immediately remove the exact `html-slide-rendering` registry row from
  `openspec/config.yaml` in the same bounded cutover; do not delete or rename the
  retained private runtime.
- [x] 3.3 [harness-charter] Immediately run the existing authority-map evaluator
  against the post-sync main-spec set and post-removal registry, plus a planted
  missing/extra-capability case. Prove the registry is bijective after the
  cutover and restoring the valid set passes before continuing apply.

## 4. Add Falsifiable Residue Control

- [x] 4.1 [production-schema-conformance] Extend the existing provider-free
  architecture/conformance evaluator with a small, explicit active-surface
  residue seam for three semantic categories: numeric `vN` identity coupled to
  a production source/state/receipt/plan/evidence/route/adapter/candidate/
  acceptance role; the retired compound invalid-protocol action or `repair or
  export`; and an affirmative claim that invalid identity is read, migrated,
  converted, adopted, exported, or fallback-handled. Scope it only to
  `ppt_maker_harness/`, `tests/`, `tests_e2e/`, `openspec/specs/`, and
  `openspec/config.yaml`; exclude all `openspec/changes/`, Backlog, `deck_*`,
  `dpt_*`, and `_generated/` by construction. Enumerate every regular file in
  those roots; scan the closed active-text set `.mjs`, `.md`, `.json`, `.yaml`,
  `.css`, `.html`, and `.txt`; classify checked-in `.woff2` as binary without
  decoding it; and fail on any unclassified extension.
- [x] 4.2 [production-schema-conformance] Represent normal Work Version `vN`
  notation as a narrow structural occurrence category or owner-root predicate,
  not a global literal exception. Cover valid version creation/path use and an
  execution-version mismatch while treating production-role-coupled numeric
  protocol usages as violations. Explicitly preserve JavaScript `export`
  syntax, unrelated-domain compatibility wording, and normative specification
  text that defines or forbids a residue category.
- [x] 4.3 [production-schema-conformance] Add planted negative controls for
  each residue category by constructing retired literals from neutral fragments
  in supplied synthetic snapshots or temporary active-root text; add no scanner
  exception for the guard's own tests. Assert exact bounded issue location, no
  repository or bundle mutation and no provider call, then restore the exact
  input and assert the same checkpoint passes. Plant an unclassified text-like
  extension and prove the coverage inventory fails until its classification is
  restored.

## 5. Verify and Close the Change

- [x] 5.1 Run focused workflow inspection, CLI/process, state, run-bundle
  structural-version, documentation/coherence, architecture, and
  production-schema-conformance tests. Verify prerequisite short-circuiting,
  action-kind convergence, byte-identical temporary bundle trees, zero
  task-projection/state/history/generated writes, and zero provider calls on
  invalid inputs, while declared-current state and delivery repair paths retain
  their existing owners.
- [x] 5.2 Sync every accepted delta not already synchronized by task 3.2, then
  compare every delta against main specs and verify `html-slide-rendering` is
  already retired with no registry row. Do not reapply or manually recreate the
  retired capability; do not begin the active-main-spec residue scan until this
  comparison passes.
- [x] 5.3 Run scoped residue searches over declared active roots and inspect
  every remaining `vN` occurrence for structural-version or exact
  execution-mismatch ownership. Confirm no role-coupled retired protocol,
  competing recovery action, unreachable reset branch, affirmative invalid-input
  compatibility-reader/migration/conversion/adoption/export/fallback claim, or
  whole-deck renderer capability remains active. Do not require zero occurrences
  of generic words such as `export`, `migration`, or `compatibility`; inspect
  their claim context. The search uses only `ppt_maker_harness/`, `tests/`,
  `tests_e2e/`, `openspec/specs/`, and `openspec/config.yaml`, never
  `openspec/changes/`.
- [x] 5.4 Run `npm test`, `npm run test:sweep`, `openspec validate
  retire-historical-protocol-surfaces --type change --strict --no-interactive`,
  `openspec validate --all --strict --no-interactive`, and `git diff --check`.
  Archive only after the archive sync assessment observes every delta as already
  synchronized; do not reapply the retired capability. Repeat strict all-spec
  validation and `git diff --check` after archive.
- [ ] 5.5 Update `_backlog/plans/harness-signal-cleanup/progress-plan.md` at
  each verified lifecycle milestone with active/archive path, test evidence,
  and closure SHA. Stage only exact intended paths, inspect the staged diff,
  make ordinary coherent commit(s), fetch/recheck the remote parent, and
  fast-forward push without rebase, reset, force push, or broad staging.
- [ ] 5.6 Reconcile and record exact `HEAD`, local `master`, `origin/master`,
  and remote `master` SHAs after push. Mark Change 2 `done` only when all four
  match and no cleanup-related worktree residue remains.
