## 1. Freeze The Producer Contract In Tests

- [x] 1.1 Replace inventory-only coverage with recursive direct-entry candidate detection (main guard, direct parser, standalone bootstrap) and assert exact equality with `EXECUTABLE_INVENTORY`; verify every registered executable's first static import has the matching literal bootstrap entry token; test absolute/relative/separator/inert matching; scan repository JS for direct fd/stdout-device/inherited-child bypasses with only the bootstrap commit exception; allow only path-specific reasoned exceptions. Capability: `cli-surface`. Edit chain: pipeline-script.
- [x] 1.2 Add an explicit `ppt_flow` command-return registry and assert exact equality with its 12 registered commands. Capability: `cli-surface`. Edit chain: pipeline-script.
- [x] 1.3 Define per-executable/command audit records for help, usage failure, contextual failure, delegated failure, catchable interruption, prose success, and JSON success, each with a deterministic probe or explicit not-applicable reason. Capability: `cli-surface`. Edit chain: pipeline-script.
- [x] 1.4 Add failing tests for category/action semantics and invariants, all v1 leaf shapes, token grammars, `next.inspect`, allowlisting/bounds, legacy parsing with discarded stack, mandatory migrated diagnostics, whole-diagnostic fallback on malformed required fields, consumer rejection of malformed v1, malformed optional data omission, aggregate truncation, credential-free structured invocation, process-global state identity, stream write overload/callback semantics, JSON-mode registration, direct transaction success/overflow, and clean success channels. Capability: `cli-surface`. Edit chain: pipeline-script.
- [x] 1.5 Add full-channel sentinel tests for credentials, raw `.env`, prompt, provider body, thrown stack, direct pre-failure stdout/stderr, child stdout/stderr, registered JSON reports/progress, deterministic human rendering, and attempted insertion through bounded `reason`/`next.default` fields; inspect complete released stdout/stderr, not only diagnostic fields. Capability: `cli-surface`. Edit chain: pipeline-script.

## 2. Implement The Diagnostic Core

- [x] 2.1 Extend `scripts/lib/cli_error.mjs` with exact character/UTF-8 byte bounds, deterministic retention/truncation order and issue-only `omitted_count`, token grammars, closed category/action invariants (including human-owned approval), trusted issue text, safe-domain `reason` values, non-executable `next.default`, and credential-free `next.invocation {program,args}`; keep top-level validation strict and legacy parsing compatible while optional diagnostic sanitation remains fail-safe. Capability: `cli-surface`. Edit chain: pipeline-script.
- [x] 2.2 Add small Node-only constructors for source, subject, reason, lineage, leaf/aggregate issues, delegated metadata, and next action; add structured diagnostic propagation plus registered fixed-template `emitCliProgress` with direct rendering and delegated JSON-frame modes, explicitly distinct from failure envelopes. Capability: `cli-surface`. Edit chain: pipeline-script.
- [x] 2.3 Add a zero-dependency query-token first-import bootstrap with exact normalized-basename matching; share transaction/mode/active-child/commit state with `cli_error.mjs` through `Symbol.for`; preserve stream write overload/callback/return behavior and restore originals once; own 1 MiB-per-stream capture plus mode-gated safe JSON reports; register `emitCliError` and commit once through saved descriptors; render the human view, replay success, discard failure prose, fail closed on overflow, handle post-bootstrap failures and `SIGINT`/`SIGTERM` with child forwarding/category `interrupted`, avoid duplicates/stacks, enforce bounds, and test evaluation/link boundaries. Capability: `cli-surface`. Edit chain: pipeline-script.
- [x] 2.4 Replace every CLI-owned async/sync child output path with independent 1 MiB pre-exit capture (including removing `stdio:inherit` from `ppt_flow test`); mark framework children for delegated progress, validate/re-render only registered progress frames, and isolate any observed native direct-fd output behind the collector; replay non-frame success output, but on failure reconstruct parent summaries, re-sanitize only supported v1 evidence from registered children, downgrade legacy/unsupported/unregistered children, apply parent control precedence, add credential-free delegated metadata, suppress recursive envelopes/raw frames/prose, and fail explicitly on overflow. Capability: `cli-surface`. Edit chain: pipeline-script.
- [x] 2.5 Normalize provider/API errors at their source into safe status/host/reason metadata so raw response bodies or secrets never enter messages, traces, or envelopes. Capability: `cli-surface`. Implementation area: `image-generation`. Edit chain: pipeline-script.

## 3. Add Context Where JS Knows It

- [x] 3.1 Refactor Stage 1 validation records to retain source path, cheap block/field line, slide id, field, reason, expected value, and bounded aggregate issues while preserving all-errors-at-once semantics through the diagnostic and deterministic human issue rendering. Capability: `cli-surface`. Implementation area: `content-parsing`. Edit chain: pipeline-script.
- [x] 3.2 Add diagnostics to bundle layout, environment check, and style-master failures; register/schema-validate `env-check --json` non-zero reports through the safe-report API, keep success/failure JSON parseable, and use structured next invocations. Capability: `cli-surface`. Implementation areas: `run-bundle-management`, `environment-check`, `style-master-generation`. Edit chain: pipeline-script.
- [x] 3.3 Add slide/prompt/image lineage and aggregate failures to Stage 2/contact sheet under provider safety rules; migrate meaningful generation/provider-poll progress to registered safe events and suppress it in JSON mode. Capability: `cli-surface`. Implementation area: `image-generation`. Edit chain: pipeline-script.
- [x] 3.4 Add source/artifact/slide diagnostics to Stages 3-5 for plans, images, fonts, PPTX, notes, and receipts; next actions point to source or prerequisite reruns, never generated-file edits. Capability: `cli-surface`. Implementation areas: `header-lock`, `pptx-assembly`, `notes-injection`. Edit chain: pipeline-script.
- [x] 3.5 Upgrade `unified_pipeline.mjs` and all 12 `ppt_flow` commands to preserve structured failures, stage/run-dir/selection context, parent authority, human-decision boundaries, and registered safe stage progress without duplicate envelopes; explicitly register/schema-validate any documented non-zero `status/state --json` reports (or mark them not applicable in the audit). Capability: `cli-surface`. Implementation area: `pipeline-orchestration`. Edit chain: pipeline-script.

## 4. Produce Discoverable Runtime And Maintenance Entries

- [x] 4.1 Update repository-root `AGENTS.md` with maintenance routing for CLI producer and MD consumer changes to main specs plus active deltas; distinguish framework maintenance from run-bundle production. Capabilities: `cli-surface`, `node-specification`. Edit chain: pipeline-script.
- [x] 4.2 Add short producer pointers to `scripts/README.md` and `cli_error.mjs`, and consumer pointers to `md_controller_reader.mjs`/`state.mjs`; extend coherence tests to reject missing/stale/different authority targets or duplicated schema. Capabilities: `cli-surface`, `node-specification`. Edit chain: pipeline-script.
- [x] 4.3 Add canonical deck-root `AGENTS.md` constant/whitelist/tree support and have `initBundle` seed it alongside `CLAUDE.md`, both pointing to `deck-guide.md`; do not make it mandatory for legacy validation. Capabilities: `run-bundle-layout`, `run-bundle-management`. Edit chain: pipeline-script.
- [x] 4.4 Extend the `initBundle` guide seed and `workflow/00-setup/template-deck-guide.md` with the same concise diagnostic consumer guidance; test a fresh temporary scaffold end-to-end, leave README placement-map seeds unchanged, and confirm the implementation diff contains no path under `deck_ai_sdlc_keynote/`. Capabilities: `run-bundle-management`, `node-specification`. Edit chain: pipeline-script.
- [x] 4.5 Explicitly update the main `openspec/specs/cli-surface/spec.md` Purpose to cover all direct CLIs before sync/archive, and test that it retains the 12-command `ppt_flow` scope. Capability: `cli-surface`. Edit chain: pipeline-script.

## 5. Complete Focused Verification And Governing Docs

- [x] 5.1 Extend focused suites for every direct executable, all 12 `ppt_flow` commands, Stages 1-5, delegation, argument-safe invocation, aggregation, full-channel secrecy, and no duplicate envelope. Capability: `cli-surface`. Edit chain: pipeline-script.
- [x] 5.2 Update CONSTITUTION with the universal diagnostic purpose and `cli-surface` producer authority; update NODE-SPEC/AGENT_CONTRACT with consumer semantics and generated run-bundle entry; keep detailed schema in the capability. Capabilities: `cli-surface`, `node-specification`. Edit chain: pipeline-script.
- [x] 5.3 Update active command/runtime references and `openspec/config.yaml` that describe only the minimal envelope; use authority pointers instead of competing schema copies. Capabilities: `cli-surface`, `node-specification`. Edit chain: pipeline-script.
- [x] 5.4 Run focused tests, full `npm test`, strict OpenSpec validation, `git diff --check`, and a clean temporary init/check. Confirm no network, persistent fixture mutation, Python/bash asset, dependency, command/code-set expansion, or `_generated/` edit. Edit chain: pipeline-script.
