> Explore review status (2026-07-17, not implementation progress): scope, safety-contract, cross-spec merge, and testability review are complete. The unchecked tasks below remain apply-time work. Automated source-history recovery was deliberately deferred to a separate future change.

## 1. Advisory Git Observation

- [x] 1.1 Add a deep internal, zero-dependency Git-probe module to `PPTMAKER_FRAMEWORK/scripts/00-setup/env-check.mjs`, separate from the raw-output-retaining general runner. It fixes production child cwd to `process.cwd()`, uses `shell: false` with ignored stdin, and uses only argument-safe, 2-second-timeout `git --version`, conditional `git rev-parse --is-inside-work-tree`, and conditional `git rev-parse --verify --quiet HEAD^{commit}` with Node `maxBuffer` 4 KiB per captured stream. Export only a non-CLI `probeGitSafetyForTest({ run, platform, env })` seam while keeping production runner internals private; it returns only the normalized public `git` check. Its injected runner receives `{ command, args, cwd, shell, timeoutMs, maxBuffer, env }` fixed as `{ command: "git", cwd: process.cwd(), shell: false, timeoutMs: 2000, maxBuffer: 4096 }` and returns only `{ kind: "ok"|"missing"|"timeout"|"permission"|"exit"|"malformed", rc?, stdout?, stderrEmpty? }`, never a shell string, caller-controlled cwd, arbitrary command/path, environment extension, or child stderr. Construct a platform-aware allowlisted child environment that preserves only documented executable-discovery variables, sets `LC_ALL=C` / `LANG=C` plus noninteractive Git constants `GIT_CONFIG_NOSYSTEM=1`, `GIT_CONFIG_GLOBAL` to Node's platform null device, `GIT_TERMINAL_PROMPT=0`, `GIT_OPTIONAL_LOCKS=0`, and `GIT_NO_REPLACE_OBJECTS=1`; omits Image2 credentials and `PPT_FONT_DIR`; removes all inherited `GIT_*` names (case-insensitively on Windows), then sets only framework-owned Git values; and on Windows forwards at most one canonicalized `PATH`/`Path` key.
- [x] 1.2 Normalize probe outcomes into one stable base `git` check: `ok` only for `git version <major>.<minor>.<patch>` with at most the exact `.windows.<number>` or ` (Apple Git-<number>)` suffix (displaying only the numeric dotted token), plus `rc=0`/literal-`true` current-working-directory worktree confirmation plus a 40/64-hex HEAD object identifier; `warn` for missing Git, every unconfirmed-worktree result, the only expected quiet no-stdout/no-stderr `rc=1` no-HEAD result, timeout, permission, unexpected exit, or malformed output. The record omits `foundation`. Nonzero worktree results use generic unconfirmed wording rather than infer non-worktree from raw stderr. Expose no raw child output, path, remote, identity, commit, branch, status, diff, config, credential, or environment value.
- [x] 1.3 Integrate that check into direct env-check text and existing `env-check-v1` JSON output without changing its generic check-array schema, `allPass`/foundation authority, or hard-error envelope rules. Git-only warnings preserve READY and exit 0; `ppt_flow doctor` continues its existing text-only delegation and gains no `--json` flag.

## 2. Startup And Agent Guidance

- [x] 2.1 Add BOOTSTRAP Step 1 `### git` guidance synchronized with the new base check name: optional-but-recommended explanation; coding-agent verify-first and bare-metal install paths with macOS/Linux/Windows commands; `git --version` verification; current-invocation-directory scope; unconfirmed-worktree/no-verifiable-HEAD explanation; confirmed project-root requirement; separate explicit authorization before any target-root worktree inspection; no initialization in `_generated/` or a `3_versions/vN/` leaf; no nested init below an existing ancestor worktree; and no `git status` environment-repair instruction.
- [x] 2.2 Add bounded Agent checkpoint guidance in BOOTSTRAP and concise explicit-authorization/non-mutation rules in `charter/AGENT_CONTRACT.md`: recommend at most once per continuous known-source-work episode after real authoring, before a meaningful structural source change the Agent has made in the current interaction, after validated vNext, or at delivery/archival. Define the episode as one current interaction's substantive source work for one deck; a decline/deferral suppresses every further reminder until it ends. Prohibit hidden cleanliness inspection, automatic init/add/commit/push/pull/remote edits/restore/reset/discard, and clean-worktree gating. Ordinary checkpoint authorization does not authorize `git status`, `git diff`, or another inspection; after explicit user authorization for a named operation and user-supplied scope, restate and assist only with that exact operation without inferring files, staged changes, or effect.
- [x] 2.3 Reconcile the exact active Git/rollback corpus: `PPTMAKER_FRAMEWORK/README.md`, `PPTMAKER_FRAMEWORK/AGENTS.md`, `charter/AGENT_CONTRACT.md`; generated `deck-guide.md` seed and reference `workflow/00-setup/template-deck-guide.md`; `COMMANDS.md` / `scripts/change-classifier.md`; `workflow/00-setup/{00-run-bundle-concept.md,04-conventions.md,README.md}`; `workflow/01-visual/04-iterate-review-lock.md`; `workflow/04-production/05-stage-5-inject-speaker-notes.md`; `workflow/05-iteration/{README.md,02-style-iteration-workflow.md}`; and glossary. State that `vN` + Structural Versioning Path is the deck work-version authority; Git is optional user-authorized audit; source/control files are eligible rather than inherently tracked; `_generated/` is never force-tracked or a recovery target; and `commit + push` is not an automatic archive/delivery step. Replace the iteration overview's “v1 unchanged, roll back anytime” shortcut with the existing repair/vNext/new-deck escape ladder. State that this change supplies no automated Git source-history recovery, framework-owned checkout/restore/source replacement, or default recovery protocol; separately authorized named Git operations remain governed by the general authorization rule. Preserve clearly scoped framework-maintenance or optional clone diagnostics without making them deck-production Git instructions.

## 3. Fresh-Bundle Safety Boundary

- [x] 3.1 Verify and, only if required, adjust `bundle_layout.mjs#initBundle`'s fresh `.gitignore` seed to ignore `.env`, `3_versions/*/_generated/`, and scratch contents while re-including `_scratch/README.md` and leaving source/control files eligible for user-owned tracking.
- [x] 3.2 Update the fresh generated guide seed and reference template with the concise optional-Git/version/derived-output/explicit-authorization rule. Preserve create-if-absent behavior for existing `deck-guide.md` files.
- [x] 3.3 Prove in code review and tests that init, ordinary new-version, structural hidden-staging publication, pipeline execution, and doctor remain Git-independent: they must not invoke Git outside the advisory env check, require a worktree/verifiable HEAD/clean worktree, create a repository/commit, or rewrite existing bundle Git state or ignore files.

## 4. Tests And Validation

- [x] 4.1 Add focused `tests/test_env_check.mjs` coverage through `probeGitSafetyForTest({ run, platform, env })` for missing executable, normal plus exact `.windows.1`/`(Apple Git-154)` version recognition and malformed-suffix rejection, valid current-directory commit history, literal-`false` and nonzero unconfirmed-worktree results, no-verifiable-HEAD only for quiet `rc=1` versus stderr-bearing/timeout/permission/unexpected/malformed HEAD outcomes, fixed command sequence (including `HEAD^{commit}`), fixed `cwd`/`shell:false`/ignored-stdin/2-second/4-KiB-per-stream invocation bounds, platform-aware restricted child environment with one Windows path key and case-insensitive `GIT_*` filtering, deterministic locale/noninteractive/global-config/replacement-ref isolation constants, and raw-output sanitization.
- [x] 4.2 Add public direct `env-check` coverage: on POSIX, use a temporary fake Git on PATH for subprocess wiring and verify direct text/JSON plus delegated text-only `ppt_flow doctor`; on every platform, use the semantic seam plus a small pure report-assembly/rendering helper if needed for non-host-Git normalized-shape/readiness behavior rather than asserting a Windows `.cmd` launcher is equivalent. Verify exactly one `git` check; its record omits `foundation`; Git-only warning preserves foundation-ready text/READY/`allPass: true`/exit 0 when established hard checks pass; a real hard failure remains blocking; and `ppt_flow doctor --json` remains unsupported and absent from help.
- [x] 4.3 Extend `tests/test_bundle_layout.mjs` with fresh temporary-bundle assertions for exact `.gitignore` exclusions/re-inclusion, source/control eligibility, no auto-created `.git`, aligned optional-Git wording in the generated guide and reference template, and Git-independent structural publication under missing/unconfirmed/no-HEAD conditions; do not use a production `deck_*` fixture.
- [x] 4.4 Extend documentation/charter consistency tests to require BOOTSTRAP `### git`, agent/bare-metal optional/current-cwd/unconfirmed/no-HEAD wording, safe confirmed-root and no-nested-init guidance, AGENT_CONTRACT context-bounded explicit-authorization checkpoint wording, preservation of visible `vN` and the repair/vNext/new-deck escape ladder, and no automated Git history recovery/source replacement. Use an exact-token semantic scan/allowlist rather than a broad substring grep, so scoped framework-maintenance or optional clone diagnostics remain allowed. Scan the named active Git/rollback corpus for mandatory Git/`commit + push` claims, full-copy/destructive-visible-version rollback guidance, second deck-version/order sources, force-tracking/recovery guidance for `_generated/`, and the obsolete “v1 unchanged, roll back anytime” shortcut.
- [x] 4.5 Run targeted environment, bundle-layout, documentation/charter, direct-entry/return, and CLI tests; run direct `env-check` in text and JSON modes and delegated `ppt_flow doctor` in text mode; run `npm test`, `openspec validate add-optional-git-safety-guidance --strict`, and `git diff --check`; confirm no production `deck_*`, `dpt_*`, generated artifact, Git repository, commit, remote, or runtime source-recovery implementation was created or modified.
