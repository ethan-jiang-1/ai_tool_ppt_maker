## 1. Style Master Owner

- [ ] 1.1 [`style-master-generation`] Broaden the pending-successor projection predicate to use the validated current plan and exact effective predecessor identity, without requiring a difference in the three Style Master input hashes; return no pending projection after an exact current-plan promotion and retain all existing input, replay, media, provenance, grant, no-mutation, and competing-selection checks.
- [ ] 1.2 [`style-master-generation`] Add focused owner coverage for a valid successor with matching predecessor Style Master bindings and its exact promotion, plus negative coverage that stale inputs, invalid predecessor replay, or a competing selection still fail closed without writes.

## 2. Human View And CLI

- [ ] 2.1 [`image-generation`] Keep the artifact-view pending-successor branch ahead of raw authority inspection and verify that, after a non-visual source edit stales the Page Image receipt while preserving the visual projection, the matching-binding successor writes only the short pending candidate copy and marks downstream work unavailable.
- [ ] 2.2 [`cli-surface`] Add direct process coverage proving `image2 artifact-view` returns normal short-navigation success and the existing owner `next_action` for that stale-receipt successor, without a stale raw diagnostic, provider initialization, authorization, attempt, selection, or state mutation; prove that an exactly promoted successor instead reaches the ordinary accepted-selection path without a predecessor-conflict diagnostic.

## 3. Verification

- [ ] 3.1 Run the focused Style Master owner, human artifact-view, and direct CLI suites; no real-provider E2E is required because the changed path is provider-free and those tests exercise the owner-to-CLI boundary.
- [ ] 3.2 Run `npm test`, `openspec validate fix-pending-successor-artifact-view --strict`, `openspec validate --all --strict`, and `git diff --check`; record results before requesting sync/archive.
