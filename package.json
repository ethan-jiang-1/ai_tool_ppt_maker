{
  "name": "ppkmaker-framework",
  "version": "1.0.0",
  "description": "Agent-driven PPT generation pipeline — production scripts (Node.js ESM)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "vitest run --config vitest.e2e.config.mjs"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.68",
    "commander": "^13.1.0",
    "pptxgenjs": "^4.0.1",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
