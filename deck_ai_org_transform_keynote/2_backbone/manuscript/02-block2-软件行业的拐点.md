# Block 2 讲稿素材：软件行业的拐点

> 来源：`../../1_upstream_raw_material/martin-fowler-ai-sdlc-digest.md`；原文回溯：`../../1_upstream_raw_material/source-martin-fowler-ai-sdlc/`

## 从犹豫到下决心

第 4 页先把观念翻转推到台前。这不是会议名录，而是 Thoughtworks 与 Martin Fowler 召集的同一西方圈层：年初还在怀疑，年中已经转向。材料是 2026 年 2 月到 6/7 月，口播可说不到半年。台上的人按西方人来画。

年初：Fowler 自称 a total, absolute skeptic；Vella 说 there is more uncertainty than certainty。年中：The evidence is in；Everybody in the room was shipping it in production。中间转折是 Fowler 自己的那句：The game is how fast can we tell whether this is right。

Kent Beck 属于同一代、同一话语圈，但只钉验证：TDD is a superpower with AI agents。不能把“半年从保守到拥抱”安到 Beck 个人头上。

这是西方早期采用者社区的切片。后面挑战更多。不能读成全行业已经完成转型，更不能读成中国企业已经发生同样的事。

软件之所以跑得快，是因为它同时具备三样东西：完整的数字上下文、可以调用的工具，以及几乎即时的反馈。Agent 可以读代码库、改文件、跑测试、查看错误，再继续迭代。硅基系统的广度、吞吐和协作速度，在这样的环境里最容易被释放。

## 能力不是跳变，而是工作单元变了

模型能力可以用四步来讲：

```text
聊天问答 → 小任务协作 → 多步任务执行 → Agent 化工作流
```

一开始，人负责拆解问题，模型负责回答。后来模型开始完成局部任务，再后来，它可以围绕一个目标连续调用工具、保留上下文、根据反馈修正。变化的不是“聊天机器人突然变成了员工”，而是交给系统的工作单元越来越完整。

Martin Fowler 这条线最值得带走的，不是某一个工具的名字，而是软件研发工作边界的扩展：一个人借助 Agent，开始能够参与从需求澄清、方案设计、编码、测试、运行到交付的更完整链路。人的工作没有消失，但工作重心往上游移动到定义目标、边界和验收标准，往下游移动到验证、判断和治理。

## Agent = Model + Harness

Agent 不只是一个更大的模型。模型给出理解、推理和生成能力，Harness 把它放进一个真实可工作的环境：它能看到哪些上下文，能调用哪些工具，要遵守哪些规则，产出之后如何测试和评估，遇到高风险动作何时必须请人确认。

这是本次分享的第一个核心公式。它解释的不是某个产品，而是 Agent 为什么能够成为新的协作主体：模型提供能力，Harness 提供工作环境。

“协作主体”不是因为模型可以持续输出，而是因为它能够在明确边界内行动、接收反馈、修正结果，并在需要时把工作交还给人。到这里，组织才真正需要面对三种接口：人和 Agent 如何配合，系统之间如何协作，最终结果由谁接住、谁负责。

所以 Codex、各种 Coding Agent，以及未来的企业 Agent，都可以用同一组问题来理解：它们提供了什么 Harness？让模型看见什么、做什么、如何得到反馈、如何停下来交还给人？

## 关键转折

软件行业真正被改变的，不只是代码生成速度，而是验证回路的速度。问题从“模型能不能写出来”变成“我们多快能知道它写得对不对”。测试、类型检查、静态分析、运行结果和人工审查，都是把自主性变得可靠的 Sensors。

这也是软件经验对其他行业的启发：AI 价值不只来自模型本身，还来自围绕模型搭建的工作环境。

## 转场

软件行业改变的不是一个工具，而是一个工作单元。它把人、Agent、工具和验证系统接成了一条新的回路。

但软件拥有一个其他行业并不天然具备的优势：代码、工具和测试让工作更容易被机器看见、执行和验证。所以问题不再是“其他行业要不要买同样的工具”，而是：当业务流程包含权限、监管、例外和不可逆决策时，企业要怎样重新建立可以接住 AI 的工作系统？
